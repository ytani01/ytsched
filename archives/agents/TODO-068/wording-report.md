# TODO-068 wording 報告

## 対象ファイル

- `archives/todo/TODO-068. 編集画面から週表示に戻ったとき、スピナーが表示されっぱなしになるのを修正.md`（新規）
- `TODO.md`（TODO-068 の節を外し、目次へ追加。追加された行はカウントの
  更新と目次 1 行のみ）
- `archives/todo/TODO-065. 編集画面に「戻る」ボタンを追加.md`（末尾に
  2 行追記）

## 拾った語と前例

新規ファイル TODO-068.md を読んで拾った、呼び名になっている語。

| 語 | 出てくる箇所 | `git grep -cF <語> HEAD -- '*.md'` の合計 | 見立て |
| --- | --- | --- | --- |
| bfcache | 「原因」節、「bfcache（戻る/進むでページごと復元されるキャッシュ）」 | 6 ファイルで計 14 件（TODO.md・TODO-056・TODO-065 など） | TODO-056・TODO-065 で既に定着した語。前例あり |
| pageshow | 「やったこと」節、「`pageshow` の listener を足した」 | 4 ファイルで計 12 件 | JS の標準イベント名。TODO-065 で既出。前例あり |
| onloadHdr | 「原因」節、「各ページの `load` ハンドラ（`onloadHdr()`）」 | 多数のファイルで既出（TODO-038 以降で頻出） | コード中の関数名。既出 |
| doGet | 「原因」節、「`doGet()` が出したスピナー」 | 多数のファイルで既出 | コード中の関数名。既出 |
| event.persisted | 「やったこと」節 | 4 ファイルで計 10 件 | TODO-065 で既出 |
| headless | 「テスト」節、「headless の chromium では」 | 多数のファイルで既出 | 既出の一般語 |
| 実機 / 実機のブラウザ | 「テスト」節 | 多数のファイルで既出 | 既出 |
| 静的ファイル | 「テスト」節、「静的ファイルを取り直し」 | 6 件（TODO-002・TODO-008・TODO-065 など） | 既出の一般語 |
| listener | 「やったこと」節、「`pageshow` の listener を足した」 | TODO.md・TODO-065 で既出（2 件） | 既出 |

`load ハンドラ` という言い方自体は `git grep -cF` で一致 0（前例なし）
だったが、「load」「ハンドラ」はそれぞれ単独では多数の前例があり
（`load` 347 件、`ハンドラ` 11 件）、`HandlerBase` を指す一般的な
組み合わせに見える。造語というより、既存語同士の自然な組み合わせと
判断した（見立て。決めるのは main）。

TODO.md・TODO-065.md の追記分は、件数の更新（「残っている項目」
「決着させた」件数）と目次 1 行、確認済みの旨を足した 1 文のみで、
新しい呼び名になる語は見当たらなかった。

## 結論

**前例の無い語: 無し。**

`load ハンドラ` の組み合わせだけ完全一致の前例が無かったが、
構成する語はいずれも既出であり、造語という見立てではない。

## 読んだファイル

- `archives/todo/TODO-068. 編集画面から週表示に戻ったとき、スピナーが表示されっぱなしになるのを修正.md`
- `TODO.md`（差分箇所）
- `archives/todo/TODO-065. 編集画面に「戻る」ボタンを追加.md`（差分箇所）

# TODO-044 wording 報告

対象: `TODO.md` のうち、冒頭 3〜6 行の書き換えと、17〜78 行目
「## TODO-044. トークン消費の測り方と、担当の走らせ方を見直す」の節。

`git diff --cached` は無かったので、指定範囲を直接 Read して語を拾った。
前例の有無は `git grep -cF <語> HEAD -- '*.md'` で確認（HEAD にはこの
TODO-044 の節はまだ入っていない）。

## 前例なしの語（前例の件数が少ない順、0 件はまとめて）

いずれも 0 件（前例なし）。

- **コンテキスト**
  - 箇所: 「そのときのコンテキスト長で決まる」（36 行）、
    「コンテキストを育てないことが、ここにも効く」（52〜53 行）
  - 件数: 0 件
  - 見立て: LLM・トークンの文脈では一般に通用する語（プロンプトの
    文脈情報を指す業界用語）だが、このリポジトリでは初出。単語単体としては
    問題なさそうに見える

- **コンテキストを育てる／育てない**
  - 箇所: 「コンテキストを育てないことが、ここにも効く」（52〜53 行）
  - 件数: 0 件（「育てる」「育てない」単独でも 0 件）
  - 見立て: 依頼で名指しされていた通り、比喩的な言い回し。「コンテキストを
    伸ばす」「コンテキストを長く保つ」のような言い方はまだ前例が無い状態
    からの造語で、意味は通るが一般的な言い方かは判断できない。前後の文
    （キャッシュの取り直しの説明）を読めば指している内容は分かるが、
    「育てる」という動詞の選び方はこのリポジトリ・分野ともに聞き慣れない

- **概算料金**
  - 箇所: 「消費欄に概算料金を足す」（26 行）、「消費欄は
    `output` / `cache_creation` に**概算料金**を足す形にし」（57 行）
  - 件数: 0 件
  - 見立て: 「概算」＋「料金」の複合語で、意味は素直に読める。一般的な
    言い方に見えるが、このリポジトリでの金額の話自体が初めてなので
    前例が無い。問題なさそうに見える

- **概算**（単独）
  - 箇所: 「cache write は入力の 1.25 倍、cache read は 0.1 倍で概算する」
    （65〜66 行）
  - 件数: 0 件
  - 見立て: 一般語。問題なさそうに見える

- **単価**
  - 箇所: 「単価は `tools/token-usage.py` に持つ」（65 行）
  - 件数: 0 件
  - 見立て: 一般語。課金の話が初めてなので前例が無いだけに見える

- **導入価格**
  - 箇所: 「Sonnet 5 の導入価格（$2 / $10）は 2026-08-31 まで」（66〜67 行）
  - 件数: 0 件
  - 見立て: Anthropic の一時的な割引価格を指す言い方として通用しそうだが、
    一般に定着した用語かは判断できない。「導入キャンペーン価格」
    「期間限定価格」のような言い方もあり得るので、選択の余地はある

- **消費欄**
  - 箇所: 「消費欄に概算料金を足す」（26 行）、「消費欄が測っているもの」
    （32 行）、「消費欄は…概算料金を足す形にし」（57 行）
  - 件数: 0 件
  - 見立て: `~/.claude/CLAUDE.md` 側の「消費の行」を指していると思われる
    語だが、`CLAUDE.md`（プロジェクト側）にも `~/.claude/CLAUDE.md`
    （ユーザー全体）にも「消費欄」という言い方の前例は無い（「消費の行」
    という言い方ならユーザー全体の CLAUDE.md にある）。「行」を「欄」と
    言い換えている点は、TODO-021 の「足場」と同じ種類の造語の疑いがある。
    確認の要あり

- **料金の割合**
  - 箇所: 表の例「main 71% + verifier 25% + wording 3%（料金の割合）」
    （62 行）
  - 件数: 0 件
  - 見立て: 「割合」自体は既存の消費欄の書き方（`main 64% + verifier
    18% + wording 18%`）に前例があるが、「料金の割合」という組み合わせ
    自体は初出。造語というより素直な複合語に見える

- **運用の心がけ**
  - 箇所: 「main の effort を見込みどおりから始める…のは運用の心がけ
    で」（75 行）
  - 件数: 0 件
  - 見立て: 「心がけ」自体は一般語だが「運用の心がけ」という結びつきは
    このリポジトリで初出。意味は通じるが、言い換えの余地はありそう

## 前例があり、揃っているか確認した語

- **`cache_read` / `cache_creation` / `output`**（英語表記のまま）
  - `CLAUDE.md` 自体、および `archives/todo/TODO-035.*` と
    `archives/agents/TODO-035/*` に多数の前例あり。TODO-044 の書き方
    （`cache_read` が 6〜7 割を占める、`cache_creation` の 48%、など）は
    既存の使い方と表記が揃っている
- **取り直し**（キャッシュの取り直し）
  - `archives/agents/TODO-038/verifier-report.md` に 1 件前例あり
    （「キャッシュの取り直し」という現象そのものは TODO-038 の verifier
    報告で既に使われていた語）。TODO-044 の「キャッシュの取り直しも
    起きている」「92,116 を丸ごと書き直した」という使い方は、その前例と
    同じ意味で使えている
- **取りこぼし**
  - `archives/agents/TODO-027/*` などに前例あり。TODO-044 の「集計
    そのものにも取りこぼしがある」は同じ意味で自然に使えている

## 前例があり問題なさそうな語（参考）

- 書き直す／書き直し、食い違う、見込みどおり — いずれも多数の前例あり

## 読んだファイル

- `TODO.md`（3〜6 行、17〜78 行のみ）

## 前例なしの語数

9 語（コンテキスト、コンテキストを育てる／育てない、概算料金、概算、
単価、導入価格、消費欄、料金の割合、運用の心がけ）

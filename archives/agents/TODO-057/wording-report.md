# TODO-057 wording 報告

## 対象

`git diff TODO.md` で増えた範囲（`## TODO-057.` の節、108〜189 行あたり）。
このコミットに入る `.md` はこの 1 ファイルのみ。

## 前例の無い語（件数の少ない順）

### パネル（0 件）
- 箇所: 「`main.html` を 3 パネルに分け、中央だけを通常フローに残す」
  「隣のパネルには `id` を付けず `data-date` にする」「中央パネルを基準にする」
- `git grep`: 0 件（前例なし）
- 見立て: 3 週分の DOM の塊を指す語として使っている。一般には「ペイン」
  「カラム」なども使われる。IT 用語として一般に通用するとは思うが、この
  リポジトリでは初出。3 回使われており、この節の中では語義が一貫している

### 振り切り（0 件）
- 箇所: 「振り切りが速いとき」
- `git grep`: 0 件（前例なし）
- 見立て: 「フリック」（勢いよく払う操作）に近い意味で使っている可能性。
  一般には聞き慣れない言い方に見える。「速く払ったとき」のような普通の
  言い方に置き換えられそうだが、判断は main に委ねる

### 絶対配置（0 件）
- 箇所: 「前後は `position: absolute` で画面の外」「隣の週を絶対配置で
  画面の外に置くのは」「絶対配置の `top: 0` なので」
- `git grep`: 0 件（前例なし）
- 見立て: CSS の `position: absolute` の和訳としては一般的な用語（フロント
  エンドでは定着した言い方）。このリポジトリでは初出なだけで、造語には
  見えない

### 通常フロー（0 件）
- 箇所: 「中央だけを通常フローに残す」「中央だけ通常フロー」「中央の週
  だけを通常フローに残せば」
- `git grep`: 0 件（前例なし）
- 見立て: CSS のレイアウト用語 "normal flow" の和訳として一般に通用する。
  初出なだけで、このリポジトリだけの言い換えには見えない

### 送る判定（0 件）
- 箇所: 表の項目名「送る判定」、および「送る判定から `SWIPE_MAX_MSEC`
  （800ms）を外すのは」
- `git grep`: 0 件（前例なし）
- 見立て: 表の見出し語で、文脈（隣の週へ送るかどうかの判定）が直前直後
  から読み取れる。一般語の組み合わせで、造語というより表の中の短い
  ラベルに見える。判断は main に委ねる

### 滑らせて（0 件、動詞「滑らせる」）
- 箇所: 「送ったあと」の決定内容「滑らせて確定してから」
- `git grep`: 0 件（0 件だが「滑らせる」という動詞自体は一般語）
- 見立て: 普通の日本語の動詞で、造語には見えない。「送ったあと」の
  項目に「読み直す」（19 件で前例あり）と並んでいるだけで、意味は
  文脈から明確

## 前例はあるが確認したもの（参考）

- **追従**（33 件）・**ラッパー**（3 件、ロギングのラッパーなど別文脈での
  使用だが「動かす対象をまとめる箱」という意味では TODO-057 が初出）・
  **読み直す**（19 件）・**退行**（24 件）・**週バー**（30 件）・
  **週送り**（24 件）・**画面端**（12 件）・**差し替える/差し替え**
  （14／66 件）・**先読み**（9 件）・**スワイプ**（69 件）・**補正**
  （25 件） — いずれも前例があり、問題なし
- **組む**（1 件、`archives/agents/TODO-037/README.md`）: 「ToDo は 3 週
  それぞれで組む」の「組む」。1 件だけで別文脈（テストを組む）だが、
  一般の日本語の動詞であり造語ではないと見立てる
- **他の経路**（0 件、表の中の短い言い回しとして気にされていたもの）:
  実際には TODO.md 本文中に「他の経路」という語自体は無く、表の項目名
  「他の経路」1 箇所のみ。`git grep -F "他の経路"` は HEAD の
  `archives/agents/TODO-029/reviewer-report.md` に別文脈で 1 件ヒットする
  （集計スクリプトの単純合算では見えにくいので個別に確認した）。表の
  見出し語で、造語には見えない

## 前例の無い語数

**5 語**（パネル・振り切り・絶対配置・通常フロー・送る判定。「滑らせて」
も 0 件だが一般の動詞なので数には入れなかった）。TODO-025 の目安
（造語を含まない依頼書・archive は 1〜3 語）よりやや多いが、大半が CSS
用語の和訳（絶対配置・通常フロー）と表の短いラベル（送る判定）で、
一般に通用しない言い換えに見えるのは「振り切り」だけ、というのが見立て。

## 読んだファイル

- `TODO.md`（`## TODO-057.` の節のみ。他の節は変更が無いため対象外）

# TODO-084 wording 報告

対象: `git diff TODO.md`（未コミット）の TODO-084 節の書き換え。

## 依頼で指定された 5 語

- **ダブルタップ** — TODO.md（新旧とも）。`git grep`: 8 件
  （`archives/agents/TODO-069/*`、`archives/agents/TODO-080/wording-report.md`、
  `archives/todo/TODO-069.…md`、`archives/todo/TODO-080.…md`、`src/README.md`）。
  前例あり。TODO-069・TODO-080 で既に使われている語。
- **マルチタップ** — TODO.md 内。`git grep`: 2 件（HEAD の TODO.md 自身、
  旧 TODO-084 節にすでにあった語）。前例あり（ただし今回の書き換えで
  「マルチタップをキューに溜める扱いは取りやめた」という言及だけが残り、
  仕様としては使わなくなった）。
- **押しっぱなし** — TODO.md 内。`git grep`: 2 件（同様に旧 TODO-084
  節に既出）。前例あり。
- **ページ送り** — 単独では `git grep -cF "ページ送り"` は **0 件（前例なし）**。
  ただし「週送り」は多数の前例があり、「自動ページ送り」の中の部分語として
  出てくるだけで、単独の見出し語としては使っていない。
- **週送り** — TODO.md 内および多数の archives。`git grep`: 40 件超
  （`TODO-049`〜`TODO-074` の各所、`src/README.md`、`tests/README.md` など）。
  前例あり、定着した語。

## その他、diff 中で気になった語

前例の件数が少ない順。

| 語 | 出てくる箇所 | `git grep -cF` (HEAD) | 見立て |
| --- | --- | --- | --- |
| 自動ページ送り | TODO-084 見出し・本文（「自動ページ送りを始める」「自動ページ送りを止め」など） | **0 件（前例なし）** | 「週送り」＋「自動」の組み合わせで意味は通るが、この語形はここが初出。一般に通用しない造語ではないが、見出し語として定着させるなら確認したほうがよい |
| 長押し | 「長押し（リピート）と、マルチタップをキューに溜める扱いは取りやめた」 | **0 件（前例なし）** | 一般的な UI 用語で、意味は通る。この語自体を採用するかどうかは、今回「取りやめた」対象として出てきただけなので実害は小さい |
| キューに溜める | 同上の文 | **0 件（前例なし）** | 「連打を溜めて」と言い換えられる箇所もあり、表現がこの文だけの独自表現。一般に通用する言い方ではあるが、リポジトリでは初出 |
| pointerdown / pointerup | 「`onmousedown` 属性をやめて `pointerdown` / `pointerup` で拾う」 | **0 件（前例なし、2 語とも）** | ブラウザ標準の Pointer Events API のイベント名で、造語ではない。技術用語なのでそのまま書くのは自然 |
| 二重に効かない | 「ボタンの上から横に払ったときに週送りが二重に効かないよう」 | **0 件（前例なし）** | 普通の日本語の言い回しで、造語には見えない |
| シングルタップ | 「シングルタップは、これまでどおり 1 週送る」 | 1 件（TODO.md 自身、旧 TODO-084 節に既出） | 前例あり |
| リピート | 「長押し（リピート）」 | 3 件（TODO.md 自身、旧 TODO-084 節に既出） | 前例あり |
| 連打 | 「連打を溜めて1 週ずつ順に送る」 | 12 件（`archives/agents/TODO-057/*`、`archives/todo/TODO-057.…md`） | 前例あり |
| 見送る | 「ボタンの上で始まったスワイプを見送る」 | 7 件（`archives/agents/TODO-026/*`、`TODO-054/reviewer-report.md`、`TODO-057/request-implementer-2.md`、`archives/todo/TODO-054.…md`） | 前例あり |

## まとめ

- 読んだファイル: `TODO.md`（未コミットの差分部分のみ）
- 前例が無い語（＝要確認候補）: **5 語**
  自動ページ送り、長押し、キューに溜める、pointerdown、pointerup
  （「二重に効かない」は語というより言い回しなので、あくまで参考として添えた）

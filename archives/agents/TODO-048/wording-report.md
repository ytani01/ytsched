# TODO-048 wording 報告

対象: `TODO.md`（`git diff HEAD -- TODO.md` の差分部分のみ。追加されたのは
「TODO-048. Font Awesome をやめて、アイコンを SVG にする」の節と、冒頭
「残っている項目」「番号は」の 2 行）。

前例の件数は `git grep -cF <語> HEAD -- '*.md'` を合計したもの。

## 前例なしの語

- **帰属表示**
  - 箇所: 「SVG を持つ形に変えると CC BY 4.0 のほうになり、帰属表示が
    要る」（ライセンスの節）
  - 前例: 0 件
  - 見立て: クリエイティブ・コモンズ（CC BY 系）の文脈では
    "attribution" の訳語として一般に通用する専門用語。造語ではないと思う

- **修飾子**
  - 箇所: 「修飾子は `fa-lg`（1.25em）・`fa-2x`（2em）・`fa-9x`（9em）と、
    読み込み中の印を回す `fa-spin`」
  - 前例: 0 件
  - 見立て: CSS クラスの役割を指す語として一般的（「モディファイアクラス」
    の訳語として自然）。判断できないが、通用しそうな語だとは思う

- **読み込み中の印**
  - 箇所: 「読み込み中の印を回す `fa-spin`」「読み込み中の印
    （`fa-spinner` + `fa-spin`）はキャプチャに写らない」
  - 前例: 「読み込み中」は 13 件あるが、「読み込み中の印」という言い回し
    自体は 0 件
  - 見立て: 一般に "spinner" を指す言い方はいろいろあり（「読み込み
    インジケータ」「くるくる」等）、この言い回し自体は特に不自然ではない。
    ただし「印」がここで初めて出た呼び名なので、次に別の書き手が
    "spinner" を別の語で呼ぶ余地はある。判断できない

- **字形**
  - 箇所: 「字形が違うので SVG も別々に要る」「いま入っている 7.3.1 の
    字形と必ず一致する」
  - 前例: 2 件（既出）
  - 見立て: タイポグラフィの一般用語（glyph の訳語）。造語ではない

- **座標系**
  - 箇所: 「フォントの座標系は y 軸が上向きなので、反転が要る」
  - 前例: 1 件（既出）
  - 見立て: 一般的な用語。造語ではない

- **出典**
  - 箇所: 「`static/icons/` へ移し、出典を書き添えること」
  - 前例: 1 件（既出）
  - 見立て: 一般的な日本語。造語ではない

- **配布物**
  - 箇所: 「配布物から取る。Font Awesome Free の zip に `svgs/` がある」
  - 前例: 3 件（既出）
  - 見立て: 一般的な日本語。造語ではない

## 前例ありで、そのまま通った語（参考）

- SVG（43 件）、アイコンフォント（9 件）、woff2（61 件）、既定値
  （130 件）、読み込み中（13 件）、突き合わせる（5 件）、散っている
  （1 件）はいずれも既出で、意味も一貫している

## 前例なしの語数

7 語（帰属表示・修飾子・読み込み中の印・字形・座標系・出典・配布物）。
ただし字形・座標系・出典・配布物は `git grep` の件数こそ少ない
（1〜3 件）が、いずれも一般的な日本語・専門用語で、造語には見えない。
実質的に見立てが要るのは「帰属表示」「修飾子」「読み込み中の印」の
3 語。

## 読んだファイル

- `TODO.md`（差分部分のみ）

TODO-025 の目安（依頼書・archive で 1〜3 語）と比べると、この文書は
語数がやや多めだが、`symbol` `currentColor` `<use>` のような HTML/CSS の
タグ・プロパティ名はコードそのものであり呼び名の候補から除外した。
残った語はいずれも一般語・専門用語寄りで、造語らしきものは見当たらない。

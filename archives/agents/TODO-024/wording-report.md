# wording 報告（対象: TODO.md, archives/todo/TODO-024. リファクタリングで見つかった 8 件の扱い.md）

## 前例の無い語

前例の少ない順（同数のものは出現順）。

### 1. 見える動き

- 出てくる箇所:
  - `TODO.md:62`「検索モードの 1825 日スキャンを、見える動きを変えずに速くする」
  - `archives/todo/TODO-024. …8 件の扱い.md:68`「検索モードの 1825 日スキャンは、**見える動きを変えずに速くする**。」
- `git grep -cF` の件数: 前例なし
- 見立て: 文書の中では別の箇所で「挙動」という語も使っており
  （TODO-021 の見出し「リファクタリング（挙動は変えない）」など、`挙動`
  自体は多数の前例あり）、「見える動き」はそれと同じ意味を指して
  言い換えているように読める。「外から見える挙動」「利用者から見える
  動き」のような既存の言い方に寄せられる可能性があり、独自の言い換え
  に見える。判断は main に委ねる。

### 2. 壊れ続ける（画面が壊れ続ける）

- 出てくる箇所: `archives/todo/TODO-024. …8 件の扱い.md:51`
  「一度踏むだけで画面が壊れ続ける経路をここで断つ」
- `git grep -cF` の件数: 前例なし
- 見立て: 普通の日本語の組み合わせで、造語というより通常の言い回しに
  近い。ただしこのリポジトリでは初出。問題は無さそうに見えるが、
  判断は main に委ねる。

### 3. 復旧（画面が復旧しない）

- 出てくる箇所: `archives/todo/TODO-024. …8 件の扱い.md:19-20`
  「8 件のリストに書かれていた 2 件より広く、一部は画面が復旧しない
  ことが分かった」
- `git grep -cF` の件数: 前例なし
- 見立て: 一般的な IT 用語（「復旧」）で、造語ではなさそう。既存の
  「画面は直る」（`archives/agents/TODO-021/implementer1-report.md` などに
  前例あり）と同じ意味で使われており、同じ文書内でも「画面は直る」
  （TODO-027 の依頼文には無いが TODO-021 に前例あり）と表現がぶれている。
  統一するかどうかは main の判断。

### 4. 安く飛ばす（安く）

- 出てくる箇所:
  - `TODO.md:71`「ファイルの無い日を安く飛ばす」
  - `archives/todo/TODO-024. …8 件の扱い.md:70`「無い日を安く飛ばす」
- `git grep -cF` の件数（フレーズ「安く飛ばす」）: 前例なし。
  「安く」単独では 1 件（`git grep -cF 安く HEAD -- '*.md'`）
- 見立て: 「コストが安い」を「安く」で表す言い方自体はエンジニアの
  俗語として通用しそうだが、「飛ばす」との組み合わせ（＝処理をスキップ
  するコストが低い、の意）はこのリポジトリでは初出。一般に通用するか
  判断が付かない。

## 前例はあるが件数が少ない語（参考）

- **さかのぼる**: `TODO.md:71`「1825 日さかのぼる」、
  `archives/todo/TODO-024. …8 件の扱い.md:70-71`「1825 日さかのぼる」。
  `git grep -cF` は 1 件（`archives/agents/TODO-021/implementer1-report.md`）。
  前例が無いわけではないので上のリストには含めていないが、件数が
  少ないので参考として記す。造語ではなく普通の動詞。

## 前例があり、造語ではないと判断した主な語（参考）

- ゴールデンマスターテスト（17 件）、検索モード（25 件）、絞り込み
  （23 件）、スキャン（4 件、うち `TODO-025` の候補語一覧にも既出）、
  既定値（70 件）、素の（18 件、「素の GET」も同じ用法）、決めるだけ
  （3 件）、些細な（2 件）、使い勝手（5 件）、照合（15 件）、揃える
  （30 件）、経路（84 件、「経路をここで断つ」も同じ用法の組み合わせ）
  — いずれも `git grep -cF` で複数の前例があり、このリポジトリで
  既に使われている言い回し。

## 読んだファイル

- `TODO.md`
- `archives/todo/TODO-024. リファクタリングで見つかった 8 件の扱い.md`

## 前例の無い語の数

4 語（見える動き／壊れ続ける／復旧／安く飛ばす）

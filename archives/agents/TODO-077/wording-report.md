TODO-077〜083 を立てるコミットの分。

対象: `docs/design-review.md`（新規）、`TODO.md`（TODO-077〜083 の 7 節を追加）。
前例は `HEAD`（この 2 ファイルの変更前）を基準に `git grep -cF` で数えた。

## 前例なしの語（0 件）

### 基本設計
- 箇所: `docs/design-review.md:1`「基本設計のレビュー」、`TODO.md:10`「基本設計のレビュー」
- 前例: 0 件
- 見立て: 一般に通用する設計用語（basic design）。このリポジトリでの初出だが、
  造語ではなく普通の専門語に見える。問題ないと考える

### 純粋な関数
- 箇所: `docs/design-review.md:73,88`、`TODO.md:68,126`
  （「`self` をログにしか使わない純粋な関数」など）
- 前例: 0 件
- 見立て: プログラミングの一般用語（pure function の訳語）。通用する語で、
  このリポジトリで使っていなかっただけに見える

### 道具箱
- 箇所: `docs/design-review.md:75`「`convert_value()` / `check_int_range()` は
  『変換の道具箱』で」（design-review.md 内でも `「」` で括って比喩と示している）
- 前例: 0 件
- 見立て: 比喩表現。一般語ではあるが、このコードの「変換関数の集まり」を
  指す呼び名としては本文書オリジナル。**このリポジトリだけの言い換えに
  近い**。書いた本人も比喩と自覚して `「」` で括っており、TODO.md 側には
  持ち込まれていない

### 再輸出
- 箇所: `TODO.md:144,154`、`docs/design-review.md:128`
  （「`__init__.py` の再輸出をやめる」）
- 前例: 0 件
- 見立て: プログラミングの一般用語（re-export の訳語）。通用する語に見える

### 意図と実態
- 箇所: `TODO.md:138`（見出し「import の意図と実態のズレ」）、
  `docs/design-review.md:126`「意図と実態が食い違っている。」
- 前例: 0 件（フレーズとして。「意図」「実態」単独では前例あり）
- 見立て: 一般的な言い回しの組み合わせで、造語というより自然な日本語。
  問題ないと考える

### 針のラベル
- 箇所: `TODO.md:55`「目盛りの位置と針のラベルが」、`TODO.md:65`
  「サーバが埋めている針のラベルを」
- 前例: 0 件（`docs/design-review.md` 側は「文字」「ラベル」と書いており、
  「針のラベル」という組み合わせは TODO.md 側で新しく出てきている）
- 見立て: ゲージの針が指す位置に添える文字列を指す、このリポジトリ内の
  呼び名。一般に通用するとは言えないが、意味は文脈から明らかで、
  造語というより説明的な言い方に見える

### 見通し
- 箇所: `docs/design-review.md:183`（表の「効き」列）「触る前の見通し」
- 前例: 0 件
- 見立て: 一般的な日本語。問題ないと考える

## 前例が少ない語（参考）

### 中間状態
- 箇所: `docs/design-review.md:38`（見出し）「削除直後の『中間状態』で
  上書きされる」、`TODO.md:16,28`
- 前例: 3 件（`HEAD`）
- 見立て: 一般的な語。少数だが既に使われており、造語ではない

### dataclass
- 箇所: `docs/design-review.md:106`、`TODO.md:81,106`
  「表示の条件を 1 つの dataclass にまとめる」
- 前例: 3 件
- 見立て: Python の標準機能名。専門用語としてそのまま使うのは問題ない

### グローバル
- 箇所: `docs/design-review.md:138`「グローバルにあり」、`TODO.md:177`
  「グローバルな状態」
- 前例: 2 件
- 見立て: 一般的なプログラミング用語。問題ないと考える

## 前例はあるが目についた言い回し（参考、判断は main）

- **「案:」「決めること:」の太字ラベル**（`docs/design-review.md` 各節、
  `TODO.md` の TODO-077・078・081・082）: このフレーズ自体（`**案:**` /
  `**決めること:**`）は `HEAD` に前例 0 件。ただし個々の語（「案」
  「決めること」）は一般語で、見出し直後に置く書式上の工夫に見える。
  造語というより文書のスタイルの問題で、main の判断に委ねる
- **効き / 手間**（`docs/design-review.md:176` の表の列見出し）: 単独では
  前例あり（23 件・15 件）。表の列名としての組み合わせは初出だが、
  一般語の範囲内と見立てる

## 読んだファイル

- `docs/design-review.md`
- `TODO.md`（全文。TODO-077〜083 の節と、変更のない既存部分の両方を確認）

## 前例なしの語数

0 件の語: 7 語（基本設計・純粋な関数・道具箱・再輸出・意図と実態・
針のラベル・見通し）。うち「道具箱」が、一般語からの比喩という点で
最も造語寄りに見える。

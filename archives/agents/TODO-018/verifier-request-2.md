# TODO-018 verifier への依頼（2 回目）

前回の確認のあと、**実データ `tmp_test_data/` を調べて分かったことを
`docs/data-format.md` の移行の節へ反映した**。その反映が正しいかを
確かめてほしい。

`tmp_test_data/` は `.gitignore` の `tmp*` で除外されている実データ。
**個人の予定なので、報告に予定の中身を書かないこと。**件数や形式の
統計だけを書く。

コードは相変わらず旧形式のまま。**直さない。**

## 確かめること

`docs/data-format.md` の「旧形式（タブ区切り）からの移行」の節が対象。
書いてある数字と規則が、実データで再現するかを見る。

### 1. 「実データを調べて分かったこと」の数字

- `{年}/{月}/{日}.cgi` が 6733 ファイル・13361 行で、**7 列でない行が無い**こと
- 日付が読めない行、ファイル名の日付と行の `date` が食い違う行が無いこと
- 文字コードの内訳（utf-8 が 2111、euc_jp が 4430、空が 179、
  どちらでも読めないものが 13）
- `sde_id` が 13352 種類で 8 種類が重複（最大 3 回）
- `type` が `□` で始まる行が日々のファイルに 0 行、`ToDo.cgi` に 9 行
- `title` が空の行が 1148 行
- `detail` の `<br />` 4711 行、`&nbsp;` 1035 行、`&amp;` 546 行、
  `<br />` 以外のタグ 13 行
- 時刻が `28:00` の行が 1 件

### 2. 手順 1（行ごとにデコードする）の根拠

「どちらでも読めないファイルが 13 件あり、そのすべてで壊れているのは
1 行だけ。同じファイルの他の 33 行は euc_jp で読める。壊れた 13 行自体も
7 列として成立し、日付も読め、読めないのは `detail` と `place` の
2〜9 文字だけ」

これが本当か。**行ごとにデコードして `errors="replace"` で拾えば、
1 行も失わずに移せる**と言えるか。

### 3. 手順 5（実体参照の戻し方）

`<br />` を改行にし、`html.unescape()` を 2 回かけ、NBSP を半角空白に
する、という規則が対象。

- **2 回必要な根拠**（`&amp;#160;` が 2205 個ある）が実データで確かめられるか
- **3 回以上はかけない**という判断が妥当か

### 4. 「移行のあとで画面の見え方が変わらないか」の検証（いちばん大事）

文書はこう主張している。

> 旧形式で画面に見えていた文字列は、`htmlstr2text()` の結果を、ブラウザが
> 実体参照を 1 回デコードしたものだった

> 53628 項目のうち 53460 項目が、旧形式で画面に見えていた文字列と完全に
> 一致した。違ったのは 168 項目（全角括弧 157、数値文字参照 7、NBSP 4）

**この数字を自分で計算し直して、合っているか確かめてほしい。**
`src/ytsched/ytsched.py` の `htmlstr2text()` を import して使ってよい。

「旧の画面 = `html.unescape(htmlstr2text(生の文字列))`」という前提そのものが
妥当かも見てほしい（`base.html` が `{% autoescape None %}` であることの
確認を含む）。

**168 件の内訳が本当にその 3 分類だけで、想定外の変化が混ざっていないか**が
いちばん大事。分類から漏れるものがあれば、その形（どういう文字がどう
変わるか）を報告してほしい。予定の中身そのものは書かないこと。

### 5. 文書として

- 追記で矛盾が生じていないか（特に手順 2 の「列数の過不足」は、実データに
  無かったと書き足してある）
- 表や箇条書きの書式

## 報告

`archives/agents/TODO-018/verifier-report-2.md` に書く。
返事は 5 行以内で。

# TODO-018 verifier への依頼

## 何を確かめてほしいか

`docs/data-format.md` に書いた**データ形式の仕様が、実際に成立するか**を
確かめる。この項目では実装はしていない。**仕様書だけ**が成果物で、
書いてある内容が絵に描いた餅になっていないかを見てほしい。

`src/ytsched/` のコードはまだ旧形式（タブ区切り）のまま。**コードは直さない。**

## 確かめること

### 1. 仕様どおりに読み書きできるか

`docs/data-format.md` の「1 件の形」「文字コード」「壊れた行の扱い」に
書いてあるとおりの読み書きを、標準ライブラリの `json` だけで書けるか
試す。試作のスクリプトは
`/tmp/claude-649/-home-ytani-work-ytsched/9f4c4154-212b-4fe2-86a1-218a69d01254/scratchpad/`
に置いてよい（リポジトリには置かない）。

- 文書に載っている 2 行のサンプルが `json.loads()` で読めるか
- 読んだ結果が「キーと値」の表のとおりの型か
- 次の値が往復（書く → 読む）で一致するか。旧形式ではすべて壊れていた:
  改行入り、タブ入り、全角括弧、`<b>強調</b>`、`x&nbsp;y`、`p<br />q`、
  末尾が改行の文字列、絵文字。`title` と `place` でも同じことを試す
- 「壊れた行の扱い」に挙がっている 4 種類（空行、JSON として読めない行、
  オブジェクトでない行、`date` が読めない行）が、**その行だけ飛ばされて
  前後の正常な行は読める**か
- キーが欠けた行が、書いてあるとおりの既定値（文字列は空文字、時刻は
  null）で読めるか

### 2. 正規化の説明が正しいか

「判定・検索に使う正規化」の節のとおりに、全角括弧を半角にして小文字化
するだけで、「重要」「取り消し」の判定が全角・半角のどちらでも効くか。
文書の表に挙がっている先頭文字列を一通り試す。

`unicodedata.normalize("NFKC", …)` を使わない理由として `㍿` → `株式会社`、
`①` → `1` を挙げている。これが事実か確かめる。

### 3. 文書が現状のコードを正しく引用しているか

文書は旧形式の問題点をいくつも挙げている。**それぞれが本当に現状の
コードの挙動か**を確かめてほしい。実際にコードを動かして確認すること。

- 「末尾に空行が 1 つあるだけで、その日のファイルが丸ごと読めなくなる
  （`ValueError` がそのまま上へ抜ける）」
- 「utf-8 でも euc_jp でも読めないバイトが混じると 0 件として扱われ、
  その状態で 2 回保存すると `.bak` も上書きされて元のデータが消える」
- 「`（重要）打合せ` は入力直後は重要と判定されず、保存して読み直すと
  判定される」
- 「表題にタブが入ると列がずれて場所に化ける」
- 「検索は最大 1825 ファイルを 1 日ずつ開く」（`SEARCH_MODE_MAX_DAYS`）
- 旧形式の例として載せているタブ区切りの 1 行が、実際の
  `SchedDataEnt.mk_dataline()` の出力と同じ並び・同じ区切りか

### 4. 移行手順のとおりに変換できるか

「旧形式（タブ区切り）からの移行」の 6 手順のとおりに、旧形式の行を
新形式へ変換できるか試す。文書に載っている旧形式のサンプル行を使って、
「1 件の形」のサンプルと同じものになるか。euc_jp で書かれた行でも
できるか。

**手順に書き漏らしがないか**を特に見てほしい（この手順をもとに移行
ツールを作るので、抜けがあるとデータを取りこぼす）。

### 5. 文書として破綻していないか

- リンクしている `archives/todo/` のファイルが実在するか
- 表の書式が揃っているか、Markdown として壊れていないか
- 文書内で矛盾している記述が無いか

## 報告

`archives/agents/TODO-018/verifier-report.md` に書く。

- 確かめた項目と、その結果（試したコードの要点も）
- **合っていなかった記述**があれば、文書のどの行が、どう違うのか
- 判断が要る点

main への返事は 5 行以内で。報告の全文は貼らない。

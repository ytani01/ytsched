# TODO-018 verifier 報告

対象: `docs/data-format.md`（実装無し、仕様書のみが成果物）。
`src/ytsched/` は一切変更していない（読むだけ）。

試作スクリプトは
`/tmp/claude-649/-home-ytani-work-ytsched/9f4c4154-212b-4fe2-86a1-218a69d01254/scratchpad/`
に置いた（`check_old_claims.py` `check_bak_loss.py` `check_json_format.py`
`check_normalize.py` `check_migration.py` `check_migration_euc.py`）。

## 1. 仕様どおりに読み書きできるか — ○

`check_json_format.py` で確認。

- 文書の 2 行のサンプルは `json.loads()` でそのまま読め、`time_start`
  が文字列/null、`place` が `""` など、表どおりの型になった
- 往復（`json.dumps(ensure_ascii=False)` → `json.loads()`）は、改行・
  タブ・全角括弧・`<b>強調</b>`・`x&nbsp;y`・`p<br />q`・末尾改行・
  絵文字のすべてで `title` `place` `detail` とも完全一致した
  （JSON は文字列をエスケープするだけで別の文字へ変換しないため、
  旧形式のような破壊が起きない）
- 壊れた行 4 種類（空行、JSON として不正、配列など非オブジェクト、
  `date` が無い/不正）を混ぜた 6 行のうち、その 4 行だけが飛ばされ、
  前後の正常な 2 行（`id=a`, `id=c`）はそのまま読めた
- キーが欠けた行（`sde_id` `date` だけの行）は、`type`/`title`/`place`/
  `detail` が空文字、`time_start`/`time_end` が `None` になった

## 2. 正規化の説明が正しいか — ○

`check_normalize.py` で確認。

- 全角括弧を半角にして小文字化するだけの正規化で、「重要」判定
  （`(重要)` `（重要）` `!` `！` `★` `☆`）、「取り消し」判定
  （`(キャンセル` `(欠` `(中止` `(休` `(無効` `(不要` `x`／`X`、
  全角括弧版も含めて）が表どおりに全部効いた
- `unicodedata.normalize("NFKC", …)` は `㍿` → `株式会社`、
  `①` → `1` になることを実際に確認した（文書の記述どおり）

## 3. 文書が現状のコードを正しく引用しているか — ○（すべて実測で再現）

`check_old_claims.py` / `check_bak_loss.py` で確認。

- **末尾空行で `ValueError`**: 末尾に空行 1 つだけのファイルを読ませると
  `ValueError: invalid literal for int() with base 10: ''` が
  `SchedDataFile.load()` の中で捕まらずそのまま外へ抜けた
- **euc_jp でも読めないと 0 件、2 回保存で `.bak` も上書き**: 実際に
  再現した。1 回目の保存では `.bak` に元データが残るが、その後
  新規追加を挟んでもう 1 回保存すると、2 回目の保存前提チェックが
  「1 回目の保存で書いた（元データを失った）ファイル」を見るため、
  `.bak` がそちらで上書きされ、元データは main にも `.bak` にも
  残らなくなった（`check_bak_loss.py` の出力で
  `original data present in bak now? False` を確認）。文書の
  「2 回保存」は「保存 → 追加 → 保存」の意味で、単純に `save()` を
  2 回連続で呼ぶだけでは再現しない（1 回目で main が空ファイルになり、
  2 回目はバックアップ条件（サイズ > 0）に掛からないため）。文書の
  記述そのものは事実に反しないが、「2 回」の意味が分かりにくいので
  一言補うと親切かもしれない（判断は main に委ねる）
- **`（重要）打合せ` が入力直後は重要と判定されず、保存後は判定される**:
  再現した（`is_important()` が `title` をそのまま `lower()` する
  だけで、全角括弧の変換をしていない一方、`SchedDataFile.load()` は
  読み込んだ全フィールドに `htmlstr2text()`（全角括弧の変換を含む）を
  掛けているため）
- **表題にタブが入ると列がずれる**: 再現した。`title="tab\tin\ttitle"`
  で保存 → 再読み込みすると `title` が `"tab"`、`place` が `"in"` に
  化けた
- **検索は最大 1825 ファイル**: `main_handler.py` の
  `SEARCH_MODE_MAX_DAYS = 365 * 5`（= 1825）と、`while` ループが
  `get_sdf()` で 1 日ずつファイルを開いていることをコードで確認した
- **旧形式サンプル行と `mk_dataline()` の出力**: 文書 156 行目の
  サンプル `3f2a…\t2026/08/20\t10:00-11:00\t予定\t会議（重要）\t3F 会議室\t議題<br />・進捗`
  と、同じ値で組み立てた `SchedDataEnt.mk_dataline()` の実際の出力
  `3f2a…\t2026/08/20\t10:00-11:00\t予定\t会議（重要）\t3F 会議室\t議題<br />・進捗`
  が完全一致した

## 4. 移行手順のとおりに変換できるか — ○（1 点、注意点あり）

`check_migration.py` / `check_migration_euc.py` で確認。

- 文書 156 行目の旧形式サンプルを 6 手順どおりに変換すると、
  「1 件の形」の 1 行目サンプルと同じ `date` `time_start` `time_end`
  `title` `place` になった（`detail` は元の本文が違うので値自体は
  違うが、`<br />` → 改行の変換は効いた）
- euc_jp で書いた行でも、utf-8 が失敗したときに euc_jp で読み直せば
  同じ内容が取れた

**注意点（判断が要る点ではないが記録）**: 手順 5 は「`<br />` を改行に
戻し、`&nbsp;` などを素の文字に戻す」とだけ書いてあり、既存の
`htmlstr2text()` を流用してよいとは書いていない。実際
`htmlstr2text()` は全角括弧の半角化（`（`→`(`）も一緒にやってしまうため、
手順 5 で安易にこの関数をそのまま使うと手順 6（全角括弧はそのまま）を
破る。手順自体は矛盾していないが、移行ツールを書くときに
`htmlstr2text()` を流用しないよう明示したほうが安全かもしれない

もう 1 点、手順 2「タブで 7 つに分ける」は、旧形式の読み込みコード
（`SchedDataFile.load()`）が「7 項目に満たない行は空文字で埋める」
という救済をしていることに触れていない。壊れて列が減っている
既存行があった場合、6 手順どおりに素直に実装すると
`ValueError`（アンパック時の項目数不一致）になりうる。実データが
無いので実害があるかは未確認だが、「書き漏らし」の候補として報告する

## 5. 文書として破綻していないか

- `archives/todo/TODO-011. …` へのリンクは実在した
- `archives/todo/TODO-018. データ形式の見直し.md` へのリンク（6 行目）
  は**まだ存在しない**。`TODO.md` を見ると TODO-018 はまだ未完了
  （チェックボックス未了、`archives/todo/` へ移していない）ため。
  この項目自体が決着して `TODO.md` から `archives/todo/` へ移される
  ときに解消される前提のリンクだと思われるので、不具合ではなく
  「決着時にリンク先ができる」前提の記述として扱ってよいかは
  main の判断が要る
- 表（キーと値の表、判定の表）の書式は揃っており、Markdown として
  壊れている箇所は見当たらなかった
- JSON サンプルの行（41・42 行目）は 78 桁を超えるが、JSON Lines は
  1 行 1 件が仕様なので折り返せない。行長のルールに対する例外として
  妥当と判断し、不具合として報告しない
- 文書内で矛盾している記述は見つからなかった

## まとめ

書いてある仕様・引用はすべて実際に成立した。見つけたのは「判断が
要る点」2 つ（上記）と、移行手順の書き漏らし候補 1 つ（7 項目に
満たない行の救済）で、いずれも文書の記述が事実と違うわけではない。

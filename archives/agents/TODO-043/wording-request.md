# wording への依頼（TODO-043 を立てるコミット）

## 見てほしいファイル

このコミットに入る `.md` は `TODO.md` の 1 つだけ。

**見るのは差分の追加部分**（`## TODO-043.` の節と、冒頭の
「残っている項目」「番号は … から」の書き換え）。既にある「完了済み」の
目次は対象外。

## やること

このリポジトリに前例の無い語を挙げる。候補は十数語に絞り、それぞれに
見立て（一般に通用する専門用語か、造語に近いか、既にある語で置き換え
られるか）を添える。**文書は直さない。** 決めるのは main。

CSS・SVG・フォントの用語（`clip-path`、`polygon`、`字面`、`箱`、
`目盛り` など）が出てくる。**前例なし ＝ 造語ではない**ので、そこは
見立てで分けてほしい。

なお `字面` は、TODO-042 で「グリフが実際に描かれる範囲」という
タイポグラフィの意味で使うと決めた（それ以前の前例は「文面上」という
別の意味）。今回もその意味で使っている。

## 報告

`archives/agents/TODO-043/wording-report.md` に書く。

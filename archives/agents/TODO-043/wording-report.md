# wording 報告（TODO-043 を立てるコミット）

## 対象

`TODO.md` の差分の追加部分のみ（`## TODO-043.` の節と、冒頭
「残っている項目」「番号は … から」の書き換え）。「完了済み」の目次は対象外。

## 前例の無い語・少ない語（前例の件数が少ない順）

### 前例なし（0 件）

- **`アイコンフォント`**
  - 箇所: 「針（▶）をインライン SVG の三角形にする」の直前の段落
    「針と基準線は図形であって文字ではないのに、Font Awesome のアイコン
    （…）で描いている」の前振り、および見出し
    「ゲージの針と基準線を、アイコンフォントでなく図形で描く」
  - `git grep`: 0 件
  - 見立て: 一般に通用する語（Font Awesome のようにグリフをアイコンとして
    使うフォントを指す標準的な呼び方）。このリポジトリでは初出なだけで、
    造語ではないと考えられる

- **`横棒`**
  - 箇所: 「基準線（＝）を `border-top` の横棒にする」
  - `git grep`: 0 件
  - 見立て: 普通の日本語。造語ではない

- **`（角の）接ぎ方`**
  - 箇所: 「`border` の三角形（角の接ぎ方を利用する書き方）」
  - `git grep`: 0 件（「接ぎ方」で検索）
  - 見立て: CSS で border を三角形に見せる技法（隣り合う辺の境界が斜めに
    接する性質を使う）を指した言い換え。標準の呼び名（「ボーダートリック」
    などの通称はあるが定訳は無い）が無いため、書いた人が説明的に言い換えた
    もの。造語というより説明であり、他に置き換えられる定訳が無いので
    妥当と思うが、判断は main に委ねる

- **`clip-path` / `polygon` / `viewBox` / `border-top` / `--fa-width`**
  - 箇所: CSS/SVG のプロパティ名・要素名として複数箇所（依頼書でも
    言及済み）
  - `git grep`: いずれも 0 件
  - 見立て: CSS/SVG の仕様上の識別子で、日本語の言い回しではない。
    造語ではない（依頼書の指摘どおり）

### 前例が少ない（1〜6 件）

- **`図形`**
  - 箇所: 見出し、「針と基準線は図形であって文字ではない」など複数
  - `git grep`: 1 件
  - 見立て: 一般的な日本語。問題なし

- **`描き方`**
  - 箇所: 「図形で描けば、この 3 つは全部要らなくなる」周辺、
    「描き方だけを入れ替える」
  - `git grep`: 1 件
  - 見立て: 一般的な日本語。問題なし

- **`三角形`**
  - 箇所: 「針（▶）をインライン SVG の三角形にする」ほか
  - `git grep`: 5 件
  - 見立て: 一般的な日本語。問題なし

- **`目盛り`**
  - 箇所: 「目盛りのラベル（`gage_r0`〜`gage_r15`）は文字なのでそのまま」
  - `git grep`: 6 件
  - 見立て: 一般的な日本語。問題なし（依頼書でも既存語として言及あり）

- **`要らなくなる`**
  - 箇所: 「図形で描けば、この 3 つは全部要らなくなる」
  - `git grep`: 4 件
  - 見立て: 普通の言い回し。問題なし

- **`fa-2x` / `fa-xs`**
  - 箇所: 「針が `fa-2x`、基準線が `fa-xs` で箱の高さが違うのに」
  - `git grep`: それぞれ 5 件・2 件
  - 見立て: Font Awesome の既存クラス名。造語ではない

## 見なかったもの

「字面」は依頼書の指示どおり、TODO-042 で決めたタイポグラフィの意味
（グリフが実際に描かれる範囲）での使用と確認した（42 件中、TODO-042 での
決定を踏まえた用法）。今回の文書内でも同じ意味で一貫しているため、
候補から外した。

## 読んだファイル

- `TODO.md`（差分の追加部分のみ）

## 前例の無い語数

7 語（アイコンフォント、横棒、接ぎ方、clip-path、polygon、viewBox、
border-top、--fa-width を数えると実質 8 個だが、CSS/SVG の識別子 5 つを
まとめて 1 グループとみなせば実質的な「語」としては 3〜4 語程度）。

## 追記（基準線も SVG にしたあと）

main が TODO-043 の節を 2 か所直した後の差分を見た。増えた語のうち
指定された `rect`・`流儀`・`見当`・`寸法` の前例を確認する。

- **`rect`**
  - 箇所: コード例の `<rect width="12" height="2" y="0" />` ほか、
    チェックボックス「基準線（＝）もインライン SVG の横棒 2 本にする」
  - `git grep`: 8 件
  - 見立て: SVG の要素名。仕様上の識別子であり造語ではない

- **`流儀`**
  - 箇所: 「基準線は横棒なので `border-top` でも描けるが、**針と流儀を
    揃える**ほうを採った」
  - `git grep`: 10 件
  - 見立て: 一般的な日本語。問題なし

- **`見当`**
  - 箇所: 「上の寸法（`13x18`、`12x6`）は今の見え方に合わせた見当で、
    実際の値は画面を見て決める。」
  - `git grep`: 17 件
  - 見立て: 一般的な日本語（「おおよその見積もり」の意）。問題なし

- **`寸法`**
  - 箇所: 同上「上の寸法（`13x18`、`12x6`）は…」
  - `git grep`: 1 件
  - 見立て: 一般的な日本語。前例は少ないが造語ではないと考えられる

### 書き換えられた「接ぎ方」の言い換えについて

「（角の接ぎ方を利用する書き方）」→「（隣り合う辺が角で斜めに接する
性質を利用する書き方）」に書き換わっていることを確認した。指摘済みの
点であり、依頼により再確認は不要とのことなので見立ては添えない。

### 前例の無い語数（追記分）

今回の追記対象（`rect`・`流儀`・`見当`・`寸法`）はいずれも前例があり、
前例なしの語は無し。

# TODO-095 / TODO-100 wording 報告

## 対象にしたファイル

- `TODO.md`（差分のみ。TODO-100 の節を追加、TODO-095 の節を削除、
  目次に TODO-095 を追加、ヘッダの残件と件数を更新）
- `archives/todo/TODO-095. ruff の規則を増やすか決める.md`（新規）
- `archives/agents/TODO-095/verifier-report.md`

  **この時点では存在しない**（`archives/agents/TODO-095/` ディレクトリ自体が
  無く、担当がまだ報告を書いていない）。したがって今回の確認対象には
  含められなかった。verifier の報告が書かれたら、別途 wording を
  走らせる必要がある。

前例の判定は `git grep -cF <語> HEAD -- '*.md'` で、HEAD（今回のコミット前）
を基準にした。

## 前例の無い語・少ない語

前例の件数が少ない順に並べる。

### 前例なし（0 件）

| 語 | 出てくる箇所 | 見立て |
|---|---|---|
| `pyupgrade` | archive: 「`UP`（pyupgrade） \| 0 \| —」（規則の表） | ruff プラグインの正式名。一般に通用する固有名詞で、造語ではない |
| `ambiguous-unicode-character` | archive: 「`RUF001`-`RUF003`（ambiguous-unicode-character）は、日本語のコメントと docstring に含まれる全角文字を「紛らわしい」と拾ったもの」 | ruff の規則名をそのまま引用したもの。造語ではない |
| `PTH` | TODO.md（TODO-100 節）: 「`pyproject.toml` の `extend-select` に `PTH` を足す」など複数箇所 | ruff のプラグイン記号（flake8-use-pathlib）。固有名詞で造語ではない |
| `B007`（ruff の規則コード） | archive:「`B007` の 1 件を直した」 | ruff の規則コードそのもの。造語ではない |
| ループ変数 / 未使用ループ変数 | archive: 「`ytsched.py:870` の未使用ループ変数」「ループ変数 `i` を本体では使わず」 | 一般的な CS 用語（loop variable の訳語）。このリポジトリでの初出だが、一般に通用する専門用語に見える |
| 誤検知 | archive: 「このプロジェクトでは誤検知にしかならない」 | 一般的な IT 用語（false positive の訳語）。初出だが特に問題なさそう |
| 候補の規則 | archive: 「候補の規則を実際に走らせて、指摘の数を数えた」 | 「候補」＋「規則」の組み合わせで、造語というより普通の言い回し。判断できない |
| 紛らわしい文字 | archive: 見出し「ambiguous-unicode-character」の説明「紛らわしい」の周辺（「紛らわしい文字」という語自体はまとまった形では出てこないが、「紛らわしい」＋「全角文字」を合成すると近い意味になる） | 単独の語としては存在しないので候補としては弱いが、念のため挙げる。一般的な言い回し |
| 全角文字 | archive: 「日本語のコメントと docstring に含まれる全角文字」 | 一般的な IT 用語。初出だが問題なさそう |
| 書き換えと同時に足す | TODO.md（TODO-100 節）・archive 末尾:「書き換えと同時に足すことにして」 | 定型句というより地の文の言い回し。造語ではなく、単に初めて使った表現に見える |

### 前例 1 件

| 語 | 出てくる箇所 | 前例 | 見立て |
|---|---|---|---|
| `bugbear` | TODO.md（今回削除される TODO-095 節、HEAD 時点でまだ残っている）:「`B`（bugbear）や `SIM` を足すかを決める」／archive にも同じ表現 | `HEAD:TODO.md:54` に 1 件（今回のコミットで TODO-095 節ごと archive へ移る、その移動元） | ruff プラグイン（flake8-bugbear）の正式名。造語ではない |
| キャッシュ破棄（ループ） | archive: 「`ytsched.py` のキャッシュ破棄のループで」 | `HEAD:archives/agents/TODO-004/implementer-report.md:117` に 1 件（「キャッシュ破棄ループ」という語で登場） | 既に TODO-004 の報告で使われている表現。このリポジトリ内で定着しつつある言い回しに見える |
| 既定の規則 | archive:「ruff は既定の規則（`E4,E7,E9,F`）に `I` が乗っただけ」 | 1 件 | 「既定」＋「規則」の組み合わせ。一般的な言い回しで問題なさそう |
| 指摘の量 | TODO.md（TODO-100 節）:「規則を増やすかは決めていない。足すと決めたら、指摘の量を見てから」（※実際には archive 側にも近い表現） | 1 件 | ruff の警告件数を指す言い回し。造語というより普通の表現に見える |

## 読んだファイル

- `TODO.md`（作業ツリーの差分のみ）
- `archives/todo/TODO-095. ruff の規則を増やすか決める.md`

`archives/agents/TODO-095/verifier-report.md` はまだ存在しないため読めていない。

## まとめ

前例なしの語: 10 語（うち `pyupgrade` / `PTH` / `B007` / `ambiguous-unicode-character`
は ruff 側の固有名詞で、造語の疑いはほぼ無い）。
前例 1 件の語: 4 語。

全体として、TODO-025 が挙げた「造語が入った報告」の 15 語という目安には
近い数だが、内訳を見ると大半が ruff の規則名・プラグイン名という固有名詞で、
TODO-021 の「足場」のような**言い換え型の造語には見えない**。
判断が要りそうなのは「書き換えと同時に足す」「候補の規則」「紛らわしい文字」
くらいで、これらも一般的な言い回しの域を出ていないように見える。
決めるのは main。

## 追記: verifier-report.md

`archives/agents/TODO-095/verifier-report.md` を追加で確認した。
前例の判定は他と同じく `git grep -cF <語> HEAD -- '*.md'`。

### 前例なし（0 件）

| 語 | 出てくる箇所 | 見立て |
|---|---|---|
| 裸で参照 | 「他に `i` を裸で参照している箇所は見当たらず」 | 「（型注釈や接頭辞などで包まず）そのまま変数名を使う」という意味の比喩的表現。一般的な言い回しとまでは言い切れず、初出。このリポジトリだけの言い回しに見える可能性があり、判断できない |
| ループ本体 | 「ループ本体（実行される行）は `_i` を参照していない」 | 一般的な CS 用語（loop body の訳語）。初出だが専門用語として通用しそう |
| 実行される行 | 同上（「ループ本体（実行される行）」という注釈） | 「ループ本体」を説明するための言い換え。一般的な言い回しで、造語には見えない |
| debug 出力 | 「コメントアウトされた debug 出力の中の」 | 一般的な IT 用語（ログ出力の一種を指す言い方）。初出だが問題なさそう |

### 前例が少ない語（参考）

「報告どおり」「コメントアウトされた」「改名」「left unchanged」は前例が
複数あり、造語ではないと判断できる。候補には含めない。

### まとめ（追記分）

前例なしの語: 4 語。うち「裸で参照」は言い回しとして目新しく、判断が
要ると考える。他の 3 語は一般的な IT/CS 用語の範囲に見える。

# TODO-141 reviewer 依頼

TODO-141 の未コミット差分を読み、次をレビューする。

- 選択した `(sde_id, trashed_at)` だけを一度の書き直しで削除し、未選択、
  表示外、壊れた行、非 object 行、元のパーミッションを保持できるか。
- 0 件、組数不一致、不正日時、一致なしを安全に扱い、誤削除しないか。
- 個別削除と全消去が表示中だけの全選択からの一括削除へ置き換わり、未選択時
  無効、部分選択、件数入り確認、削除後の遷移が要件どおりか。
- JavaScript 無効時や改変リクエストでも意図しない削除にならないか。
- テストと `docs/User.md` が TODO-141 の受け入れ条件を十分に覆うか。
- TODO-141 の範囲を超えた変更や、既存の復活機能への回帰がないか。

テスト実行やコード変更はせず、確信度の高い指摘だけを
`archives/agents/TODO-141/reviewer-report.md` に書く。コミットもしない。

# TODO-175 verifier 報告

対象: `docs/token-usage-analysis.md`。文書は直していない。

## 合格した項目

- 消費行の集計（99 件・合計 $508.2・平均 $5.13・中央値 $3.30）: 一致
- 「内訳は cache_read が 3 分の 2」の 3 行（$562.6/66.0%, $169.8/19.9%,
  $119.8/14.1%）: 表示桁で一致
- 「担当別では main が 73%」の表: main 72.7% 一致。リクエスト数・
  コンテキスト長も表示桁でおおむね一致（下記の「時間経過による差」を除く）
- 「会話は進むほど高くなる」の 100 回以上の帯（17 本・3,007・$250.7・
  40.2%・$0.083）、会話の長さの中央値 43 回・最長 705 回: 一致
- 上位 5 件の料金・main の割合（TODO-077 96%、TODO-069 91%、TODO-047 60%、
  TODO-049 46%、TODO-048 87%）と TODO-077 の $43.8 / 8.6%: `stat.py` の
  再計算と一致
- 「効かない節約」verifier $0.44・reviewer $0.60・wording $0.18、
  TODO-052 の $2.6→$1.0 / $2.6→$0.3 は `archives/todo/TODO-052. …` の
  表と完全一致
- 「効いているか」の番号帯ごとの件数・料金の中央値（TODO-040〜179 の
  7 帯すべて）: `stat2.py` の再計算と一致
- 項目の題名 TODO-049（1 画面 1 週間の表示にする）、TODO-077・TODO-069 の
  番号とファイルの対応: 存在は確認
- `TODO.md` の TODO-175 の節: 見込みの行あり、書式は他の項目と揃っている

## 不一致・要確認

1. **本文と表が矛盾（要修正判断）** — 「高くつく項目の特徴」の本文
   「高額な上位 5 件は、どれも main の割合が 87% 以上」に対し、直後の表は
   TODO-047 が 60%、TODO-049 が 46%。表の数値自体は正しい（`stat.py` と
   一致）が、**本文の「87% 以上」は 5 件中 2 件と矛盾している。**

2. **項目の題名が省略・変形されている** — TODO-069 の題名を文書は
   「数ヶ月ぶんの週を DOM に持つ」としているが、`archives/todo/` の
   ファイル名は「数ヶ月ぶんの週を DOM に持ち、週移動でページを読み直さない」。
   後半が丸ごと落ちていて、動詞も「持ち」→「持つ」に変わっている。
   TODO-047・TODO-048 も「やめて、」の読点が文書側で落ちている（軽微）。

3. **「何を数えたか」の数字が、今数え直すと少し増えている** —
   `total.py` を実行すると リクエスト 20,177（文書は 20,161）、
   概算 $853.4（文書は $852）。担当別も main 8,854 件（文書 8,842）、
   verifier 4,464 件（文書 4,460）など、implementer/reviewer/wording/
   writer/runner は文書と完全一致。**この検証セッション自体が同じ
   transcript に記録され続けているための増分とみられ**、収集方法や
   スクリプトの不具合ではなさそうだが、表示桁での一致にはならなかった
   ので報告する。「会話は進むほど高くなる」の 20〜49 回・50〜99 回の帯も
   同じ理由で割合が 20.2%→20.3%、34.8%→34.7% とずれている。

4. **「見込みから外れたら」の 68 件/31 件を厳密には再現できず** —
   依頼にある `parse.py`/`total.py`/`curve2.py` にはこの比較を出す
   コードが無く、見込み行と実施行の担当構成を独自の正規表現で比較して
   再現したところ 67 件・中央値 $2.7 / 32 件・中央値 $4.6 となった
   （文書は 68 件 $2.65 / 31 件 $5.20）。件数は 1 件ずれ、外れた側の
   中央値は $4.6 と $5.20 で表示桁を超えて違う。**元の判定方法（何を
   もって「見込みどおり」とするか）を再現できるスクリプトが残って
   いないため、これは私の再現方法の粗さによる可能性がある。** 元の
   スクリプトを確認してもらうか、判定条件を文書化してほしい。

5. **「100 行あたり」の数字と TODO-063/TODO-059 の行数を検証できず** —
   これらのもとになった `diff.json`
   （`/tmp/.../scratchpad/diff.json`）を生成したスクリプトが依頼に
   挙がった 3 本（`parse.py`/`total.py`/`curve2.py`）に無く、
   再現できなかった。`diff.json` の中身自体は文書の数字と辻褄が合う
   （TODO-063: 81+9=90 行、TODO-059: 134+14=148 行）が、**単純に
   `git show --numstat <TODO-063 の fix コミット> -- src tests` を
   叩くと 4+9=13 行にしかならず**、`diff.json` がどの範囲・どのコミットを
   足し合わせたものかを確認できなかった。implementer 45 件 $1.35 /
   main 実装 51 件 $1.78 / main 85% 以上 29 件 $2.08 も同様に未検証。
   **生成に使ったスクリプトが scratchpad に残っていない**ので、
   main の側で経緯を確認してほしい。

## 判断が要る点

- 上記 1（本文と表の矛盾）は文書の記述ミスに見えるので、修正が要るか判断してほしい
- 上記 4・5 は元のスクリプトが残っておらず、私の再現方法が粗いだけの
  可能性がある。元のスクリプト（または判定条件）を示してもらえれば、
  再検証する

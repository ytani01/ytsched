# TODO-137 verifier 報告

## 1. lint / pytest

- `mise run lint`（fmt・fmtjs・ruff check・basedpyright・mypy・eslint）→ すべて通過
- `uv run pytest tests -q` → **569 件成功**（実装者報告の 522+47 と一致）

## 2. アプリの起動・ブラウザ操作（playwright、`--datadir` は一時ディレクトリ）

すべて期待どおり。

- 週間表示のミニカレンダーの `2026/08` キャプションをクリック →
  `data-view="month"`、URL に `view=month`、月間パネル 3 個
  （`2026/01〜2026/06` / `2026/07〜2026/12` / `2027/01〜2027/06`）
- アクティブなブロックのミニカレンダーが 6 個、`.my-month-grid` に
  2 列×3 行で並ぶ（`grid_count=6`）。先頭月は 1 月・7 月のみ
- スクリーンショット `/home/ytani/tmp/playwright-mcp/todo137-month-view.png`
  で 2×3 のレイアウト・今日/表示中の週の枠・祝日色を目視確認。
  テンプレートタグの生残りなし
- 月間表示で日付（`2026-03-12`）をクリック → `data-view="week"`、
  週間表示に戻り、クリックした日を含む週（`03/10`〜`03/15`）が表示
  （`todo137-week-view-after-click.png` で確認）
- 月間表示でマウスの左右ドラッグ →
  `2026/07〜2026/12` → `2027/01〜2027/06`（6 ヶ月移動）、
  ドキュメント再読み込み無し（`nav_count` の増分 0）
- フッターの `#forward_button`（＞） → `date=2027-01-01`（6 ヶ月移動）、
  再読み込み無し。`#back_button`（＜）で元のブロックへ戻るのも
  再読み込み無し
- キーボード `ArrowRight` → 6 ヶ月移動、再読み込み無し
- ブラウザの戻る（`page.go_back()`）→ `view=month` の URL・
  `data-view="month"` に復元

これで「1 回目の 6 ヶ月移動でページの読み直しが起きない」
（前後ブロックの先読み）を確認できた。

## 3. 既存の週間表示のリグレッション確認

- 週間表示のフッター＞クリック → `2026-09-01` → `2026-09-07`
  （週送り、既存どおり 1 週分）
- 週間表示のミニカレンダーを左右ドラッグ →
  キャプション `2026/09` → `2026/10`（TODO-136 の 1 ヶ月移動が
  そのまま働く）
- 検索（`search_str=test`）→ `data-view="week"` のまま、
  検索フォームの画面が正しく表示（`todo137-search-view.png`）

## 4. サーバログ

`server.log` に `error` / `exception` / `traceback` の該当なし。

## 結論

依頼された確認はすべて期待どおり。不具合の指摘は無し。

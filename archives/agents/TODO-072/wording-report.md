# TODO-072 wording 報告

対象ファイル（そのコミットに入る `.md` 4 件、`TODO.md` は差分のみ）:

- `TODO.md`（差分のみ。目次の入れ替え）
- `archives/todo/TODO-072. ゲージの針の上の相対日数の単位を調整する.md`
- `archives/agents/TODO-072/README.md`
- `archives/agents/TODO-072/verifier-request.md`
- `archives/agents/TODO-072/verifier-report.md`

`git grep -cF <語> HEAD -- '*.md'` で前例を数えた（コミット前の `HEAD` 基準）。

## 前例の無い語（前例 0 件）

前例が少ない順（同数はまとめた）に並べる。

### 相対日数

- 出てくる箇所: `archives/todo/TODO-072. ….md` のファイル名・見出し
  （「ゲージの針の上の相対日数の単位を調整する」）。本文中には出てこない
- 前例: 0 件
- 見立て: 「相対」＋「日数」の単純な組み合わせで、意味は文脈から
  取りやすい。ただし本文側は「差」「週の差」と言い換えていて、
  見出しだけこの語を使っている。**呼び方が 2 通りある**とも言えるので、
  一般に通用する語かは判断できない

### 境界値

- 出てくる箇所: 直接この語は使われていない。近い言い回しとして
  `TODO-072. ….md` の「境界は、月曜どうしの差なので 7 日刻みに飛ぶ」
  （36 行目）、`verifier-request.md` の「境界の値が仕様どおりか」
  （4 番）、`verifier-report.md` の「3. 境界値」の見出し
- 前例: `境界値` そのものは 0 件（`境界` 単独は 35 件で既出）
- 見立て: ソフトウェアテストで一般的な用語（境界値分析）で、
  このリポジトリだけの言い換えには見えない。一般に通用する語だと思う

### とっさに

- 出てくる箇所: `TODO-072. ….md` 14 行目「とっさに読めなかった」
- 前例: 0 件
- 見立て: ごく普通の日本語の副詞。造語の心配は薄い

### 素で叩く

- 出てくる箇所: `TODO-072. ….md` 85 行目「`ruff format` を素で
  叩いてはいけない」
- 前例: 0 件（`叩く` 単独は既出だが未計測）
- 見立て: 口語的だが一般的な言い回し（「素のまま実行する」の意）。
  このリポジトリの前の項目（TODO-070 相当の作業）で実際に起きたことの
  言い換えで、特別な用語ではないと思う

### 書式を…引き上げた（引き上げた）

- 出てくる箇所: `TODO-072. ….md` 48 行目の見出し
  「書式をテンプレートから引き上げた」、53 行目「書式を Python 側へ
  寄せ」
- 前例: `引き上げた` 0 件
- 見立て: 「引き上げる」を、値の上昇でなく「担当を移す」の意味に
  比喩的に使っている。分かりにくくはないが、このリポジトリでは
  今までコードの移動を指すのに使われていない言い回し。一般に通用する
  比喩だとは思うが、判断できない

### 思い込み

- 出てくる箇所: `README.md`（TODO-072 分担理由）「同じ思い込みで
  両方書いた本人が確かめても意味が薄い」
- 前例: 0 件
- 見立て: ごく普通の日本語の名詞。造語の心配は薄い

### 往復する

- 出てくる箇所: `TODO-072. ….md` 89 行目「88 と 78 の間を往復する
  ことになる」
- 前例: 0 件
- 見立て: ごく普通の日本語の動詞。造語の心配は薄い

## コード識別子（参考、造語判定の対象外と見立て）

`calc_gage_label` / `gageDiffLabel` / `DAYS_MONTH` は前例 0 件だが、
実装で新しく付けた関数名・定数名で、文章の言い回しではなくコードの
名前。この項目で初めて登場するのは当然なので、造語の候補としては
挙げない（念のため記載のみ）。

## 前例があった語（参考）

`ゲージ`（155）・`針`（224）・`針の上`（既出、TODO-066）・`目盛り`
（58）・`突き合わせ`（65）・`分岐`（155）・`食い違う`／`食い違い`
（26／41）・`丸め方`（1）・`全一致`（28）・`独立に`（16）・
`書き写す手間`（3、`書き写す手間のほうが大きい`は TODO-072 と
ほぼ同文が既に 1 件ある）・`名指しして`（3）などは、いずれも既に
このリポジトリで使われている。

## 読んだファイル

- `TODO.md`（差分）
- `archives/todo/TODO-072. ゲージの針の上の相対日数の単位を調整する.md`
- `archives/agents/TODO-072/README.md`
- `archives/agents/TODO-072/verifier-request.md`
- `archives/agents/TODO-072/verifier-report.md`

## 前例の無い語数

7 語（`相対日数`・`境界値`・`とっさに`・`素で叩く`・`引き上げた`・
`思い込み`・`往復する`）。目安の 1〜3 語をやや超えるが、いずれも
特殊な専門用語ではなく普通の日本語の組み合わせで、TODO-025 の
「足場」のような造語には見えない。

# TODO-046 wording 報告

対象: `git diff TODO.md` で追加された文面（冒頭 2 行 + 「## TODO-046.」節）。

## 挙がった語（前例の件数が少ない順）

### 版のズレ
- 出てくる箇所: 「~/.cache/ms-playwright にあるビルドは playwright-mcp が
  入れたもので、uv run --with playwright が取ってくる版とは合わず
  起動しない（TODO-045で実際に起きた）。追加のダウンロードが要らず、
  **版のズレ**も再発しないので、システムの chromium を使う。」
- `git grep -cF` の件数: 前例なし（0 件）
- 見立て: 「版」単体は多数の前例がある（`.claude/agents/verifier.md` など）。
  「版のズレ」というつながりでの前例は無いが、一般の日本語として不自然
  ではなく、造語というより普通の複合表現に見える。判断は main に委ねる

### 画面のキャプチャ
- 出てくる箇所: 見出し「画面のキャプチャを撮るスクリプトを置く」
- `git grep -cF` の件数: 前例なし（0 件、ただし「キャプチャ」単体は
  `archives/agents/TODO-007/implementer-report.md` など 3 ファイルに
  前例あり、「撮る」単体も複数のファイルに前例あり）
- 見立て: 一般的な IT 用語（画面キャプチャ／スクリーンショット）で、
  このリポジトリでの言い換えではなく通常の言い方に見える

## 前例のある語（参考、造語の疑いは薄い）

- 「出力先」「置き場所」「詳細」「開閉」「playwright-mcp」
  「executable_path」「ms-playwright」は、いずれも `archives/` 内に
  1 件以上の前例があった（`docs/data-format.md` などにも出てくる）

## 全体の見立て

追加された文面のうち、機械的に前例なしと出た語（フレーズ単位）は
「版のズレ」「画面のキャプチャ」の 2 つのみで、いずれも要素（「版」
「ズレ」「画面」「キャプチャ」「撮る」）単体では前例があり、組み合わせ
として初出になっているだけ。TODO-025 の目安（10 語超で疑わしい）には
遠く、内容も普通の日本語・一般的な IT 用語に見える。**造語の疑いのある
語は見当たらなかった。**

## 読んだファイル

- `/home/ytani/work/ytsched/TODO.md`（`git diff TODO.md` で追加分のみ）

前例の無い語（フレーズ単位）: 2 語（「版のズレ」「画面のキャプチャ」、
いずれも一般的な言い回しの組み合わせと見立てる）

## 決着時の確認

対象: `TODO.md`（差分のみ）、`docs/Developer.md`（差分のみ、「## 画面を撮る」
節とタスク一覧の 2 行）、`archives/todo/TODO-046. 画面のキャプチャを撮る
スクリプトを置く.md`（新規全文）、`archives/agents/TODO-046/
verifier-report.md`（新規全文）。基準は `HEAD`（今回のコミット前）。

`TODO.md` の差分は「版のズレ」「画面のキャプチャ」を含む節がそのまま
`archives/todo/` へ移っただけで、新しい語は増えていない（上の 2 語は
そのまま当てはまる）。追加の 2 ファイルと `docs/Developer.md` から
挙がった語は次のとおり（前例の件数が少ない順）。

### 掲載例
- 出てくる箇所: `verifier-report.md` 50 行目「`mise run tokens --
  TODO-046` も**掲載例**どおり実行でき、出力の書式（`消費:` 行）も
  既存の形式と合っていた」
- `git grep -cF` の件数: 前例なし（0 件）
- 見立て: 「文書に載っている実行例」という意味で、日本語として不自然
  ではない。「掲載」「例」はいずれも単体では前例があり、組み合わせが
  初出なだけに見える。判断は main に委ねる

### 版を固定する案
- 出てくる箇所: `archives/todo/TODO-046. …md` 45〜46 行目「`playwright
  install` で入れ直す案もあったが…**版を固定する案**は playwright-mcp
  側の更新に引きずられる」
- `git grep -cF` の件数: 前例なし（0 件）
- 見立て: 検討した選択肢を指す普通の言い回しに見える。造語というより
  文脈依存のフレーズ

### 見た目の確認としてはむしろ実態に近い
- 出てくる箇所: `archives/todo/TODO-046. …md` 49 行目「このアプリを
  実際に見るブラウザも同じ chromium なので、**見た目の確認**としては
  むしろ**実態に近い**。」
- `git grep -cF` の件数: いずれも前例なし（0 件）
- 見立て: 「見た目の確認」「実態に近い」とも一般的な日本語で、この
  リポジトリ独自の言い換えには見えない。判断できないなら main へ

以下は前例があり、造語の疑いが薄いもの（参考）:
- `chromium-1200`（3 件、TODO-031・TODO-041 に前例）
- `後片付け`（4 件、TODO-020・TODO-038 に前例）
- `なぞって` / なぞる（1 件、TODO-026 に前例。今回の「型を…なぞって
  確認」「手順をそのままなぞれること」もこの延長に見える）
- `終了ステータス`（30 件）、`アイコン`（75 件）、`展開`（40 件）、
  `見た目`（66 件）、`スクリプト`（57 件）、`スマホ`（19 件）、
  `画面を撮る`（3 件）はいずれも複数の前例あり

`epilog`・`--toggle`・`executable_path` は英語のコード上の識別子
（argparse のオプション名・引数名）であって日本語の言い換えではないため、
造語の対象から外した。

## 決着時の全体の見立て

前例なしと出たのは「掲載例」「版を固定する案」「見た目の確認」
「実態に近い」の 4 件（フレーズ単位）。いずれも普通の日本語の組み合わせ
で、意味が特殊化した専門用語や言い換えには見えず、TODO-025 の目安
（10 語超で疑わしい）にも遠い。**造語の疑いのある語は見当たらなかった。**

## 決着時に読んだファイル

- `/home/ytani/work/ytsched/TODO.md`（差分のみ）
- `/home/ytani/work/ytsched/docs/Developer.md`（差分のみ）
- `/home/ytani/work/ytsched/archives/todo/TODO-046. 画面のキャプチャを
  撮るスクリプトを置く.md`（新規全文）
- `/home/ytani/work/ytsched/archives/agents/TODO-046/verifier-report.md`
  （新規全文）

前例の無い語（フレーズ単位、決着時分）: 4 語
（「掲載例」「版を固定する案」「見た目の確認」「実態に近い」）

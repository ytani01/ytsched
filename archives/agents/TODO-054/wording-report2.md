# TODO-054 wording 報告2

対象は `git diff --cached` / `git diff HEAD` / 未追跡ファイルで拾える
`.md` 全部（`TODO.md`、`archives/todo/TODO-054. 左右のスワイプで週を送る.md`、
`archives/agents/TODO-054/` 配下の README・依頼書・verifier-report・
reviewer-report・request-wording.md）。

前例の件数が少ない順（0 件から）に並べる。

## 縦優勢

- **箇所**: `reviewer-report.md` 11 行目「境界条件（`SWIPE_MIN_X` 未満、
  縦優勢、800ms 超過…」、`verifier-report.md` 35 行目「縦優勢（dx=90,
  dy=280〜400）→変化なし」
- **`git grep` の件数**: 前例なし（「優勢」単体は TODO.md の当該 diff の
  1 件のみで、それも今回のコミット）
- **見立て**: 「縦の動きが優勢」を詰めた略記。文脈が無いと分かりにくいが、
  同じ報告内で先に「優勢」の使い方が出ているので読めなくはない。
  このリポジトリだけの言い換えというより、単なる省略表記に見える

## バブリング

- **箇所**: `reviewer-report.md` 16 行目「`stopPropagation()` を呼んで
  いる箇所が無いので、バブリングも問題ない。」
- **`git grep` の件数**: 前例なし
- **見立て**: DOM イベントの一般用語（event bubbling）。一般には通用する
  専門用語だが、このリポジトリでは初出

## フルナビゲーション

- **箇所**: `reviewer-report.md` 18〜19 行目「ページ遷移は `doGet()` が
  `location.href` を書き換えるフルナビゲーションなので…」
- **`git grep` の件数**: 前例なし
- **見立て**: SPA との対比で使う一般的な言い回し（full page navigation
  の訳）ではあるが、一般に定着した和訳かは判断できない。このリポジトリ
  では初出

## 防御コード

- **箇所**: `reviewer-report.md` 47 行目「この判定は、今の実装では
  実質到達しない防御コードで、コメントは…」
- **`git grep` の件数**: 前例なし
- **見立て**: 「defensive code」の和訳として一般に通用する語だが、
  このリポジトリでは初出

## 収束

- **箇所**: `verifier-report.md` 48 行目「…261.2 → 261.5px で収束）」
- **`git grep` の件数**: 前例なし
- **見立て**: 普通の日本語（値が一定値へ落ち着く、の意味）。造語ではなく、
  たまたまこのリポジトリで初めて使っただけに見える

## ダミー予定

- **箇所**: `verifier-report.md` 30 行目「2026-08-24〜28 週にダミー予定を
  置いて確認した。」
- **`git grep` の件数**: 前例なし
- **見立て**: 「テスト用のデータ」を指す一般的な言い方（dummy data の
  「予定」版）。このリポジトリのドメイン語「予定」と組み合わせただけで、
  特に問題は無さそう

## ピンチズーム

- **箇所**: `archives/todo/TODO-054. 左右のスワイプで週を送る.md` の
  節見出し「### ピンチズームを止める」ほか、`verifier-report.md` に
  複数回
- **`git grep` の件数**: 前例なし（「ピンチ」単体は 5 件あるが
  「ピンチズーム」は無し）
- **見立て**: pinch zoom は一般に通用する IT 用語。このリポジトリでは
  初出なだけで、造語ではなさそう

## 払う（スワイプの意味で）

- **箇所**: `TODO.md`（今回の diff 内、既存節の引用部分）、
  `archives/todo/TODO-054….md` 32・113 行目「左へ払うと次の週、右へ
  払うと前の週」、`request-verifier.md` 47 行目、`request-reviewer.md`
  27 行目、`verifier-report.md` 34・59 行目
- **`git grep` の件数**: 前例なし（動詞「払う」でスワイプを表す用例は
  これまで無い）
- **見立て**: 「指で払う」は日常語としても通用する自然な言い方で、
  この項目全体で一貫して使われている。造語というより、この項目で
  初めて出た普通の言い回しに見える

## 二重の確認 / 二重チェック

- **箇所**: `archives/todo/TODO-054….md` 137 行目「`touchMoveHdr` の
  判定は念のための二重の確認になる」、`reviewer-report.md` 51 行目
  「『念のための二重チェック』だと明記するかの整理を勧める」
- **`git grep` の件数**: どちらも前例なし
- **見立て**: 一般的な言い回し。同じ内容を「二重の確認」と
  「二重チェック」で表記揺れさせているが、造語というより表記の
  揺れに近い。判断できない部分もあるので、そのまま挙げる

## 誤検出に倒し（「〜に倒す」の言い回し）

- **箇所**: `reviewer-report.md` 55 行目「`SWIPE_MAX_MSEC = 800` は、
  ゆっくり払うスワイプを取りこぼす方向に効く（誤検出よりは
  取りこぼしに倒している）」、`archives/todo/TODO-054….md` 89 行目
  「誤検出よりは取りこぼしに倒してある」
- **`git grep` の件数**: 「誤検出に倒し」や「取りこぼしに倒し」の
  組み合わせ自体は前例なし（「誤検出」「取りこぼし」単体には前例あり。
  TODO-035 系で「取りこぼす」を含む文書が既にある）
- **見立て**: 「どちらかに倒す」という言い回し自体は一般的な日本語表現。
  単語の組み合わせが初出なだけで、造語には見えない

---

## 判断できないもの

無し（上の語はすべて何らかの見立てを添えた）。

## 読んだファイル

- `TODO.md`（差分）
- `archives/todo/TODO-054. 左右のスワイプで週を送る.md`
- `archives/agents/TODO-054/README.md`
- `archives/agents/TODO-054/request-verifier.md`
- `archives/agents/TODO-054/request-reviewer.md`
- `archives/agents/TODO-054/request-wording.md`
- `archives/agents/TODO-054/verifier-report.md`
- `archives/agents/TODO-054/reviewer-report.md`

## 前例の無い語の数

9 語（縦優勢、バブリング、フルナビゲーション、防御コード、収束、
ダミー予定、ピンチズーム、払う、二重の確認/二重チェック）。

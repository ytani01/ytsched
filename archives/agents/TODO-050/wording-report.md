# TODO-050 wording 報告（2 回目：決着時のコミット）

対象は依頼にある 8 つの `.md`（`TODO.md`・`src/README.md` の diff、
`archives/todo/TODO-050. 週を URL に持たせて GET にする.md`、
`archives/agents/TODO-050/` の README・依頼書・実装/確認/レビュー報告）。
基準は `HEAD`（このコミットが入る前）。件数は
`git grep -cF <語> HEAD -- '*.md'` で数えた。

なお `archives/agents/TODO-050/wording-report.md` 自体は、TODO-050 を
立てたときの 1 回目の報告がすでに `HEAD` に入っている。今回はそれを
上書きする。1 回目の報告で挙げた語（「フォーカス」など）は今回の対象と
重ならないので、この報告では扱わない。

## 前例の無い語

前例の件数が少ない順（すべて 0 件なので、出てくる場所の順に近い）。

### 回り道

- 出てくる場所: `archives/todo/TODO-050. …GET にする.md:64,100`
  （見出し「## 回り道（履歴の積み方を 2 回作り直した）」）
- 前例: なし
- 見立て: 「作り直した経緯」を指す見出しとして意味は通るが、この
  リポジトリの他の archives では同種の経緯を「気をつけること」
  「決めたこと」「テスト」のような機能名の見出しで書いており、
  「回り道」のような比喩の見出しは無い。依頼で名指しされていた語で、
  一般に通じなくはないが、このリポジトリの書き方からは外れて見える

### 完結する

- 出てくる場所: `archives/todo/TODO-050…md:58`「画面内で完結する移動」、
  `implementer-report.md:95,160`、`verifier-report.md:34,59,112,172`
  （「画面内で完結する移動」を指す言い回しとして繰り返し使われている）
- 前例: なし
- 見立て: 一般語ではあるが、この文脈だけの決まり文句として何度も
  出てくる。「画面内で終わる／収まる移動」のような普通の言い方でも
  足りたかもしれない。判断は main

### 画面内

- 出てくる場所: `archives/todo/TODO-050…md:51,52,58`、
  `implementer-report.md:14,95,160`、`verifier-report.md:33,41,59,98,112,
  143,172`（多数）
- 前例: なし
- 見立て: 「画面外」は TODO-038・042・043 に前例があるが、対になる
  「画面内」は今回が初出。対で使う語としては自然で、造語というより
  単に今まで使う場面が無かっただけに見える

### replaceState

- 出てくる場所: `archives/todo/TODO-050…md:104`、
  `implementer-report.md:65,95,159`、`verifier-report.md:35,41-47,68,69,
  96,97,190`、`reviewer-report.md:74`
- 前例: なし（`pushState` には TODO.md・TODO-049 wording 報告に前例あり）
- 見立て: `history.replaceState` という JS の API 名そのもの。
  一般に通用する固有名で、和訳や言い換えの余地は無い。造語ではない

### popstate

- 出てくる場所: `archives/todo/TODO-050…md:61,108,162`、
  `implementer-report.md:16,65,97,99`、`verifier-report.md:67,97,105`
- 前例: なし
- 見立て: `popstate` イベントの名前そのもの（API 名）。造語ではない

### 本命

- 出てくる場所: `verifier-report.md:30`（見出し「## 見つけたこと（本命）」）、
  `verifier-report.md:111`「戻る/進むの本命の症状は直っている」
- 前例: なし
- 見立て: 「いちばん重要な／主要な」の意味の口語的な言い換え。
  一般には通じるが、このリポジトリの報告では「見つけたこと」だけで
  済ませている例が多く、「本命」を付けた例は無い

### 光らせる

- 出てくる場所: `archives/todo/TODO-050…md:74`「更新した行を光らせる値」、
  `implementer-report.md:63`「光らせる（`class_blink`）ための一時的な値」
- 前例: なし
- 見立て: `class_blink`（コード上の識別子）が表す挙動を指す口語的な
  言い方。一般的な言い回しだが、この機能をこう呼んだのは初出

### 用途を限って

- 出てくる場所: `implementer-report.md:105`「`doPost()` を、用途を限って
  復活させた」、`archives/todo/TODO-050…md:119`「`doPost()` を用途を限って
  戻し」
- 前例: なし
- 見立て: 普通の日本語の組み合わせで、造語というほどではないと思うが、
  前例が無いのは事実なので挙げる

### 二重に積まれた

- 出てくる場所: `archives/todo/TODO-050…md:109`「今度は履歴が二重に
  積まれた」
- 前例: なし（「重複エントリ」も 0 件、同じ現象を指す言い方が 2 通り
  混在している）
- 見立て: 「履歴が二重に増えた」の意味で自然だが、初出

### 重複エントリ

- 出てくる場所: `verifier-report.md:164`「同じ URL の重複エントリに乗る」
- 前例: なし
- 見立て: 「二重に積まれた」（上）と同じ現象を、報告のこの箇所だけ
  違う言い方で書いている。一般的な IT 用語（重複エントリ）ではあるが、
  このリポジトリでは初出で、しかも同じ文書内で言い方が割れている

### 飛び越える

- 出てくる場所: `archives/todo/TODO-050…md:106`「途中の日付を飛び越える」、
  `implementer-report.md:67,93`
- 前例: なし
- 見立て: 普通の日本語。造語というほどではないと見立てるが、前例は
  無いので挙げる

### 履歴に積む

- 出てくる場所: `implementer-report.md:15`「履歴に積む」、
  `implementer-report.md:151`「（履歴に積む）」
- 前例: なし（「履歴が増える」「履歴を増やさない」という言い方は
  `archives/todo/TODO-050…md` の表などで使われている）
- 見立て: 「履歴を増やす」と同じ意味を「積む」で言っている。
  同じ文書の中でも「増やす」と「積む」が両方出てくるので、
  表記が割れている可能性がある

### 筋に合う

- 出てくる場所: `reviewer-report.md:41`「フォームに任せる形にするのが
  筋に合うと思う」
- 前例: なし
- 見立て: 「筋が通る」「理にかなう」に近い口語的な言い方。一般に
  通じなくはないが、このリポジトリでは初出

## 判断できないもの

なし（見立てが付かず「判断できない」と書くしかない語は無かった）。

## 読んだファイル

- `TODO.md`（diff）
- `src/README.md`（diff）
- `archives/todo/TODO-050. 週を URL に持たせて GET にする.md`
- `archives/agents/TODO-050/README.md`
- `archives/agents/TODO-050/request-implementer.md`
- `archives/agents/TODO-050/implementer-report.md`
- `archives/agents/TODO-050/verifier-report.md`
- `archives/agents/TODO-050/reviewer-report.md`

## 前例の無い語数

13 語（回り道・完結する・画面内・replaceState・popstate・本命・光らせる・
用途を限って・二重に積まれた・重複エントリ・飛び越える・履歴に積む・
筋に合う）。

# TODO-050 wording 報告

対象: `git diff TODO.md`（1 ファイル）。TODO-050 の新設節と、TODO-049 の
書き直し（「一緒に考えた案」→「検索したとき」「この項目に一緒に入れる案」
「別の項目にする案」「やらないもの」への分割）。

前回（TODO-049）で決めた「追加読み込み」「競合する」「縦に並べる表示」は
対象から外した。

基準は `HEAD`（このコミットが入る前）。`git grep -cF <語> HEAD -- '*.md'`
で数えた。

## 前例の無い語（0 件）

件数が少ない順（すべて 0 件なので、出てくる場所の並び順）。

### フォーカス

- 出てくる箇所: 「**入力欄にフォーカスがあるときは拾わないこと。**検索欄で
  `/` を打てなくなったり…」（TODO-050「キーボードの割り当て（案）」の
  直後）、「キーボードは、一覧・編集画面・検索欄にフォーカスがある状態の
  3 つで試す」（「確かめ方」）
- 前例: 0 件
- 見立て: 一般に通用する UI 用語（`focus` の訳語として定着している）。
  ただし、このリポジトリの `.md` では初出。コード側（`my.js` など）で
  `focus` / `activeElement` に類する語がどう呼ばれているかは、
  `.md` の対象外なので確認していない。判断は main へ

### スライダー

- 出てくる箇所: 「別の項目にする案」の「ゲージを作り直す」の中、
  「**横向きのスライダーにして、ヘッダかフッターに置く。**」
- 前例: 0 件（「ゲージ」は 62 件、「GAGE」は 3 件と、既存の言い方が
  ある）
- 見立て: 一般語ではあるが、この文書はここまで一貫して「ゲージ」
  （`GAGE` / `.my-gage-r` などの識別子に対応）と呼んできた対象を、
  この 1 箇所だけ「スライダー」と呼んでいる。**同じものの言い換えに
  見える。** 「横向きのゲージ」で足りるかどうかは main の判断

### POST-Redirect-GET

- 出てくる箇所: 「決めること」の「**追加・修正・削除のあとの遷移。**
  今は `cmd` の POST がそのまま画面を描いているので、リロードすると
  再送信になる。更新したあとは GET へリダイレクトする形
  （POST-Redirect-GET）にするか」
- 前例: 0 件
- 見立て: Web 開発では広く通用する定着した業界用語（PRG パターン）。
  このリポジトリでは初出なだけで、造語ではないと見る

### 手数

- 出てくる箇所: 「なぜやるか」の「**PC で使うときに手数が多い。**
  週を送るのに、下部のメニューバーの ←/→ を押しに行くしかない」
- 前例: 0 件
- 見立て: 一般的な日本語（「操作の手間・回数」の意味で普通に使う）。
  問題ないと見る

## 前例はあるが、意味がずれていそうな語

指摘の対象外（前例あり）だが、文脈が違うので念のため書いておく。

### リダイレクト

- 前例: 3 件。ただしいずれも Bash のリダイレクト（`>` によるファイルへの
  出力）の意味で使われており（`.claude/agents/verifier.md`、
  `archives/agents/TODO-006/verifier-report.md`、
  `archives/agents/TODO-026/verifier-report.md`）、**HTTP のリダイレクト
  （3xx 応答でブラウザを別の URL へ移す）の意味では初出**
- 見立て: 一般に通用する Web 用語で、造語ではない。ただし機械的な
  文字列一致では「前例あり」に見えてしまう例として記録しておく

### 割り当て / クエリ / パス / 遷移 / 土台

- いずれも `src/README.md` や過去の `archives/todo/` で、URL・ルーティング・
  クエリ文字列と同じ意味ですでに使われている（例: `src/README.md` の
  「`WebServer` がどの URL にどちらを割り当てているか」、
  `archives/agents/TODO-008/implementer-report.md` の「日付はクエリで
  渡す作り」）。**このリポジトリで定着した言い方と見てよく、問題ない**

## 前例の無い語のうち、見送ったもの（普通の日本語）

正規表現的な切れ端ではなく実際に読んで拾ったが、造語ではなく一般的な
言い回しと判断し、上の一覧から外したもの。

- 「間引く」「つまんで動かせば」「主要なものだけ」「空いたこの操作を
  『その日に予定を追加』に充てる」「統一される」「左右される」
  「ひと続きになる」「組み合わせられ」「縦に立てている」
  — いずれも普通の日本語で、前例が無いのは単に文字列として
  初めて組み合わさっただけと見た

## 読んだファイル

- `TODO.md`（`git diff TODO.md` の追加行のみを対象に読んだ。前例確認は
  ファイル全体に対して `git grep` した）
- 参考: `src/README.md`、`archives/agents/TODO-031/`、
  `archives/agents/TODO-049/wording-report.md`、
  `archives/todo/TODO-039. スマホ用の設定を追加.md` ほか（`git grep` の
  結果から）

## まとめ

- 前例の無い語: **4 語**（フォーカス、スライダー、POST-Redirect-GET、
  手数）
- うち「スライダー」は、既存の「ゲージ」との言い換えの疑いがあり、
  main の判断が要る
- 「リダイレクト」は前例ありと出るが、HTTP の意味では初出という
  ずれがある点も判断材料として残す

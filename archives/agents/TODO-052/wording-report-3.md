# wording 報告（3 回目）: archives/agents/TODO-052/README.md

対象は `archives/agents/TODO-052/README.md`（新規、このコミットで足すファイル
のみ）。前回の指摘（「文脈」「やり取り」「区間を切る」の言い換え）自体が
新しい言い換えになっていないかを中心に見た。

## 前例の無い語

前例の少ない順。

### 1. 使い捨てのスクリプト（8 行目）

> 測定は main が使い捨てのスクリプトから `tools/token-usage.py` の
> `collect()` / `sum_by()` を呼んで行った。

- `git grep -cF` 前例なし
- 見立て: 「使い捨て」単体はこのリポジトリで多数の前例がある一般語
  （TODO-002・013・017・026 など）が、「使い捨てのスクリプト」という
  組み合わせは初出。一般に通用する言い方（"throwaway script" の直訳に
  近いが、日本語としても普通）に見えるので、造語というより初出の
  組み合わせだと思う。判断は main に任せる

### 2. 定型業務（24 行目）

> 「定型業務」は前例が無いと挙がったが、**利用者がこの項目を指すのに
> 使った語**なので、そのまま残した。

- `git grep -cF` 前例なし
- これは報告文中で「前回すでに前例なしと指摘され、そのまま残すと決めた語」
  として言及しているだけで、README 側の新しい造語ではない
  （TODO-052 本体の文書側にある語への言及）。念のため挙げるが、
  今回新たに生まれた語ではない

## 前例があり、問題なさそうな語（参考）

「文脈 → コンテキスト長」「やり取り → リクエスト」「区間を切る → 範囲を
切る」と説明している箇所自体で使われている言い換えの言葉（「言い換え」
「定着した」「決めるだけの項目」「揃えた」「立てたとき」「使い捨て」）は、
いずれも `git grep -cF` で複数件の前例があり、この報告の説明文が新しい
言い換えを作っているようには見えなかった。「コンテキスト」「リクエスト」
「範囲を切る」自体も TODO-044 由来の定着語として前例がある。

## 読んだファイル

- `archives/agents/TODO-052/README.md`（このコミットで新規に足すファイル。指定通りこれのみ）

## まとめ

前例の無い語: 2 語（「使い捨てのスクリプト」「定型業務」）。ただし
「定型業務」は README 内で新たに作った語ではなく、他文書の語への言及。
実質的に新規の候補は「使い捨てのスクリプト」1 語のみ。

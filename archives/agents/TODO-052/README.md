# TODO-052 の担当

この項目は決めるだけなので、確かめるものが無い。実装も確認も立てず、
**main のみ + `wording`** で進めた（`~/.claude/CLAUDE.md` の
「決めるだけの項目は担当を `main のみ` でよい」に当たる）。
`.md` が入るコミットなので `wording` だけを立てた。

測定は main が使い捨てのスクリプトから `tools/token-usage.py` の
`collect()` / `sum_by()` を呼んで行った。

## 報告ファイル

- `wording-report.md` — **立てたとき**（`4ec4ef3`）のもの。
  そのときの `TODO.md` の差分だけを見ている
- `wording-report-2.md` — **決着させるとき**（この commit）のもの。
  新規の `archives/todo/TODO-052. ….md` と `TODO.md` の両方を見ている

決着時の指摘のうち、**「文脈」「1 回あたりの文脈」「やり取り」が
TODO-044 で定着した「コンテキスト（長）」「リクエスト」の言い換えに
なっている**という 3 件を受けて、`archives/todo/` のファイルを直した。
「区間を切る」も、`CLAUDE.md` と `tools/token-usage.py` で定着している
「範囲を切る」に揃えた。

「定型業務」は前例が無いと挙がったが、**利用者がこの項目を指すのに
使った語**なので、そのまま残した。

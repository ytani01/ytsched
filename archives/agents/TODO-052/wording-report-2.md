# TODO-052 wording 報告（決着時）

対象: `archives/todo/TODO-052. 項目を立てる・アーカイブする作業のトークンを減らす.md`（新規）、
`TODO.md`（差分）。前例の判定基準は `HEAD`（このコミットが入る前）。

前例の件数が少ない順に並べる。

## 前例なし（0 件）

- **1 回あたりの文脈**（`archives/todo/TODO-052...md:32` の表の見出し、
  および「同じ作業でも、立てるとき（11.6 万）の倍かかる」の説明）
  - `git grep -cF "回あたり" HEAD -- '*.md'` は 1 件のみで、TODO-044 の
    「244 リクエストで 1 回あたり平均〜」。**単位が「リクエスト」で、
    量が「コンテキスト（長）」**という組み合わせが既に定着している。
    TODO-052 はこれを「やり取り」「文脈」に言い換えている
  - 見立て: 依頼で挙がっていた懸念そのもの。同じ量を指すのに
    TODO-044 と違う語を当てていて、リポジトリ内で二重の呼び名になる

- **文脈が太る／文脈がいちばん太っている**（`...md:20`「効いた」、
  `:40`「文脈がいちばん太っているときにやる」、`:74`「文脈は、
  会話が進むほど太る」）
  - `git grep -cF` は「文脈が太る」「文脈がいちばん太っている」
    「太る」「太って」いずれも 0 件
  - 見立て: 「太る」は比喩として分かるが、この意味（トークン量が
    増える）でこのリポジトリに前例が無い。TODO-044 の
    wording-report では、近い比喩「コンテキストを育てる／育てない」が
    指摘され、そのまま定着しなかった経緯がある。今回の「太る」も
    同種の比喩の作り直しに見える

- **文脈**（トークン量・コンテキストサイズの意味での用法。
  `...md:32` の表見出し、`:40`「文脈がいちばん太っている」、
  `:57`「文脈はやり取りのたびに丸ごと払い直す」ほか多数）
  - 「文脈」という語自体は `git grep -cF "文脈" HEAD -- '*.md'` で
    31 件あるが、中身を見ると「会話の流れ・議論の前後関係」の意味
    （例: TODO-002「同じ文脈を 2 回読ませる」、TODO-051「文脈が違う」）
    ばかりで、**「コンテキストウィンドウに載っているトークン量」の
    意味では 1 件も無い**。その意味では TODO-044 で「コンテキスト」
    「コンテキスト長」が使われ、`.claude/agents/*.md` にも
    「コンテキスト」で定着している
    （`git grep -cF "コンテキスト" HEAD -- '*.md'` は 9 件）
  - 見立て: 依頼の懸念どおり。同じ量を指すのに「コンテキスト」と
    「文脈」の 2 通りの呼び名が並ぶことになる。一般語としては
    自然だが、このリポジトリの用語としては言い換えに見える

- **定型業務**（`...md:25` の見出し「定型業務の区間を切り出して測った」、
  `:28`「定型業務だけを見たいので」、`:48`「26 回のうち 12〜16 回が
  機械的な部分」など複数箇所）
  - `git grep -cF "定型業務" HEAD -- '*.md'` は 0 件。「定型」という
    語自体は「定型句」「定型見出し」の形で前例があるが、
    「定型業務」という名詞化した組み合わせは無い
  - 見立て: 一般的な言い回し（「ルーチンワーク」の和訳としてよく
    見る語）だが、このリポジトリでは初出

- **区間を切って／区間を切り出して**（`...md:25`「区間を切り出して
  測った」、`:28`「transcript から時刻で区間を切って数えた」）
  - `git grep -cF "区間を切" HEAD -- '*.md'` は 0 件。「区間」単独では
    TODO-036 の reviewer-report に 1 件あるが、コードの実行区間
    （`debug=False` で初期化される区間）の意味で、時間の範囲を
    「切る」という言い方ではない
  - 見立て: 一般に通用しそうだが、時間範囲を測る文脈でこの言い方を
    使ったのはこの文書が初めて

- **機械的な部分**（`...md:48`「26 回のうち 12〜16 回が機械的な部分」）
  - `git grep -cF "機械的な部分" HEAD -- '*.md'` は 0 件
  - 見立て: 普通の日本語。造語というより一般語の組み合わせに見える

- **書く材料／材料がファイルに（ある）**（`...md:115`「これが成り立つのは、
  書く材料が会話ではなくファイルに残っているから」、`:119`「報告
  ファイルから書くほうが」、`:180`「材料（測定結果）がこの会話にしか
  無く」）
  - 「材料」単独は `git grep -cF "材料" HEAD -- '*.md'` で 6 件前例あり
    （「決める材料」など）。ただし「書く材料」「材料がファイルにある」
    という組み合わせでの前例は 0 件
  - 見立て: 「材料」自体は定着した語なので、造語というより組み合わせの
    初出。一般に通用しそう

- **中央値**（`...md:61`「文脈の中央値を出した」、表の見出し
  「文脈の中央値」）
  - `git grep -cF "中央値" HEAD -- '*.md'` は 0 件
  - 見立て: 統計用語として一般的。このリポジトリでは初出なだけ

- **桁が違う**（`...md:92`「同じ `.md` を見る作業でも、main が末尾で
  やるのとは桁が違う」）
  - `git grep -cF "桁が違う" HEAD -- '*.md'` は 0 件
  - 見立て: 普通の言い回し

- **目次の直し忘れ**（`...md:17`「番号の取り違え、目次の直し忘れ」、
  `TODO.md` から移された節にも同文がある）
  - `git grep -cF "目次の直し忘れ" HEAD -- '*.md'` は 0 件
  - 見立て: 普通の日本語。造語には見えない

- **1 体あたり／本/体**（`...md:80` の表の見出し「本/体」「1 体
  あたり」）
  - `git grep -cF "1 体あたり" HEAD -- '*.md'` および `"本/体"` は
    いずれも 0 件。サブエージェントの起動回数を「本」「体」で数える
    言い方自体は TODO-044 系の報告にも見られるが、「1 体あたり」と
    いう単位の言い方はここが初出
  - 見立て: 判断できない。文脈からは「サブエージェント 1 回の起動
    あたり」の意味と読めるが、一般に通用する言い方かは分からない

- **気をつけたこと**（`...md:176` の見出し）
  - `git grep -n "^## " HEAD -- 'archives/todo/*.md'` で集計した見出しの
    中に「気をつけたこと」は無い（近い見出しは「気になったが直さ
    なかったもの」「気づいたが直さなかったもの」）
  - 見立て: 普通の日本語。見出しとしての言い回しが毎回違うのは
    他の項目でも同様なので、造語というより単なる新しい見出し

- **曲線**（`...md:74`「アーカイブは、この曲線のいちばん右でやって
  いる作業」、表の直前「何回目ごとに文脈の中央値を出した」の後）
  - `git grep -cF "曲線" HEAD -- '*.md'` は 1 件あるが、TODO-043 の
    「フォントの輪郭をベジェ曲線で表す」という別分野の話で、
    「グラフの傾向線」の意味での用法は無い
  - 見立て: 一般に通用する比喩だが、この意味ではこのリポジトリで
    初出

## 前例が少ないもの

- **やり取り**（`...md:32` の表の見出し「main のやり取り」、本文
  各所）
  - `git grep -cF "やり取り" HEAD -- '*.md'` は 1 件のみ
    （TODO-018/README「1 回目のやり取りが残っていて」）。ほぼ同じ
    意味だが、TODO-044 以降は同じ量を数えるときに「リクエスト」を
    使っており（`git grep -cF "リクエスト" HEAD -- '*.md'` は 59 件、
    うち「1 リクエストで」「リクエスト数」のように回数を数える用法が
    複数）、`tools/token-usage.py` の出力も「リクエスト」を使っている
  - 見立て: 依頼で挙がっていた懸念どおり。一般語としては自然だが、
    このリポジトリでは「リクエスト」が定着した呼び名で、「やり取り」
    は言い換えに見える

## 前例があり、問題無さそうなもの（参考）

- **切り出して**（3 件）、**効き目**（6 件）、**概算**（32 件）、
  **割合**（45 件）、**切り分け**（28 件）、**決めるだけの項目**（4 件、
  `CLAUDE.md` にも同じ言い回しがある）、**使い捨て**（22 件、
  「使い捨てのスクリプト」という組み合わせ自体は 0 件だが「使い捨て」
  「スクリプト」いずれも定着語）、**跳ね上がる**（1 件、TODO-038 の
  画像の指標の話だが意味は同じ）は、いずれも前例があるか、部品の語が
  定着しているため見立てとしては問題無さそうと考えた。

## 読んだファイル

- `archives/todo/TODO-052. 項目を立てる・アーカイブする作業のトークンを減らす.md`
- `TODO.md`（差分箇所）

## 前例なしの語数

15 語（「前例なし」節）＋ 1 語（「やり取り」、前例 1 件のみで実質
言い換え）＝ 実質 16 語。目安の「10 語を超えたら疑ってよい」を超えている。

とくに「文脈」「1 回あたりの文脈」「やり取り」の 3 つは、TODO-044 で
定着した「コンテキスト（長）」「リクエスト」という組み合わせを、
別の語に言い換えているように見える点で他の語と性質が違う。決めるのは
main。

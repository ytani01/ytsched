# TODO-053 wording 報告

## 対象

`git diff TODO.md`（`HEAD` に対する差分）2 か所のみ。
- 先頭の「残っている項目」「番号は」の行
- 新設した「## TODO-053. キャプチャで、404 のページを黙って撮ってしまうのを直す」の節

依頼書に「対象は `TODO.md` の差分だけ」と明記されていたため、
`git diff --cached --name-only -z` 等での他の `.md`（報告ファイル自体を除く）の
洗い出しは行っていない。

## 前例の有無（件数の少ない順）

### 見出しになりそうな語（前例なし）

| 語 | 出てくる箇所 | `git grep -cF` の件数 | 見立て |
|---|---|---|---|
| 撮らずに | 「200 以外なら、撮らずに終える」「**200 以外なら、撮らずに止める**」 | 0（前例なし） | 「撮る」＋「ずに」の普通の組み合わせ。造語ではなく、たまたま初出と見る |
| 見間違え | 「見た目が同じなので『変わっていない』と読めてしまう」の段落の直後、確かめ方の文脈「前後の突き合わせで見間違えずに済む」 | 0（前例なし） | 「見間違える」の普通の活用形。造語ではないと見る |
| 寄せられそう | 「そこに寄せられそう」（既存の `except Exception` 経路にまとめられそう、の意） | 0（完全一致では前例なし。「寄せられる」という近い語形は archives/agents/TODO-024/wording-report.md に 1 件あるが、語形が違うため別扱い） | 「寄せる」の可能性の言い方で一般的。ただし「実装をどこかの経路へ寄せる」という比喩的な使い方がこのリポジトリ独自の言い回しかどうかは判断できない |

### 前例が少数だけある語（参考）

| 語 | 件数 | 備考 |
|---|---|---|
| 旧既定 | 2（archives/agents/TODO-051/README.md、同 verifier-report.md） | TODO-051 由来の言い回しがそのまま踏襲されている。今回が初出ではない |
| リダイレクト | 9 | 一般的な IT 用語で前例あり |
| 終了コード | 13 | 前例あり |

### 前例が多く、造語の心配が薄い語（参考）

キャプチャ（39）、黙って（54）、ステータス（51）、突き合わせ（42）、
追随（36）、戻り値（29）、既定（220）。いずれも既に十分使われている
一般語・既存の言い回し。

## 前例の無い語数

3 語（撮らずに／見間違え／寄せられそう）。目安の「1〜3 語」の範囲内で、
いずれも普通の日本語の活用形・言い回しに見える。ただし「寄せられそう」の
比喩的な用法（実装の置き場所を指す意味）が独自かどうかは判断できないため、
そのまま挙げる。

## 読んだファイル

- `TODO.md`（差分部分のみ、`git diff TODO.md` で確認）

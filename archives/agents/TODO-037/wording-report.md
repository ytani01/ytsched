# TODO-037 wording 報告

## 立てるコミット（TODO-037・038 の見込み・やることの追記）

対象: `TODO.md`（未ステージ、`git diff -- TODO.md` の追加行のみを見た）。
基準は `HEAD`（コミット `f22bafd`）。

### 前例の無い語（前例 0 件）

#### 使用箇所
- 箇所: TODO-037「`jquery` / `popper` / `bootstrap.js` の 3 行を消す（**使用箇所**ゼロ）」
- `git grep -cF 使用箇所 HEAD -- '*.md'`: 前例なし
- 見立て: ごく普通の複合語で、専門用語でも言い換えでもない。問題ないと思う。

#### webfont
- 箇所: TODO-037「Font Awesome 5.6.1 の `all.css` と **webfont** を `static/vendor/` へ置く」
  「`uv tool install --reinstall .` で **webfont** まで配信されるか確かめる」
- 前例なし
- 見立て: 一般的な技術用語（Web フォント）で、英語表記のまま使うのは
  CLAUDE.md の方針（IT 用語はカタカナか英語のまま）に沿う。造語ではない。

#### 遮断
- 箇所: TODO-037「**CDN を遮断した状態**で、同梱前と見た目が変わらないことを確かめる」
- 前例なし
- 見立て: 一般語。問題ないと思う。

#### integrity / sha384
- 箇所: TODO-037「（`base.html` にある `integrity` の `sha384` で照合する）」
- 前例なし
- 見立て: HTML 属性名とハッシュアルゴリズム名。固有名詞に近く、
  和訳しようがない。造語ではない。

#### ライセンス表記
- 箇所: TODO-037「**ライセンス表記**を置く（Bootstrap は MIT、Font Awesome Free は…）」
- 前例なし
- 見立て: 一般語。問題ないと思う。

#### スクリーンショット
- 箇所: TODO-038「確かめ方は**スクリーンショット**の比較になる」
- 前例なし（この語は他の `.md` にまだ出ていない）
- 見立て: 一般的な IT 用語。`~/.claude/CLAUDE.md` の
  「画像の見せ方」でも使われている語なので、リポジトリ独自の言い換えではない。

### 前例は少ないが挙げておくもの

#### 再代入
- 箇所: TODO-038「`edit.html:98` の `const detail_h` **再代入**を直す（横向きで TypeError）」
- 前例 1 件
- 見立て: 一般的なプログラミング用語（reassignment の訳語）。問題ないと思う。

#### 確かめ方
- 箇所: TODO-038「reviewer を入れるのは…**確かめ方**はスクリーンショットの比較になる」
- 前例 2 件
- 見立て: 普通の日本語。問題ないと思う。

### 前例があり、見立ても問題なさそうな語（参考）

崩れ（10 件、既存の「整形崩れ」「表示が崩れる」と同じ使い方）、
引用符（10 件）、丸ごと（13 件）、同梱（21 件、TODO-037 の見出しに
既にある語）、照合（29 件）、重複（64 件）、挙動（230 件）。
いずれも既存の使われ方と整合しており、新しい言い換えには見えない。

### 判断できないもの

無し。

### 読んだファイル

- `TODO.md`（`git diff -- TODO.md` で見た追加行のみ）

### 前例の無い語の数

7 語（使用箇所・webfont・遮断・integrity・sha384・ライセンス表記・
スクリーンショット）。いずれも一般語・技術用語で、造語らしいものは
無いと見立てる（決めるのは main）。

---

## 決着のコミット（`feat(webapp): 外部 CDN をやめて同梱する（TODO-037）`）

対象は次の `.md` 全部。基準は `HEAD`（コミット `3eb53bf`）。

- `TODO.md`（TODO-037 の節を外し、目次へ移した）
- `README.md`（「同梱しているライブラリ」の節が増えた）
- `archives/todo/TODO-037. CDNに依存しないよう同梱する.md`（新規）
- `archives/agents/TODO-037/README.md`（新規）
- `archives/agents/TODO-037/implementer-request.md`（新規）
- `archives/agents/TODO-037/implementer-report.md`（新規）
- `archives/agents/TODO-037/verifier-request.md`（新規）
- `archives/agents/TODO-037/verifier-report.md`（新規）
- `archives/agents/TODO-037/wording-report.md`（このファイル自身。上の
  「立てるコミット」の節）

`TODO.md` の差分は、TODO-037 の節を削って目次へ 1 行足しただけで、
文言はすべて上の「立てるコミット」の節で確認済みのもの（`HEAD` に
既に入っている）。新しく増えた語は無かった。

### 前例の無い語（前例 0 件）

#### 画素単位 / 画素
- 箇所: `archives/agents/TODO-037/README.md:21`「chromium を CDP で動かして
  **画素単位**で」、`archives/todo/TODO-037. …md:72`「同じ画面を撮って
  **画素単位**で比べた」、同 74「違う**画素**」、同 81「0 **画素**だった」
- `git grep -cF 画素単位 HEAD -- '*.md'` / `画素`: いずれも前例なし
- 見立て: 一般的な IT 用語（pixel の訳語）。造語ではないと思う。

#### CDP
- 箇所: `archives/agents/TODO-037/README.md:21`「chromium を **CDP** で
  動かして」、37「確かめる仕組み（**CDP** でスクリーンショットを撮って」
- 前例なし
- 見立て: Chrome DevTools Protocol の略。固有の技術名で、和訳しようがない。
  造語ではないと思う。

#### 改竄
- 箇所: `archives/agents/TODO-037/verifier-report.md:53`「## 4. Bootstrap の
  **改竄**チェック」、`implementer-request.md:52`「Bootstrap は**改竄**
  されていないことを確かめること」
- 前例なし
- 見立て: 一般語（tampering の訳語）。改ざんの漢字表記だが、専門文書では
  普通に使う。問題ないと思う。

#### 検証サーバ
- 箇所: `archives/agents/TODO-037/verifier-report.md:91`「自分の**検証
  サーバ**はポート 10097」
- 前例なし
- 見立て: リポジトリ独自の言い換えというより、「確認用に自分で立てた
  サーバ」を指す普通の呼び方に見える。ただし verifier が今回だけ使った
  語で、他の TODO の verifier 報告でどう呼んでいたかは確かめていない。
  判断できない部分がある

#### デバッグ変数名
- 箇所: `archives/agents/TODO-037/verifier-report.md:85`「実際は
  `filter_error=False` / `search_error=False` の**デバッグ変数名**のみで」
- 前例なし
- 見立て: 普通の複合語（デバッグ用の変数名）。造語には見えない。

#### タグ設定
- 箇所: `archives/agents/TODO-037/verifier-report.md:78`「`git describe`
  系の**タグ設定**によるもので」
- 前例なし
- 見立て: 一般語。問題ないと思う。

#### コンポーネント
- 箇所: `archives/agents/TODO-037/implementer-report.md:48`「JS を必要と
  する Bootstrap **コンポーネント**は使っていないと判断した」
- 前例なし
- 見立て: 一般的な IT 用語（カタカナのまま使う CLAUDE.md の方針に沿う）。
  造語ではない。

#### 基準の画面
- 箇所: `archives/agents/TODO-037/README.md:40`「**基準の画面**を、実装を
  始める前に撮っておいたのが効いた」
- 前例なし
- 見立て: 「比較の基準にする画面」という意味で、文脈から読める普通の
  言い方に見える。ただし「基準」+「画面」の組み合わせはここが初出で、
  今後 TODO-038 でも使うと README に書いてあるので、定着させるつもりの
  呼び名だと思われる。一般に通用するかは判断できない

#### 動作環境
- 箇所: `archives/agents/TODO-037/implementer-report.md:79`「あちらは
  **動作環境**の話で内容が違うため分けた」
- 前例なし
- 見立て: 一般語。README には「使用環境」の見出しが既にある（8 件）ので、
  同じ意味を指す言い換えの可能性がある。ただし implementer 自身が
  「動作環境の話」と `## 使用環境` を区別して書いており、同じものを
  指しているとは限らない。判断できない

#### ライセンス文書
- 箇所: `README.md:207`「同梱しています。**ライセンス文書**はそれぞれの
  ディレクトリにあります」
- 前例なし
- 見立て: 一般語（LICENSE ファイルのこと）。問題ないと思う。

#### 到達を確認済み
- 箇所: `archives/agents/TODO-037/implementer-request.md:45`「取得元
  （**到達を確認済み**）」
- `到達を確認済み` は前例なし。ただし単語の「到達」自体は 8 件の前例が
  ある
- 見立て: 「到達」という一般語に「確認済み」を続けただけで、造語では
  ないと思う。

### ブレ（実質、前例なし）

- 箇所: `archives/agents/TODO-037/README.md:43`「0 画素なので**ブレ**では
  ない」、`archives/todo/TODO-037. …md:81`「一覧の差は**ブレ**ではない」
- `git grep -cF ブレ HEAD -- '*.md'` は 1 件返るが、中身は
  `README.md:103` の「タブレット」への部分一致で、同じ語（測定のばらつき
  という意味の「ブレ」）ではない。実質は前例なし
- 見立て: 一般的な IT 用語（テストの結果が揺れること）。造語ではないと
  思う。`git grep -F` は文字列一致なので、他の語を含む語（「タブレット」
  の中の「ブレ」）を誤って前例に数えることがある、という注意点として
  報告に残す

### 前例があり、見立ても問題なさそうな語（参考）

同梱（23 件）、丸ごと（14 件）、崩れ（12 件）、照合（30 件）、
引用符（11 件）、挙動（231 件）、仕組み（24 件）、分担（106 件）、
振り返り（2 件）、ハッシュ（2 件）、残プロセス（2 件）、使用環境（8 件）、
開発者向け（8 件）、ライブラリ（10 件）、単独で決めたこと（3 件）。
`webfont` / `integrity` / `sha384` / `使用箇所` / `遮断` /
`ライセンス表記` / `スクリーンショット` は、今回のコミットで
`TODO.md` に前例ができたため 1 件以上になっている（中身は「立てるコミット」
の節で見た箇所そのもの）。いずれも新しい言い換えには見えない。

### 判断できないもの

- **検証サーバ** — 他の TODO の verifier 報告での呼び方を確かめていない
- **基準の画面** — 一般に通用するかは判断できない
- **動作環境** — README の「使用環境」と同じものを指す言い換えかどうか
  判断できない

### 読んだファイル

- `TODO.md`
- `README.md`
- `archives/todo/TODO-037. CDNに依存しないよう同梱する.md`
- `archives/agents/TODO-037/README.md`
- `archives/agents/TODO-037/implementer-request.md`
- `archives/agents/TODO-037/implementer-report.md`
- `archives/agents/TODO-037/verifier-request.md`
- `archives/agents/TODO-037/verifier-report.md`
- `archives/agents/TODO-037/wording-report.md`（このファイル自身、
  「立てるコミット」の節）

### 前例の無い語の数

11 語（画素単位・画素・CDP・改竄・検証サーバ・デバッグ変数名・
タグ設定・コンポーネント・基準の画面・動作環境・ライセンス文書）に、
実質前例なしの「ブレ」と、単語自体は前例があるが複合表現としては
初出の「到達を確認済み」を加えると 13 語。10 語を超えており、
`CLAUDE.md` の目安では疑ってよい範囲に入る。ただし内訳は
CDP・コンポーネント・画素（単位）のような一般的な IT 用語が大半で、
TODO-021 の「足場」のように既存の概念を独自の呼び名に置き換えた例は
見当たらなかった。**検証サーバ・基準の画面・動作環境の 3 語は
見立てが付かないので、main の判断を仰ぎたい。**

---

## main の判断（3 語）

- **検証サーバ → 「自分が起動したサーバ」に直した。** 過去の verifier
  報告を見ると、どれも「サーバ」「一時ディレクトリで起動したサーバ」と
  呼んでいて、この語だけ今回の造語だった
- **基準の画面 → 「同梱前の画面」に直した。** TODO-038 でも同じ比べ方を
  するので、定着させる前に普通の言い方へ戻した
- **動作環境 → 「使用環境」に揃えた。** README の見出しと同じものを
  指していたので、言い換えを増やさない

残りの 10 語（画素単位・画素・CDP・改竄・デバッグ変数名・タグ設定・
コンポーネント・ライセンス文書・ブレ・到達を確認済み）は、そのままに
した。いずれも一般に通用する語か、和訳しようのない固有の技術名。

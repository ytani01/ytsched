# TODO-099 wording 報告

## 対象ファイル

このコミットに入る `.md`:

- `TODO.md`（変更 / `git diff`）
- `docs/Developer.md`（変更 / `git diff`）
- `archives/todo/TODO-099. JavaScript の整形ツール（Prettier）を導入する.md`（新規）
- `archives/agents/TODO-099/README.md`（新規）
- `archives/agents/TODO-099/implementer-report.md`（新規）
- `archives/agents/TODO-099/verifier-report.md`（新規）

前例の件数は `git grep -cF <語> HEAD -- '*.md'` で数えた（基準は HEAD）。

## 前置き

HEAD には 6b8e706 で入った TODO-099 の項目本文がまだ残っている。
今回のコミットはそれを archives へ移す。そのため
`一括整形` `バグ検出寄り` `devDependency` `機械的な差分` は
HEAD で 1 件ヒットするが、その 1 件は移動元の項目本文そのもの
（＝今回の作業の一部）なので、実質は初出に近い。下でも触れる。

## 前例の無い語（件数の少ない順）

### 1. 整形差

- **箇所**: `verifier-report.md:29`「印字幅を 999 にして整形差を潰し、意味差だけ残す」
- **件数**: 前例なし（0）
- **見立て**: 「整形による差」を詰めた言い方。一般語ではない。
  「整形の差」「整形による違い」で足りる。造語の疑い。

### 2. 意味差

- **箇所**: `verifier-report.md:29,35,37`「意味差だけ残す」「意味差なし」
- **件数**: 前例なし（0）
- **見立て**: 「意味の差」を詰めた言い方。一般語ではない。
  「意味の違い」で足りる。造語の疑い。

### 3. 明示カッコ

- **箇所**: `verifier-report.md:43`「演算子混在時の明示カッコ」
- **件数**: 前例なし（0）
- **見立て**: 「明示的なカッコ」の短縮。定着した語ではない。
  「カッコの追加」「明示的なカッコ」で足りる。造語の疑い。

### 4. 演算子混在

- **箇所**: `verifier-report.md:43`「演算子混在時の明示カッコ」
- **件数**: 前例なし（0）。`演算子` 単独は前例 1、`混在` を含む語も別途あり
- **見立て**: 「演算子が混在するとき」を名詞化した言い回し。
  一般的な複合語ではない。言い換えで避けられる。造語の疑い（弱）。

### 5. 複数行展開

- **箇所**: `verifier-report.md:37`「同種の差 1 か所。… の複数行展開のみ」
- **件数**: 前例なし（0）
- **見立て**: 「複数行に展開されること」の名詞化。読めば通じるが
  定着した用語ではない。造語の疑い（弱）。

### 6. 機械的差分 / 機械的整形

- **箇所**: `implementer-report.md:35`「中身は機械的差分のみ」、
  `TODO-099….md:28,45`「機械的差分として許容」「機械的差分のみ」、
  `verifier-report.md:25`「機械的整形のみか」
- **件数**: `機械的差分` は前例なし（0）。`機械的な差分` は HEAD で 1
  （移動元の TODO-099 項目本文）。`機械的` 単独は 39 件と定着
- **見立て**: 「機械的」はリポジトリで多用されており、`機械的差分`
  も読めば通じる。ただし複合語としては初出。判断は main。

### 7. 非競合

- **箇所**: `README.md:8`「`mise run lintjs`（ESLint との非競合）」
- **件数**: 前例なし（0）。本文中の「競合しない」は複数あり
- **見立て**: 「競合しないこと」を名詞化した圧縮表現。一般語ではない。
  「ESLint と競合しないこと」と書けば済む。造語の疑い。

### 8. 冪等性

- **箇所**: `verifier-report.md:5`「## 1. fmtjs の冪等性」
- **件数**: `冪等性` は前例なし（0）。`冪等` は前例 1
  （`archives/agents/TODO-018/verifier-report-2.md`）
- **見立て**: 一般に通用する専門用語（idempotency）。`冪等` の前例も
  あり、造語ではない。初出なので一応挙げる。

### 9. 印字幅

- **箇所**: `verifier-report.md:29`「印字幅を 999 にして整形差を潰し」
- **件数**: 前例なし（0）
- **見立て**: Prettier の `--print-width`（1 行の折り返し幅）を指す。
  「印字幅」は定訳ではなく、この文書だけの訳語に見える。
  `--print-width` そのままか「折り返し幅」が無難。造語の疑い。

### 10. 末尾カンマ

- **箇所**: `verifier-report.md:41`「クォート `'`→`"`、末尾カンマ追加、セミコロン」
- **件数**: 前例なし（0）
- **見立て**: trailing comma の一般的な和訳。造語ではない。初出なので挙げる。

### 11. 左結合

- **箇所**: `verifier-report.md:48`「`*` と `/` は同順位・左結合なので評価順は不変」
- **件数**: 前例なし（0）。`同順位` は前例 1、`評価順` は前例 6
- **見立て**: left-associative の一般的な訳語。造語ではない。初出なので挙げる。

### 12. devDependency

- **箇所**: `TODO.md:127`（移動元の項目本文）、`TODO-099….md:32`
  「`devDependencies` に … を追加」
- **件数**: HEAD で 1（移動元の TODO-099 項目本文）。実質初出
- **見立て**: npm の用語。造語ではない。

## 前例があって問題なさそうな語（記録）

- `正規化`（43）`整形`（65）`収束`（5）`評価順`（6）`実行環境`（7）
  `インデント`（14）`クォート`（8）`セミコロン`（2）`バイト一致`（4）
- `タスクランナー`（1）`オブジェクトリテラル`（1）`同順位`（1）
  `stylistic`（1）`冪等`（1）
- `一括整形`（1）`バグ検出寄り`（1）— HEAD の 1 件は移動元の項目本文。
  今回のコミットで archives 側へ移るだけで、新語ではない。

## まとめ

- 読んだファイル: 上の「対象ファイル」6 つ
- 前例の無い語: **12 語**
  （整形差 / 意味差 / 明示カッコ / 演算子混在 / 複数行展開 /
  機械的差分・機械的整形 / 非競合 / 冪等性 / 印字幅 / 末尾カンマ /
  左結合 / devDependency）
- 12 語のうち、末尾カンマ・左結合・冪等性・devDependency は一般的な
  専門用語で、リポジトリで初出というだけ。
  整形差・意味差・非競合・明示カッコ・印字幅 は、この文書だけの
  圧縮表現／訳語に見える。判断は main。
- ほとんどが `verifier-report.md` に集中している。10 語超だが、
  半分は標準的な整形用語で、造語というより初出。依頼書
  （`TODO-099….md`）側は `機械的差分` くらいで、問題は薄い。
</content>
</invoke>

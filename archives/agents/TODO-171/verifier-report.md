# TODO-171 verifier 報告

## 1. `mise run fmt` / `typecheck` / `lint` / `test`

- `mise run fmt` — ○（ruff format 43 files unchanged、ruff check All checks passed!）
- `mise run typecheck` — ○（basedpyright 0 errors、mypy Success: no issues found in 40 source files）
- `mise run lint` — ○（fmt/typecheck/fmtjs/lintjs すべて通過）
- `uv run pytest -q --ignore=tests/test_browser.py` — ○ 595 passed
- `uv run pytest -q tests/test_browser.py` — ○ 61 passed（今回はタイミング依存の
  失敗は再現せず、全件成功）

## 2. `ytsched fix-id`

一時ディレクトリに旧形式・版なし UUID・既に `{UUID}-{版}`・ゼロ埋め
（`-001`）の 4 通りを仕込んで実行。

- `{年}/{月}/{日}.jsonl`・`ToDo.jsonl`・`trash.jsonl` の 3 つが対象。
  `.bak`・`.cgi` は内容・mtime とも変更なし — ○
- `sde_id` 以外のキー・並び順は変わらず（`trash.jsonl` の `trashed_at` も
  先頭のまま）— ○
- 既に UUID だった行は UUID を保って `-1` が付いた — ○
- 既に新形式の行しか無いファイルは書かれない（mtime 不変、
  `書き換えたファイル: 0`）— ○
- `--dry-run` で書き出されない（mtime・内容とも不変）— ○
- 出力件数（走査 3・書き換え 3・行 6・元から新形式 1）は実際の内訳と一致 — ○
- ゼロ埋め（`-001`）は新しい形式と見なされず、UUID も含めて新規に
  振り直された（仕様どおり）— ○

## 3. アプリの起動と画面（curl で form を組んで確認）

`--datadir` を一時ディレクトリにして起動、HTTP 200。ログに例外・
トレースバックなし。

- 予定作成 → `{UUID}-1` — ○
- 編集 → 版 `2` になり、ゴミ箱に版 `1` の行 — ○
- もう一度編集 → 版 `3`、ゴミ箱に版 `1`・`2` の 2 行 — ○
- ゴミ箱画面 — 3 行が同じグループにまとまり（`同じ予定の内容が 2 件` の
  表示、削除時は最新の 2 件がゴミ箱にある状態で確認）、各行に
  `版 1` `版 2` が出た。テンプレートに `{{ }}` `{%` の生残りなし — ○
- `?sde_id=` に版込み ID・UUID のみ、どちらを渡しても同じグループが
  出た — ○
- ゴミ箱から版 `1` を復活 → UUID を引き継ぎ、版は既存の生きている
  予定（版 `3`）ともゴミ箱最大（版 `2`）ともぶつからない版 `4` に
  なった — ○
- 予定を削除 → ゴミ箱の行の ID は削除した予定の ID（版 `3`）のままで、
  版は増えなかった — ○
- ゴミ箱の完全削除 — 1 件指定で狙った行だけ消え、続けて残り 2 件を
  まとめて指定した削除で狙った行だけ消えた（消した後 `trash.jsonl` は
  空）— ○

## 4. 旧形式の `sde_id` のまま編集

`fix-id` を当てる前を想定し、旧形式 ID の予定を直接ファイルに仕込んで
編集 → 新しい ID（`{UUID}-1`）が発行され、302 で編集画面へ遷移。落ちず、
ログにも例外なし — ○

## 気づいたこと（バグではなく既存の挙動）

- `_restore()` はゴミ箱の元の行を削除しない（復活後もゴミ箱に残る）。
  これは TODO-171 での変更ではなく、`git show HEAD:src/ytsched/trash_handler.py`
  で確認した既存の挙動のまま。依頼の確認項目（「ぶつからないこと」）は
  満たしているが、念のため記載する

## 見つかった不具合

なし。

---

## 2 回目の確認（reviewer 指摘の直し）

`list_all_files()` への一本化と、復活の版をデータディレクトリ全体の
走査で決めるようにした直しを確認した。

### 1. `mise run fmt` / `typecheck` / `lint` / `test`（`test_browser.py` 含む）

- `mise run fmt` — ○（43 files unchanged、All checks passed!）
- `mise run typecheck` — ○（basedpyright 0 errors、mypy no issues in 40 files）
- `mise run lint` — ○（fmtjs/lintjs も含めすべて通過）
- `uv run pytest -q`（`test_browser.py` 込みの全件）— ○ **664 passed**
  （前回の 595 + browser 61 + 今回追加分。今回はタイミング依存の失敗も
  再現しなかった）

### 2. `fix-id` の再実行

前回と同じ 4 通り（旧形式・版なし UUID・既に新形式・ゼロ埋め）を新しい
一時ディレクトリに仕込んで実行。

- 走査 3・書き換え 3・行 6・元から新形式 1 — 前回と同じ内訳 — ○
- `.bak`・`.cgi` は内容・mtime とも変更なし — ○
- 対象は `{年}/{月}/{日}.jsonl`・`ToDo.jsonl`・`trash.jsonl` の 3 つの
  ままで、`list_all_files()` への一本化後も対象が変わっていないことを
  確認 — ○

### 3. reviewer の再現手順（日付を変える編集 → 復活）

一時ディレクトリでアプリを起動し、curl で form を組んで再現。

- 予定作成（`2026-09-05`）→ `{UUID}-1` — ○
- 日付を `2026-09-06` に変えて編集 → 生きている予定は `2026-09-06` に
  版 `2`、ゴミ箱に `2026-09-05` の版 `1` の行 — ○
- ゴミ箱の版 `1` を復活 → 復活先は `2026-09-05`（ゴミ箱の行の日付に
  戻る）、ID は UUID を引き継いだ**版 `3`**。生きている予定（別ファイル
  `2026-09-06` の版 `2`）ともゴミ箱の行（版 `1`）とも重ならなかった。
  **直し前ならここで版 `2` になって `2026-09-06` の生きている予定と
  衝突していたはずの箇所** — ○（reviewer 指摘は解消）

### 4. 復活を続けて 2 回

同じゴミ箱の行（版 `1`。この実装は復活してもゴミ箱の行を消さない
既存仕様なので、もう一度同じ行を復活できる）をもう一度復活。

- 2 回目の復活で版 `4` が発行され、`2026-09-05` ファイルに版 `3` と
  版 `4` の 2 行が並んだ。重複なし — ○

### 5. 一連の操作の回帰確認

同じ環境で続けて確認。

- ゴミ箱画面 — 1 件だけのグループでは見出し
  （`同じ予定の内容が N 件`）が出ない仕様どおりの表示（テンプレートの
  `{% if len(group) > 1 %}` のとおり）。版の表示（`版 1`）は正しく出た
  — ○
- 予定を削除（`2026-09-06` の版 `2`）→ ゴミ箱の行の ID は版 `2` の
  ままで増えなかった — ○
- ゴミ箱の完全削除 — 1 件指定・複数件指定とも狙った行だけ消え、
  最終的に `trash.jsonl` が空になった — ○
- サーバのログに例外・トレースバックなし — ○

### 見つかった不具合

なし。

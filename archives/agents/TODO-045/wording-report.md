# TODO-045 wording 報告

## 対象

`git diff TODO.md` で追加された文面のみ（冒頭 1 行と「## TODO-045.」節）。
このコミットに入る `.md` は `TODO.md` の 1 ファイルだけなので、これが全て。

## 前例の有無

前例の件数が少ない順。

### `fa-angle-down`（0 件）

- 出てくる箇所: 「閉じているとき、詳細が長いと `fa-angle-down` のあとで
  折り返して 2 行になる」
- `git grep` の件数: 前例なし
- 見立て: Font Awesome のアイコンクラス名をそのまま指しているだけで、
  リポジトリ内の言い換えではない。造語ではないと判断できる。

### `1行に収める`（0 件）

- 出てくる箇所: 「折り返さず 1 行に収める」
- `git grep` の件数: 前例なし
- 見立て: ごく普通の言い回し。造語には見えない。

### `見え方`（複数件、TODO-038 系で頻出）

- 出てくる箇所: 「詳細（detail）の見え方を直す」（見出し）、
  「前後の空行は元からの見え方を保つために入れたもの」
- `git grep` の件数: 15 件（`archives/agents/TODO-038/` `archives/todo/TODO-038…md`
  `docs/data-format.md` など）
- 見立て: TODO-038 で既に使われている語で、今回が初出ではない。問題なし。

### `折り返す` / `折り返して`（TODO-033・TODO-039・TODO-007 に前例あり）

- 出てくる箇所: 「`fa-angle-down` のあとで折り返して 2 行になる」
  「折り返さず 1 行に収める」
- `git grep` の件数: `折り返し` 4 件、`折り返す` 2 件
- 見立て: 一般語であり、リポジトリ内でも既出。問題なし。

### `空行`（多数の前例あり、TODO-020・TODO-038 など）

- 出てくる箇所: 「開いたとき、詳細の前後に空行が入る」「前後の空行は…」
- `git grep` の件数: 24 件超
- 見立て: 一般語で既出。問題なし。

### `pre-wrap` / `white-space`（TODO-030・TODO-038・src/README.md に前例あり）

- 出てくる箇所: 「`.longtext` の `white-space: pre-wrap` は他でも使っているので」
- `git grep` の件数: それぞれ複数件
- 見立て: CSS プロパティ名で、既にこのリポジトリでも使われている。問題なし。

### `longtext`（TODO-030・TODO-038・TODO-039・TODO-040 に前例あり）

- 出てくる箇所: 「`.longtext` の `white-space: pre-wrap`」
- `git grep` の件数: 複数件
- 見立て: CSS クラス名の指示で、既出。問題なし。

## 判断できないもの

無し。

## 読んだファイル

- `TODO.md`（差分部分のみ）

## 前例の無い語

`fa-angle-down`、`1行に収める` の 2 語（いずれも造語というより、
アイコンクラス名の言及・普通の言い回しと見立てる）。

# TODO-026 wording 確認報告（対象: コミット 95895c1）

TODO-026 の手順（担当 = 呼び名を拾い `git grep` で前例の無い語を挙げる、
文書は直さない）を、過去のコミット `95895c1` に対して流した。
前例の基準は `95895c1^`（このコミットが入る直前）。

## 対象ファイル（`95895c1` に入っていた `.md` 全部）

- `TODO.md`
- `archives/agents/TODO-021/README.md`
- `archives/agents/TODO-021/implementer1-report.md`
- `archives/agents/TODO-021/implementer1-request.md`
- `archives/agents/TODO-021/implementer2-report.md`
- `archives/agents/TODO-021/implementer2-request.md`
- `archives/agents/TODO-021/reviewer-report.md`
- `archives/agents/TODO-021/reviewer-request.md`
- `archives/agents/TODO-021/runner-report.md`
- `archives/agents/TODO-021/verifier-report.md`
- `archives/agents/TODO-021/verifier-request.md`
- `archives/todo/TODO-021. リファクタリング（挙動は変えない）.md`

いずれも `git show 95895c1:<パス>` で読んだ（作業ツリーは切り替えていない）。

## 前例の無い語（`git grep -cF <語> 95895c1^ -- '*.md'` が 0）

前例の件数が少ない順（すべて 0 件のため、出てくる箇所の多さで並べた）。

### 1. 足場

- **出てくる箇所**: `implementer1-report.md`
  - 「新しいファイルにしたのは…『TODO-021 の**足場**』という位置づけを
    ファイル単位で分けたほうが…」（207〜209 行目付近）
  - 「単独で決めた判断 1」の同文言（339 行目付近）
- **件数**: 前例なし（0 件）
- **見立て**: characterization test／ゴールデンマスターテストを指して
  「足場」と呼んでいる。TODO-021 のときに main が気づかず、TODO-025 の
  きっかけになった造語そのもの。**このリポジトリ固有の言い換え**で、
  一般に通用する言い方ではない（「土台」の比喩は通じても、テストの
  意味では読み手に伝わらない）。今回の確認手順が、まさにこの語を
  拾えるかを試す再現テストになっている。

### 2. ゴールデンマスターテスト

- **出てくる箇所**: `implementer2-request.md`、`verifier-request.md`、
  `verifier-report.md`、`archives/todo/TODO-021. リファクタリング（挙動は変えない）.md`
  など計 7 か所（例:「ゴールデンマスターテストを先に足した（290 → 330 件）」）
- **件数**: 前例なし（0 件）
- **見立て**: "golden master test" はソフトウェア業界で広く通用する
  一般的な用語のカタカナ表記で、造語ではないと判断できる。ただし
  `95895c1^` 時点のこのリポジトリでは初出（TODO-025 が「ゴールデンマスター」も
  前例 1 件だった、と書いているのと同種の事例）。

### 3. まとめ役

- **出てくる箇所**: `implementer2-report.md`「単独で決めた判断 7」
  （「まとめ役のデータ型を作ることになる」）、
  `reviewer-report.md` の同文言の引き写し（「まとめ役の型を作ることに
  なり」）
- **件数**: 前例なし（0 件）
- **見立て**: 「まとめ役」という日本語自体は一般的だが（役割・世話役の
  意味）、ここでは「複数の値を 1 つにまとめて持つデータ型（集約用の
  型）」という技術的な意味で使っている。一般的な IT 用語（「集約型」
  「コンテナ」「まとめ役のオブジェクト」等）とまでは言い切れず、
  判断できない。

### 4. 呼び出し口

- **出てくる箇所**: `implementer2-request.md`「追記」
  （「`exec_update()` の分割は、`cmd_add()` の**呼び出し口**をそのままに
  行うこと」）
- **件数**: 前例なし（0 件）
- **見立て**: プログラミングでは「呼び出し元」「呼び出し箇所」
  「call site」のほうが一般的で、「呼び出し口」はこのリポジトリだけの
  言い換えに見える。

### 5. 受け側

- **出てくる箇所**: `implementer2-report.md`
  （「4 つの戻り値の**受け側**の並びも `date, time_start, time_end,
  detail` で合っている」）
- **件数**: 前例なし（0 件）
- **見立て**: 「受け取る側」の意味で自然には読めるが、「戻り値の受け側」
  という言い回し自体は一般的な IT 用語とは言えず、このリポジトリの
  言い換えに見える。

### 6. かたまり

- **出てくる箇所**: `reviewer-report.md`
  （「`filter_re` / `filter_neg` / `search_re` / `search_mode` /
  `search_n` の 5 つは『今回の検索条件』という 1 つの**かたまり**なので、
  別項目にする価値がある」）
- **件数**: 前例なし（0 件）
- **見立て**: ごく普通の日本語（「ひとかたまり」）で、一般に通用する
  表現。造語には見えない。

### 7. 差し戻す

- **出てくる箇所**: `reviewer-report.md`
  （「TODO-021 の C に挙がっていた指摘を B の範囲へ広げた形なので、
  範囲を超えたと見るなら**差し戻す**余地がある（実害は無い）」）
- **件数**: 前例なし（0 件）
- **見立て**: レビューの文脈で「差し戻す」は一般的な開発用語（コード
  レビューの reject／request changes の意味）。通用する言い方で、
  造語ではないと判断してよい。

### 8. 素通り

- **出てくる箇所**: `reviewer-report.md`
  （「`cmd == "del"` … `sde` の読み直しを飛ばし、`update` でもないので
  **素通り**」）
- **件数**: 前例なし（0 件）
- **見立て**: 一般的な日本語表現（「そのまま通り過ぎる」の意）。
  通用する言い方で、造語には見えない。

## 前例が既にある語（参考。今回の「前例の無い語」には含めない）

読みながら候補に挙げたが、`95895c1^` 時点で既に使われていたため対象外。

- 「挙動を押さえる」（1 件）
- 「揃っていない（条件）」（1 件）
- 「軽量な担当」（1 件）
- 「重複の解消」（1 件）
- 「見立て」（1 件）
- 「打ち切り」（5 件）
- 「水準」（5 件）
- 「食い違い」（12 件）
- 「切り分け」（7 件）
- 「確信度」（13 件）
- 「実害」（19 件）

## 読んだファイル

上の「対象ファイル」の 12 個すべて。

## 前例の無い語数

**8 語**（足場・ゴールデンマスターテスト・まとめ役・呼び出し口・受け側・
かたまり・差し戻す・素通り）。

TODO-025 の目安表（造語入りの報告は 15 語、造語なしの依頼書・archive は
1〜3 語）と比べると、8 語は中間的な数。ただし今回の 8 語のうち、
「足場」（既知の造語）を除く 7 語は、一般的な日本語表現（かたまり・
差し戻す・素通り）か一般に通用する専門用語（ゴールデンマスターテスト）が
半分を占め、リポジトリ固有の言い換えに見えるのは「まとめ役」
「呼び出し口」「受け側」の 3 語にとどまる。

# TODO-069 wording 報告

対象: `git diff TODO.md`（TODO-069 の節の書き直し）のみ。

前例の件数が少ない順に並べる。

## 1. `load_months`（前例なし）

- 箇所: 「N ヶ月は `conf.json` に置く（キー名は `load_months`）」
- `git grep -cF` の件数: 0
- 見立て: `conf.json` の新しいキー名。設定項目の識別子であって、
  一般語・専門用語というより固有の命名。造語というより「新しく決めた
  設定名」なので、この節で初出になるのは自然。判断できない。

## 2. 持っている範囲（前例なし）

- 箇所: 「持っている範囲の外へ出るときは、読み直して中心を移す」
  「持っている範囲の外へ出るときだけ `doGet()` する」
- 件数: 0
- 見立て: ブラウザが DOM に持っている週の範囲を指す言い回し。専門用語
  ではなく普通の日本語の組み合わせに見えるが、この文書内で繰り返し
  出てくる呼び名になっている。一般的な言い回しの範囲内という見立て。

## 3. 控えめ（前例なし）

- 箇所: 「既定値は控えめに 1 ヶ月から始めて」
- 件数: 0
- 見立て: 普通の日本語の形容動詞。造語ではない。

## 4. ダブルタップ（前例なし）

- 箇所: 「ホームボタンのダブルタップで読み直す」
- 件数: 0（旧title側の「ダブルクリック(タップ)」は 1 件ヒットするが、
  「ダブルタップ」という表記そのものは前例なし）
- 見立て: 一般に通用する IT 用語（スマートフォンの操作用語）。
  このリポジトリでは初出なだけで、造語ではないと見る。

## 5. DOM に抱えた（データ）（前例なし・「抱える」全体で 0）

- 箇所: 「ブラウザが数ヶ月ぶんの DOM を持てば」「DOM に抱えたデータは
  古くなるので」
- 件数（`抱える`）: 0
- 見立て: データを保持している状態を指す比喩的表現。一般的な日本語
  表現ではあるが、この文脈（ブラウザが週データを保持する）専用の
  言い回しとして定着させるつもりなら、以後も同じ言葉を使うべきという
  見立て。造語というより通常の比喩の範囲内。

## 6. N ヶ月（前例なし）

- 箇所: 「前後 N ヶ月ぶんの週を描いて返す」「N ヶ月を `conf.json` で
  設定できるようにする」
- 件数: 0
- 見立て: 変数のプレースホルダー的な表記。造語ではなく一般的な書き方。

## 7. 取り直せる道（前例なし）

- 箇所: 「手で取り直せる道を用意する」
- 件数: 0
- 見立て: 普通の日本語表現（「手段」の意味で「道」を使う言い回し）。
  一般に通用する範囲内と見るが、判断は main に委ねる。

## 8. panel（英単語のまま、前例 9 件）

- 箇所: 「隣の週の panel が DOM にあればそれを今の週にして」
- 件数: 9（`.my-week-panel` のクラス名としての言及を含む）
- 見立て: 「パネル」とカタカナ化せず英単語のまま地の文に置いている。
  クラス名 `.my-week-panel` を指している文脈なので、コードの識別子を
  そのまま使ったとも読める。造語ではないが、カタカナ表記との
  使い分けが気になる点。判断できない。

## 9. 先読み（前例 9 件）

- 箇所: 「だからサーバ側の先読みではなく、ブラウザ側で持つ」
- 件数: 9
- 見立て: 一般的な IT 用語（プリフェッチの意味の「先読み」）。
  このリポジトリでも既に使われており、造語ではない。

## 10. 通常フロー（前例 11 件）

- 箇所: 「通常フローに残るのは今週だけ」「通常フローに残す週を、
  JavaScript が差し替える」
- 件数: 11
- 見立て: CSS の "normal flow" を訳した専門用語で、このリポジトリでも
  既に使われている。造語ではない。

## 11. 週の差（前例 18 件）

- 箇所: 「`#cur_day` / `#date` / ヘッダのゲージ / 週の差の表示も、
  そこで揃える」
- 件数: 18
- 見立て: TODO-066 由来の既存の呼び名。造語ではない。

## 12. 取り直し（前例 10 件）

- 箇所: 「取り直しも描き直しも要らなくなる」
- 件数: 10
- 見立て: 一般的な日本語（「データを取り直す」の名詞形）。造語ではない。

---

## 読んだファイル

- `TODO.md`（差分のみ、`git diff TODO.md` の対象範囲）

## 前例の無い語数

6 語（`load_months` / 持っている範囲 / 控えめ / ダブルタップ / DOM に
抱えた / N ヶ月 / 取り直せる道 のうち、単語として数えると 7 語 —
下記参照）

※ 内訳: `load_months`、持っている範囲、控えめ、ダブルタップ、
DOM に抱えた（抱える）、N ヶ月、取り直せる道 の **7 語**。
いずれも一般的な日本語・IT 用語の範囲内に見え、TODO-025 で問題視された
ような「読み返しても気づけない独自の言い換え」（例: 足場）には
当たらないというのが見立て。ただし判断は main に委ねる。

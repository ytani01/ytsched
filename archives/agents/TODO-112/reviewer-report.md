# TODO-112 reviewer 報告

## 指摘

無し。

`tests/test_browser.py` の 237 行目と 261 行目は、週の値を ISO 形式の
日付文字列で比較しているため、目標週を通過しても待機が終わる。これで途中の
週を取り逃してタイムアウトする条件は除かれている。

開始・継続のテストは、ダブルタップ後に 5 週先へ達することを確認する。
ダブルタップによる 2 週送りだけでは届かず、少なくとも 3 回の自動送りが必要
なので、自動送りの開始と継続を確認できる。停止のテストも、自動送りによる
少なくとも 1 回の遷移を確認した後で停止し、1200 msec 後も週が変わらないことを
確認している。

実装差分は `tests/test_browser.py` と `tests/test_web.py` である。

## 追加確認

`tests/test_web.py` の 658 行目は、`main.html` 287 行目の
`window.ytsched.scrollToDate(...)` と一致する。945、955、965 行目の期待値も、
`main.html` 9 行目で `main-page.js` へ渡す
`window.ytsched.auto_turn_msec` と一致する。いずれも TODO-107 の公開先変更に
追随し、TODO-112 で全件テストを合格させるために必要な更新である。範囲外の
実装変更はない。

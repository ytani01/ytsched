# TODO-087 wording 報告

対象: `git diff` で追加された行のみ（`TODO.md`・`docs/design-review.md`）。

**この報告は 10 項目に分けた版を見たもの。** 報告のあとで C・F と
`webapp.py` の件を TODO-090 に寄せて 9 項目にしたので、TODO-093 以降の
番号が 1 つずつ繰り上がっている（報告中の TODO-094 は現在の TODO-093、
TODO-096 は TODO-095）。挙げられた語そのものは変わっていない。

指摘のうち、**「決めるだけのもの」は前例のある「決めるだけの項目」に
直した**。残りは一般語で、このリポジトリで未使用だっただけと判断して
そのままにした。

前例の件数が少ない順に並べる。

## 手戻り

- 出てくる箇所: `TODO.md` TODO-088「先にこちらを済ませてから TODO-071 に
  入るほうが手戻りが少ない。」
- `git grep -cF 手戻り HEAD -- '*.md'` → 前例なし（0 件）
- 見立て: 一般語だが、このリポジトリでは初出。「戻ってやり直す手間」の意で
  普通に通じる言い回しに見える。造語というより単に未使用だっただけの可能性が高い

## dataclass 化

- 出てくる箇所: `TODO.md` TODO-091 の見出し「`SchedData` の渡し方と、
  表示に渡す値の dataclass 化」
- `git grep -cF "dataclass 化" HEAD -- '*.md'` → 前例なし（0 件）
- 見立て: `〜化` は一般的な接尾辞で、`dataclass` 自体は既存語（他の項目でも
  型として言及あり）。組み合わせとして初出なだけで、造語には見えない

## 決めるだけのもの

- 出てくる箇所: `TODO.md` TODO-096「基本設計のレビュー（`docs/design-review.md`
  の P）のうち、決めるだけのもの。」
- `git grep -cF "決めるだけのもの" HEAD -- '*.md'` → 前例なし（0 件）
  （※「決めるだけの項目」という言い回しは 10 件の前例あり。「項目」→「もの」
  への言い換え）
- 見立て: 既存の言い回し「決めるだけの項目」とほぼ同義の言い換え。
  問題は薄いと思うが、統一するなら「項目」に揃える手もある

## 検索側の持ち物にする

- 出てくる箇所: `TODO.md` TODO-088「`SchedLoadCond` の `search_re` /
  `search_n` を検索側の持ち物にする」
- `git grep -cF "検索側の持ち物にする" HEAD -- '*.md'` → 前例なし（0 件）
  （※「持ち物」という語自体は `docs/design-review.md:70` に 1 件の前例あり）
- 見立て: 「持ち物」は既存語なので、組み合わせが初出なだけ。判断できない

## 実質もう一つの画面

- 出てくる箇所: `TODO.md` TODO-088「検索は実質もう 1 つの画面になっている」
- `git grep -cF "実質もう" HEAD -- '*.md'` → 前例なし（0 件）
  （「実質」単独は 43 件の前例あり）
- 見立て: 普通の日本語の言い回しで、造語には見えない

## ゲージへ値を渡している

- 出てくる箇所: `TODO.md` TODO-094「`#date_from` はゲージへ値を渡している
  だけなので `data-*` 属性にする」
- `git grep -cF "ゲージへ値を渡している" HEAD -- '*.md'` → 前例なし（0 件）
  （「ゲージ」単独は 217 件、「ゲージのために値を渡している」という
  近い言い回しが `docs/design-review.md:262` に既にある）
- 見立て: 既存の言い回しの言い換え。問題は薄い

## 覚えている変更

- 出てくる箇所: `TODO.md` TODO-093 の見出し「`get_conf_arg()` の重複と、
  キャッシュが覚えている変更を見ていない件」
- `git grep -cF "覚えている変更" HEAD -- '*.md'` → 1 件
  （`HEAD:docs/design-review.md`。同じ文書の既存部分で「変更を覚えている」
  という近い表現がある）
- 見立て: 「キャッシュが覚えている」は一般的な比喩表現。造語ではなさそう

## 初めて壊れる

- 出てくる箇所: `TODO.md` TODO-094「順番を入れ替えても読み込み時には
  何も起きず、押したときに初めて壊れる。」
- `git grep -cF "初めて壊れる" HEAD -- '*.md'` → 1 件
- 見立て: 普通の日本語。造語には見えない

## 割り切る

- 出てくる箇所: `TODO.md` TODO-094「いま見ている日付を `ytState` に持ち、
  hidden input はフォームで送るときに書くものと割り切る。」
- `git grep -cF 割り切る HEAD -- '*.md'` → 1 件（`docs/design-review.md:262`、
  同一文書の既存箇所）
- 見立て: 一般的な日本語の動詞。造語ではない

## 更新の実行

- 出てくる箇所: `TODO.md` TODO-087 の見出し「更新の実行を `MainHandler` から
  出す」
- `git grep -cF "更新の実行" HEAD -- '*.md'` → 3 件
- 見立て: 既存文書（TODO-077 系）で使われている言い回し。前例あり

## 掃除

- 出てくる箇所: `TODO.md` TODO-092 の見出し「テンプレートの掃除」
- `git grep -cF 掃除 HEAD -- '*.md'` → 4 件
- 見立て: 一般語で問題なさそう

## 諦める

- 出てくる箇所: `TODO.md` TODO-095「`SEARCH_MODE_DAYS`…は『1 件も当たらない
  ときに諦める日数』」
- `git grep -cF 諦める HEAD -- '*.md'` → 3 件
- 見立て: 一般語で問題なさそう

---

## 読んだファイル

- `TODO.md`（追加された TODO-087〜096 の節、先頭ヘッダの更新箇所）
- `docs/design-review.md`（冒頭に追加された対応表）

## 前例なしの語数

4 語（手戻り／dataclass 化／決めるだけのもの／実質もう一つの画面／
検索側の持ち物にする／ゲージへ値を渡している の完全一致ベースで数えると
実際は 6 語。ただし「決めるだけのもの」「検索側の持ち物にする」
「ゲージへ値を渡している」は構成語・近い言い回しに 1 件以上の前例があり、
語そのものが真新しいのは「手戻り」「dataclass 化」「実質もう一つの画面」の
3 語程度と見ている）。

# TODO-101 wording 報告

対象は次の 4 本（コミットに入る `.md` 全部）。

- `TODO.md`（差分）
- `archives/todo/TODO-101. 編集画面フッターのボタンをセンタリングする.md`
- `archives/agents/TODO-101/README.md`
- `archives/agents/TODO-101/verifier-report.md`

## 前例の無い語（件数が少ない順）

### 実クリック / 実クリック相当
- 出てくる箇所: `verifier-report.md`「`add` のみ実クリック相当で確認」
  「『戻る』『update』『fix』『del』の onmousedown まで実クリックでは
  確認していない」
- `git grep` の件数: 0 件（前例なし）
- 見立て: 「クリックイベントを実際に発火させて確認する」という意味は
  文脈から通じるが、「実クリック」という短縮の言い方はこのリポジトリで
  初出。一般に通用するかは判断できない。

### コード経路
- 出てくる箇所: `verifier-report.md`「複製ボタンの `submitCmd('add')` と
  同じコード経路であることを確認」
- `git grep` の件数: 0 件（「コード経路」という並びとしては前例なし。
  ただし「経路」単独は 90 件超の前例がある一般語）
- 見立て: 「コードパス」の和訳として一般にも通じそうだが、この
  並び自体は初出。単独の「経路」で足りた可能性がある。

### 主観の余地
- 出てくる箇所: `verifier-report.md`「`gap: 2rem` は見た目やや広めだが
  崩れてはいない（この判断は目視によるもので、主観の余地あり）」
- `git grep` の件数: 0 件（前例なし）
- 見立て: 一般的な言い回しで、リポジトリ固有の言い換えには見えない。

### 指の当たる範囲
- 出てくる箇所: `TODO-101. 編集画面フッターのボタンをセンタリングする.md`
  「`.my-edit-menu > .my-btn` には左右のパディングを付けて、アイコンの
  周りにも指の当たる範囲を残した」
- `git grep` の件数: 0 件（前例なし）
- 見立て: 「タップ領域」「タッチターゲット」に相当する意味だが、この
  言い方自体は初出。一般に通用するかは判断できない。

### 一定の間隔
- 出てくる箇所: `TODO.md`（旧 TODO-101 節の文言、消える差分側）
  「4つのボタン(戻る、リロード、保存、複製)を一定間隔でならべ、
  センタリングする」／ 同アーカイブ文書「削除も含めた 5 つを、
  一定の間隔で中央に並べるようにした」
- `git grep` の件数: 0 件（「一定の間隔」という並びとしては前例なし。
  「間隔」単独は他の文書に前例あり）
- 見立て: 一般的な言い回しで、問題は無さそうに見える。

### かためて（詰めて中央に「かためる」）
- 出てくる箇所: `TODO-101. ….md`「利用者から『もっと中央にかためて』と
  指摘があり」
- `git grep` の件数: 0 件（前例なし）
- 見立て: 利用者の発言をそのまま引用した口語的表現。一般的な日本語で、
  リポジトリ固有の造語には見えない。

### 空の枠 / 枠ごと
- 出てくる箇所: `TODO-101. ….md`「複製の枠を空の `.col-2` で埋めていた」
  「新規作成のときは枠ごと出さない」／ `README.md`「新規作成のときに
  複製の枠を空の `.col-2` で埋めていたのをやめ、枠ごと出さないように
  した。センタリングでは空の枠が中央をずらす」
- `git grep` の件数: どちらも 0 件（「枠」単独は 25 件の前例あり）
- 見立て: HTML の 12 列グリッドの 1 マスを「枠」と呼ぶこと自体は自然で、
  「空の枠」「枠ごと」という組み合わせがこの文書で初出なだけに見える。
  問題は無さそうに見える。

## 前例はあるが、念のため触れておく語

- **センタリング**（`git grep` 2 件、前例あり）— TODO.md の旧節や過去の
  文書で既出。造語ではない。
- **入れ物**（10 件）・**目視**（30 件）・**崩れ**（65 件）・
  **スクラッチパッド**（1 件）・**折り返さず**（2 件）— いずれも
  このリポジトリで既に使われている言い方で、前例あり。

## 読んだファイル

- `TODO.md`
- `archives/todo/TODO-101. 編集画面フッターのボタンをセンタリングする.md`
- `archives/agents/TODO-101/README.md`
- `archives/agents/TODO-101/verifier-report.md`

## 前例の無い語数

7 語（実クリック／コード経路／主観の余地／指の当たる範囲／一定の間隔／
かためて／空の枠・枠ごと）

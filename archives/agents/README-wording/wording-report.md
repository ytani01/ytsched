# README.md wording 報告

対象: `git diff README.md`（README.md 1 ファイルのみ）

## 前例の件数が少ない順

### 送る間隔（0 件）
- 箇所: 「主な特徴」の自動ページ送りの説明「送る間隔は調整可能。」
- `git grep -cF 送る間隔 HEAD -- '*.md'`: 前例なし
- 見立て: 「間隔」自体はよく使う語だが、「送る間隔」という組み合わせは
  このリポジトリでは初出。一般には通用しそうだが、「更新間隔」
  「送信間隔」などのほうが通りやすいかもしれない。判断できない

### 画面で操作した内容（0 件）
- 箇所: 「設定」節「検索語や絞り込みなど、画面で操作した内容は
  自動的に保存されるので、ふだん触る必要はない。」
- `git grep -cF 画面で操作した内容 HEAD -- '*.md'`: 前例なし
- 見立て: フレーズとしては初出だが、構成語（画面／操作／内容）は
  いずれも普通の語で、造語には見えない

### 自動的に保存（0 件）
- 箇所: 同上
- `git grep -cF 自動的に保存 HEAD -- '*.md'`: 前例なし
- 見立て: ごく一般的な言い回し。問題なさそうに見える

### 調整可能（1 件）
- 箇所: 「送る間隔は調整可能。」
- `git grep -cF 調整可能 HEAD -- '*.md'`: 1 件
- 見立て: 一般的な語。問題なさそうに見える

### 土台の指定（5 件）
- 箇所: 「外部のライブラリ」節「土台の指定（`body` のフォント・文字色・
  行の高さ、`box-sizing` など）は Bootstrap 5.3.8 から写したもの」
- `git grep -cF 土台の指定 HEAD -- '*.md'`: 5 件（元の文にも同じ表現が
  あり、今回はそこから短く再構成しただけ）
- 見立て: 既にこのリポジトリで定着している言い回し。判断できないが、
  少なくとも今回の新造ではない

### 帰属表示（6 件）
- 箇所: 削除された旧文にはあったが、今回の diff の追加分には
  直接は出てこない（アイコン節の記述を短くした結果、語自体は
  今回のバージョンには残っていない）
- 一応確認したが対象外。省略

## 前例が十分にある語（参考、造語の疑いは薄い）

- 自動ページ送り（32 件）、ダブルタップ（48 件）、フッター（54 件）、
  読み直す（43 件）、既定値（151 件）、データディレクトリ（37 件）、
  設定（219 件）、線画（16 件）、座標（16 件）、余白（58 件）、
  グリッド（11 件）、読めない値（13 件）、アプリは読むだけ（2 件）、
  手で書く（8 件）、文字列で（19 件）

## 読んだファイル

- README.md（`git diff README.md` の差分部分を中心に読んだ）

## 前例の無い語数

3 語（送る間隔／画面で操作した内容／自動的に保存）。いずれも
一般的な言い回しの組み合わせで、造語というより初出のフレーズに
見える。

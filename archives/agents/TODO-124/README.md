# TODO-124 の分担

| 担当 | 役割 |
| --- | --- |
| main | `main.html` のフッタから「ゴミ箱」の文字列を削る |
| verifier | lint・テストと、一時データディレクトリで起動したアプリのフッタを確認する |

変更はテンプレート 1 行の削除なので main が実装する。リンクが残ったまま
アイコンだけになっているかを、verifier が別に確認する。

各担当の報告は、このディレクトリに置く。

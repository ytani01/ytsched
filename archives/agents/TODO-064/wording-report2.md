# TODO-064 wording 報告（2 回目）

対象は、このコミットに入る `.md` 7 個。基準は `HEAD`（`5c2fca9`）。

## 前例なしの語

### 死角

- 出てくる場所: `TODO-064.md`「クリックとドラッグの間の死角」（見出し）、
  `reviewer-report.md`「1. クリックとドラッグの間に「死角」があり、
  クリックが無音で消える」
- `git grep -cF 死角 HEAD -- '*.md'`: 前例なし
- 見立て: 「隙間」「盲点」と言い換えられる一般語で、専門用語ではない。
  reviewer が最初に使い、TODO-064.md（main が書いた本文）へそのまま
  取り込まれている。危険な造語ではないが、初出なので報告する

### 窓の外

- 出てくる場所: `TODO-064.md`「窓の外で `mouseup` を出さずに離す」、
  `verifier-request.md`「窓の外でボタンを離すと `mouseup` が来ず」、
  `verifier-report2.md`「窓の外で mouseup を出さずに離した状況」
- `git grep -cF "窓の外" HEAD -- '*.md'`: 前例なし
- 見立て: 「ウィンドウの外」の意味の普通の言い回し。一般に通用する
  表現で、リポジトリ固有の言い換えには見えない

### 混線

- 出てくる場所: `reviewer-request.md`「タッチとマウスで `swipeStart` /
  `swipeDragging` を共有していることによる混線」
- `git grep -cF 混線 HEAD -- '*.md'`: 前例なし
- 見立て: 「状態の取り違え」を指す比喩として一般に使われる語。
  専門用語ではなく、造語というより通常の日本語表現

### タッチ対応ノート PC

- 出てくる場所: `TODO-064.md`「タッチ対応ノート PC で、タッチの直後
  700ms 以内に本物のマウス操作をすると」、`reviewer-report.md`
  「両方を持つ機器（タッチ対応ノート PC など）」
- `git grep -cF "タッチ対応ノート PC" HEAD -- '*.md'`: 前例なし
- 見立て: 機種の説明であって用語ではない。問題視するようなものではない

### 無音で消える

- 出てくる場所: `reviewer-report.md`「クリックとドラッグの間に「死角」
  があり、クリックが無音で消える」（見出し）
- `git grep -cF "無音で消える" HEAD -- '*.md'`: 前例なし
- 見立て: 「何も起きずに失敗する」の比喩表現。TODO-064.md 本文では
  「黙って何も起きない」と言い換えられており（この言い回しは既存で
  前例 62 件）、reviewer 独自の表現がそのまま報告書に残っている形。
  一般に通用するかは微妙で、判断できない

### 回帰なし

- 出てくる場所: `verifier-report2.md`「動かさずクリック → 編集画面へ
  （回帰なし）」「200px 左ドラッグ → 次週、200px 右ドラッグ → 前週
  （回帰なし）」
- `git grep -cF "回帰なし" HEAD -- '*.md'`: 前例なし（「回帰」単体では
  10 件の前例あり）
- 見立て: リグレッションテストの文脈で「回帰」は一般的な用語。
  「回帰なし」という結合はこのリポジトリでは初出だが、造語というより
  自然な言い方に見える

### 後始末が無い

- 出てくる場所: `reviewer-report.md`「2. `mouseDownHdr` に
  `touchStartHdr`相当の後始末が無い」（見出し）
- `git grep -cF "後始末が無い" HEAD -- '*.md'`: 前例なし（「後始末」
  単体では 16 件の前例あり）
- 見立て: 既存語「後始末」の自然な活用形。問題視する必要は無さそう

### 小刻み

- 出てくる場所: `verifier-report2.md`「指定 px だけ小刻みに動かして
  から離す」
- `git grep -cF 小刻み HEAD -- '*.md'`: 前例なし
- 見立て: ごく普通の日本語。造語ではない

### 光学マウス

- 出てくる場所: `reviewer-report.md`「手ぶれ、光学マウス以外の追従
  特性、トラックパッドなど」
- `git grep -cF 光学マウス HEAD -- '*.md'`: 前例なし
- 見立て: 一般名詞。問題無い

### 手ぶれ

- 出てくる場所: `reviewer-report.md`（上記と同じ文）
- `git grep -cF 手ぶれ HEAD -- '*.md'`: 前例なし
- 見立て: 一般名詞。問題無い

### トラックパッド

- 出てくる場所: `reviewer-report.md`（上記と同じ文）
- `git grep -cF トラックパッド HEAD -- '*.md'`: 前例なし
- 見立て: 一般名詞。問題無い

## 前例が少ないが挙げるほどでもないと判断し、報告に含めなかったもの

「素通し」「素通り」「後始末」「確信度」「見送った」「追従」「見立て」
「見分け」「非対称」「近似」「画面外」「伝播」「capture」「余白」
「黙って」は、いずれも単語単体で `HEAD` に 4 件以上の前例があり、
このリポジトリで既に定着している語だったため除外した。

## 読んだファイル

- `archives/todo/TODO-064. PC のマウスの左右ドラッグでも週を送れるようにする.md`
- `archives/agents/TODO-064/README.md`
- `archives/agents/TODO-064/verifier-request.md`
- `archives/agents/TODO-064/reviewer-request.md`
- `archives/agents/TODO-064/verifier-report.md`
- `archives/agents/TODO-064/verifier-report2.md`
- `archives/agents/TODO-064/reviewer-report.md`

## 前例なしの語数

11 語（死角・窓の外・混線・タッチ対応ノート PC・無音で消える・回帰なし・
後始末が無い・小刻み・光学マウス・手ぶれ・トラックパッド）。

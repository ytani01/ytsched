# TODO-065 wording 報告

## 対象

`git diff --cached --name-only -z -- '*.md' | tr '\0' '\n'` および
`git diff --name-only -z HEAD -- '*.md' | tr '\0' '\n'` で確認したところ、
今回の変更で `.md` は `TODO.md` の 1 ファイルのみ（TODO-065 の節の追加と、
冒頭の「残っている項目」「番号は」の行の更新）。

基準は `HEAD`（この差分がまだ入っていない時点）。

## 前例の無い語（0 件）

### 未保存

- 箇所: 「**未保存の変更があっても確認しない。** 押したらそのまま戻る」
- `git grep -cF 未保存 HEAD -- '*.md'`: 前例なし（0 件）
- 見立て: 「未保存の変更」は UI・エディタ一般で広く通用する言い回し
  （例: ブラウザやエディタの「未保存の変更があります」）。造語ではなく、
  単にこのリポジトリで初めて使っただけと見える

### 直リンク

- 箇所: 「`{{ url_prefix }}?date={{ date }}` への**直リンク**にする」
- `git grep -cF 直リンク HEAD -- '*.md'`: 前例なし（0 件）
- 見立て: 「直リンク」はネットスラング寄りだが広く通用する語（Wikipedia
  等でも使われる）。ここでの意味（history.back() を使わず、URL を直接
  埋め込む）とも整合する。一般に通用しそうで、このリポジトリだけの
  言い換えには見えない

### 検索表示

- 箇所: 「検索語は `conf.json` に残るので、週表示へ移っても**検索表示**の
  ままになりうるが、この項目では触らない」
- `git grep -cF 検索表示 HEAD -- '*.md'`: 前例なし（0 件）
- 見立て: 判断できない。「週表示」（前例37件）と対をなす言い方として
  自然だが、既存の archives で「検索表示」という呼び名を使った箇所は
  見当たらず、このコミットでの言い換えの可能性もある

## 参考（前例が少数だが 0 件ではないもの）

- **戻るボタン**: 5 件（`archives/todo/TODO-048...` 等）。一般語で問題なし
- **フッター**: 1 件。一般的な IT 用語
- **history.back()**: 1 件（`archives/todo/TODO-048...`）。コードそのもの
  なので語ではない
- **更新ボタン**: 1 件。一般語
- **col-4**: 1 件。Bootstrap のクラス名そのもの

いずれも一般的な語・コードの引用であり、造語の疑いは無いと見立てる。

## 読んだファイル

- `TODO.md`（差分部分、TODO-065 の節）

## まとめ

- 前例の無い語: **3 語**（未保存 / 直リンク / 検索表示）
- 目安（TODO-025 の 1〜3 語 = 概ね問題ない範囲）に収まっている
- 3 語とも一般的な日本語・広く通用する言い回しに見え、造語の疑いは薄いが、
  「検索表示」だけは見立てが付かないため、そのまま挙げる

# TODO-041 wording 報告

基準は「その文書が入る前」＝ `HEAD`（`b9a12a8`）。作業ツリーではなく
`HEAD` を対象に `git grep -cF` で数えた。

前例の少ない順に並べる。

## 判定基準（前例なし）

- **出てくる箇所**: `archives/agents/TODO-041/verifier-report.md` 61-62 行目
  「依頼書の判定基準（エラーが出ないか・遷移するはずのときに遷移するか）を満たす」
- **`git grep -cF` の件数**: 0 件
- **見立て**: 「判定」（115 件）「基準」も単独ならありふれた語で、
  組み合わせも一般的な言い回しに見える。この 2 語をつなげた形が
  リポジトリで初出というだけで、造語には見えない

## アンカーリンク（前例なし）

- **出てくる箇所**: `archives/todo/TODO-041. ….md` 78-79 行目
  「`:root { scroll-behavior: auto; }` を書けばアンカーリンクなど他の
  暗黙のスクロールにも効くが」
- **`git grep -cF` の件数**: 0 件
- **見立て**: HTML の `<a href="#id">` によるページ内リンクを指す一般的な
  IT 用語。このリポジトリでは初出だが、言い換えではなく普通の技術用語

## reboot（前例なし）

- **出てくる箇所**: `archives/todo/TODO-041. ….md` 27 行目
  「Bootstrap 5.3.8 の reboot が次の 1 行を持っている」
- **`git grep -cF` の件数**: 0 件
- **見立て**: Bootstrap が自身の CSS リセット部分に付けている固有名詞
  （`_reboot.scss` 由来）。訳さずそのまま使うのが妥当で、このリポジトリの
  言い換えではない

## 道具（1 件）

- **出てくる箇所**: `archives/todo/TODO-041. ….md` 107 行目
  「headless の Chromium が smooth を実行しないことは、確認の道具になる」
- **`git grep -cF` の件数**: 1 件（`archives/agents/TODO-027/implementer-report4.md`
  「共通の道具を 2 つ足した」）
- **見立て**: 「確認の手段」というほどの意味。1 件だけとはいえ前例があり、
  普通の日本語。造語には見えない

## 遷移（1 件）

- **出てくる箇所**: `archives/agents/TODO-041/verifier-request.md` 95・102 行目、
  `verifier-report.md` 58-62 行目（「遷移するはずのときに遷移するか」など）
- **`git grep -cF` の件数**: 1 件（`archives/agents/TODO-039/reviewer-report.md`
  「ページ遷移のたびに…」）
- **見立て**: Web の画面遷移を指す一般的な IT 用語。問題なし

## 位置合わせ・一度で移る・狙った位置・自動スクロール（各 1 件、いずれも同じ TODO-041 の先行コミットに前例あり）

- **出てくる箇所**: いずれも `archives/todo/TODO-041. ….md` 中で
  繰り返し使われている（「その最後の位置合わせがアニメーションになっていた」
  26 行目、「一度で移った」83-84 行目 相当、「狙った位置」92-93 行目、
  タイトルの「自動スクロール」）
- **`git grep -cF` の件数**: いずれも 1 件（前例は `TODO.md` の
  TODO-041 項目の説明文。これは今回のコミットより前の `b9a12a8`
  「TODO-041 として立てる」で既に入っている）
- **見立て**: 項目を立てたときの文言をそのまま踏襲しているだけで、
  今回のコミットで新しく作った言い回しではない。造語ではない

## 前例が多く、問題なさそうな語（参考）

以下は件数が多く、このリポジトリで既に定着した語なので詳細は省く。
切り分け（22）・申し送り（11）・化ける（8）・実機（12）・headless（12）・
分担（116）・見立て（199）・既定（185）・着手（56）・決着（40）・
判定（115）・見え方（14）・想定どおり（8）・瞬時（2）・暗黙の（1）・
打ち消す（4）。

## 読んだファイル

- `TODO.md`（TODO-041 を archives へ移した差分）
- `archives/todo/TODO-041. 追加読み込みのたびに自動スクロールが起きるのを直す.md`
- `archives/agents/TODO-041/README.md`
- `archives/agents/TODO-041/verifier-request.md`
- `archives/agents/TODO-041/verifier-report.md`
- `archives/agents/TODO-041/wording-request.md`

## 前例なしの語数

**3 語**（判定基準・アンカーリンク・reboot）。TODO-025 の目安
（「造語を作っていない依頼書・archive」は 1〜3 語）の範囲内で、
いずれも一般的な IT 用語か普通の日本語の組み合わせに見え、
このリポジトリだけの言い換えには見えない。

# TODO-121 の分担

| 担当 | 役割 |
| --- | --- |
| main | ゴミ箱画面のテンプレートとスタイルを、既存の予定表示・操作アイコンに揃える |
| verifier | 既存テストと一時データディレクトリでの表示確認 |

データと復活の挙動は変えず、既存部品の組み合わせだけを変えるため、実装は main が
担当する。画面変更でも実際の表示と既存の復活処理を別担当で確認する。

各担当の報告は、このディレクトリに置く。

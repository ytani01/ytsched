# TODO-074 wording 報告

対象: TODO.md の差分（`git diff TODO.md`、TODO-074 を立てる部分）。
このコミットに入る `.md` は `TODO.md` の 1 ファイルのみ。

## 前例の無い語

### xPercent2days

- 出てくる箇所: `TODO.md` の TODO-074 節、1 個目のチェック項目
  「`days2xPercent()` の逆算（`xPercent2days()`）を `my.js` に足す」
- `git grep -cF xPercent2days HEAD -- '*.md'`: 前例なし（0 件、`HEAD` 時点）
- 見立て: これから作る関数名そのものなので、前例が無いのは当然。
  対になる `days2xPercent` は TODO-058／TODO-059 の archives に既出
  （`git grep -cF days2xPercent HEAD -- '*.md'` で TODO-058 の archive 1 件、
  TODO-059 の archive 1 件、`archives/agents/` にも複数件）。命名の対称性
  から自然に導ける名前で、造語というより実装対象の識別子。問題ないと見る

## 前例があった語（参考。造語ではない）

念のため、他に呼び名になりそうな語も `HEAD` に対して確認したが、
いずれも TODO-042／043／049／054〜072 などの既存 archives・
`src/README.md`・`tests/README.md` に前例があった。

逆算、頭打ち、帯（`.my-gage-bar` を含む）、針、目盛り、週送り、
スワイプ、タップ、ゲージ、月曜、読み直す、ラベル、週バー、ジャンプ、
追従、`days2xPercent`、`my-gage-bar`、針の位置。

## 読んだファイル

- `TODO.md`（差分部分。このコミットに入る唯一の `.md`）

## まとめ

前例の無い語: **1 語**（`xPercent2days`）。これは新しく作る関数名を
文中で言及したものであり、一般的な意味での造語（呼び名の言い換え）
には当たらないと考えるが、判断は main に委ねる。

## 追記: 決着分の確認

対象: TODO-074 を決着させるコミットに入る `.md` 全部（未コミット）。

- `TODO.md`（差分）
- `archives/todo/TODO-074. ゲージをタップして、その週へジャンプできるようにする.md`（新規）
- `archives/agents/TODO-074/README.md`（新規）
- `archives/agents/TODO-074/implementer-task.md` / `implementer-report.md`（新規）
- `archives/agents/TODO-074/verifier-task.md` / `verifier-report.md`（新規）

実際に読んで拾った、呼び名になり得る語のうち、`git grep -cF <語> HEAD -- '*.md'`
で前例の無かったものを、件数の少ない順（0 件が並ぶものは登場順）に挙げる。

### 握りつぶす（「握りつぶされたりしないか」）

- 出てくるファイルと箇所: `implementer-task.md` 28行目
  「逆にゲージのタップが握りつぶされたりしないか確かめること」
- 件数: 前例なし（0 件）
- 見立て: 「イベントが途中で消費されて後続に伝わらない」ことを指す、
  日本語のプログラミング談話で広く使われる言い回し。このリポジトリでの
  初出というだけで、造語には見えない

### 由来がずれる（「呼び出し経路によって由来がずれるため」）

- 出てくるファイルと箇所: `TODO-074.md` 55行目
  「`event.target` / `event.currentTarget` に頼らず…取り直している。
  …呼び出し経路によって由来がずれるため」
- 件数: 前例なし（0 件）
- 見立て: 「イベントオブジェクトの `target` が指す要素が、呼び出し元に
  よって食い違う」ことを短く言っているが、「由来がずれる」という
  組み合わせ自体はこのリポジトリで見ない言い方。意味は文脈で通るので
  読めなくはないが、やや独自の圧縮した言い回しに見える。判断できない

### 塗り潰される／塗りつぶす（「帯が一色に塗り潰され」「帯全体を塗りつぶす」）

- 出てくるファイルと箇所: `TODO-074.md` 59行目、`verifier-report.md` 53行目
- 件数: 前例なし（0 件）
- 見立て: 「背景色で覆われて他が見えなくなる」という一般的な言い方。
  造語ではないと見る

### 対数の目盛り（`days2xPercent()` の説明として）

- 出てくるファイルと箇所: `TODO-074.md` 23行目
  「`days2xPercent()`（対数の目盛り）の逆算で」
- 件数: 前例なし（0 件、フレーズとして。「対数」14件・「目盛り」64件は
  それぞれ既出）
- 見立て: 既出の語の組み合わせで、造語ではないと見る

### 丸め誤差（「丸め誤差（1 週間ぶん）の範囲内」）

- 出てくるファイルと箇所: `verifier-report.md` 44行目
- 件数: 前例なし（0 件）
- 見立て: 一般に通用する IT 用語（rounding error の訳語）。この
  リポジトリでは初出というだけ

### 視覚的な手がかり（「視覚的な手がかりが一瞬消える」）

- 出てくるファイルと箇所: `verifier-report.md` 51行目
- 件数: 前例なし（0 件。「視覚的」3件・「手がかり」4件はそれぞれ既出）
- 見立て: 一般的な言い方の組み合わせ。造語ではないと見る

### 薄い半透明（「薄い半透明の白」）

- 出てくるファイルと箇所: `TODO-074.md` 60行目、`verifier-report.md` 74行目
- 件数: 前例なし（0 件）
- 見立て: 一般的な形容の組み合わせ。造語ではないと見る

### 手作業（「手作業のスクリプト」「別途手作業でも確認」）

- 出てくるファイルと箇所: `implementer-report.md` 33・40行目、
  `verifier-report.md` 43行目
- 件数: 前例なし（0 件）
- 見立て: ごく普通の日本語。このリポジトリでの初出というだけで、
  問題ないと見る

### 強い変化・控えめな変化（「黄色一色だったときのような強い変化ではなく、控えめな変化になった」）

- 出てくるファイルと箇所: `verifier-report.md` 91行目
- 件数: 前例なし（「強い変化」0件、「控えめ」自体は4件既出）
- 見立て: 一般的な形容。造語ではないと見る

### 帯全体（「帯全体が黄色一色になり」）

- 出てくるファイルと箇所: `verifier-report.md` 48行目
- 件数: 1 件（`帯` 単体は 52 件既出）
- 見立て: 「帯」に「全体」を付けただけで、造語には見えない

## 読んだファイル

- `TODO.md`（差分）
- `archives/todo/TODO-074. ゲージをタップして、その週へジャンプできるようにする.md`
- `archives/agents/TODO-074/README.md`
- `archives/agents/TODO-074/implementer-task.md`
- `archives/agents/TODO-074/implementer-report.md`
- `archives/agents/TODO-074/verifier-task.md`
- `archives/agents/TODO-074/verifier-report.md`

## まとめ

前例の無い語: **9 語**（握りつぶす／由来がずれる／塗り潰される・
塗りつぶす／対数の目盛り／丸め誤差／視覚的な手がかり／薄い半透明／
手作業／強い変化・控えめな変化）。いずれも一般的な言い回しの組み合わせ
に見え、明確な造語（このリポジトリだけの呼び名）とは見えなかったが、
「由来がずれる」だけは独自に圧縮した言い方に見え、判断できない。
決めるのは main。

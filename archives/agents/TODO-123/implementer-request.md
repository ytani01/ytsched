# TODO-123 実装依頼

## 目的

検索画面のフッターの ＜ ＞ をダブルタップすると、検索の基準日を 1 週間ずつ
自動で移動できるようにする。

## 対象

- `src/ytsched/webroot/static/js/main-page.js`
- `tests/test_browser.py`
- 必要なら、この機能に直接関係する JavaScript ファイルだけ

## 完了条件

- 検索画面で同じ方向の ＜ または ＞ をダブルタップすると、直後の 2 回の移動後も
  自動送りが継続する
- 検索結果の再読み込みをまたいでも、検索基準日を 7 日ずつ動かす
- 間隔は既存の `AutoTurnMsec` を使う
- 検索画面では週枠のアニメーションを出さない
- 同じボタンまたは画面上の別の場所を押すと自動送りを止める
- 前後の自動送りと停止をブラウザテストで確認する

## 制約

- 通常の週表示の自動ページ送りの挙動を変えない
- TODO-116/117 で検索画面の単発移動に使っている `moveActiveDate()` を
  基礎にする
- TODO.md は編集しない。コミットしない

## 確認

変更後に対象のブラウザテストと、必要な lint を実行すること。

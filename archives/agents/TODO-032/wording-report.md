# TODO-032. wording 報告

対象: `git diff TODO.md`（新設した `## TODO-032. \`Conf.cgi\` を JSON 形式にする`
の節、18〜46 行目あたり）。まだステージ・コミットされていない差分を対象にした
（依頼にその旨の指定があったため）。

`HEAD` の `*.md` 全体を基準に `git grep -cF` で前例を数えた。

## 前例の無い語（件数 0）

- **後方互換**
  - 箇所: 「移行のコードは `migrate.py` に集め、`handler.py` に**後方互換**を残さない」（21〜22 行目付近）
  - `git grep -cF 後方互換 HEAD -- '*.md'`: 0 件（「後方互換性」も 0 件）
  - 見立て: ソフトウェア開発では一般的な専門用語（backward compatibility の定訳）。
    このリポジトリでは初出というだけで、造語ではなさそう

- **名残**
  - 箇所: 「`.cgi` は Perl CGI 時代の**名残**で、中身とも合わない」（41 行目）
  - `git grep -cF 名残 HEAD -- '*.md'`: 0 件
  - 見立て: 普通の日本語（「昔の名残」）。問題なさそう

- **手が入る**
  - 箇所: 「数値を `int` で持つと、不正な値を保存しない扱い（TODO-027）まで**手が入る**」（46 行目）
  - `git grep -cF 手が入る HEAD -- '*.md'`: 0 件
  - 見立て: 一般的な言い回し（「〜まで手を入れる／手が入る」＝影響が及ぶ）。
    ただし「範囲が広がる」「影響が及ぶ」のような、より直接的な言い方もできる。
    このリポジトリだけの言い換えではなさそうだが、少しくだけた表現ではある

- **範囲を絞る**
  - 箇所: 「形式の変更だけに**範囲を絞る**」（44 行目）
  - `git grep -cF 範囲を絞る HEAD -- '*.md'`: 0 件
  - 見立て: 一般的な言い回し。問題なさそう

- **TOML 化**
  - 箇所: 「あのときの主題は **TOML 化**で」（32 行目）
  - `git grep -cF 'TOML化' HEAD -- '*.md'`: 0 件（`TOML` 単独は 6 件あり、TODO-011 由来）
  - 見立て: 「〜化」は一般的な接尾辞で、`TOML` 自体は既に使われている語なので
    造語ではなさそう

## 前例はあるが件数が少なめの語（参考、造語ではなさそう）

- 対応しない: 3 件
- 入口（を揃える）: 2 件
- 利用者の判断: 8 件
- 自前のパーサ: 1 件（「自前」単独は 8 件）

これらは一般的な日本語・既出語の組み合わせで、問題は無さそうと見た。

## 読んだファイル

- `TODO.md`（差分部分。`## TODO-032.` の節）

## 前例の無い語の数

**5 語**（後方互換、名残、手が入る、範囲を絞る、TOML化）。

いずれも一般に通用する語・言い回しに見え、TODO-021 の「足場」のような
リポジトリ独自の言い換えには見えなかった。最終判断は main に委ねる。

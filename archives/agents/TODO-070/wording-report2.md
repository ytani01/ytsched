# TODO-070 wording 報告2

対象は次の 4 つ（すべて未コミットの作業ツリー）。

- `archives/todo/TODO-070. 緑色の点滅と、予定追加のプラスアイコンを廃止.md`（新規）
- `archives/agents/TODO-070/README.md`（新規）
- `archives/agents/TODO-070/verifier-report.md`（新規）
- `TODO.md`（節を完了済みへ移した差分）

前例の件数が少ない順に並べる。

## スケジュール追加ボタン

- 出てくる箇所: `TODO-070.md`「やったこと」の「プラスアイコン」節、
  `main.html` — 各日の下の「スケジュール追加ボタン」の `div` を削除
- `git grep` の件数: 前例なし
- 見立て: `main.html` 内のコメントや変数名から取った引用と思われ、
  文脈からは意味が通る。ただし「プラスアイコン」（本文で使っている呼び名）
  との使い分けが無く、初出。造語というより単発の引用の可能性がある

## 検証用予定

- 出てくる箇所: `verifier-report.md`「3. アプリの起動確認」——
  `title=検証用予定` として POST した予定のタイトル
- `git grep` の件数: 前例なし（「検証用」単独では 1 件）
- 見立て: テストデータのタイトル文字列であり、呼び名として定着させる
  語ではなさそう。問題は無いと見るが、判断は main に委ねる

## フォントスタック

- 出てくる箇所: `verifier-report.md`「2. 消し残しの grep」——
  「`my.css:25` の `BlinkMacSystemFont`（フォントスタックの一部、無関係）」
- `git grep` の件数: 前例なし
- 見立て: CSS の一般的な専門用語（`font-family` に複数のフォント名を
  並べたもの）。一般には通用しそうだが、このリポジトリでは初出

## 壊れの有無

- 出てくる箇所: `verifier-report.md` の見出し「4. 壊れの有無」
- `git grep` の件数: 前例なし（「壊れ」単独では 246 件、汎用語として
  既によく使われている）
- 見立て: 「壊れ」自体はこのリポジトリで定着した言い回しだが、
  「壊れの有無」という見出しの形は初出。造語というほどではなく、
  既存語の組み合わせに見える

## 直接やり／確認だけ分けた

- 出てくる箇所: `README.md`「そのため**実装は main が直接やり、
  確認だけ分けた**」
- `git grep` の件数: いずれも前例なし
- 見立て: 分担の説明として自然な日本語で、呼び名にはなっていない
  （名詞化された用語ではなく動詞句）。造語には見えない

## 消す場所

- 出てくる箇所: `TODO-070.md`「きっかけ」——
  「どちらも消すだけで、触る場所も重ならないので」
  （正確には「触る場所」。近い形の「消す場所」は README にも同義で
  1 件前例がある）
- `git grep` の件数（「消す場所」）: 1 件
- 見立て: 前例が 1 件あり、既存の言い回しに寄せている。問題は無さそう

## 点滅させる

- 出てくる箇所: `TODO-070.md`「やったこと」——
  「点滅させる行を伝えるためだけに動いていた `modified_sde_id`」
- `git grep` の件数: 1 件
- 見立て: 「点滅」自体は前例 5 件で定着した語。動詞化した形も
  ほぼ問題無さそう

## 読んだファイル

- `archives/todo/TODO-070. 緑色の点滅と、予定追加のプラスアイコンを廃止.md`
- `archives/agents/TODO-070/README.md`
- `archives/agents/TODO-070/verifier-report.md`
- `TODO.md`（差分）

## 前例の無い語数

4 語（スケジュール追加ボタン、検証用予定、フォントスタック、壊れの有無）。
残り 2 語（消す場所・点滅させる）は前例 1 件で、ほぼ既存の言い回し。

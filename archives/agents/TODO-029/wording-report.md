# TODO-029 wording 報告

対象: `git diff -- TODO.md` の差分（新設した「## TODO-029. コードレビューで
見つかった 3 件を直す」の節）。基準は `HEAD`。

## 前例なしの語

### グループ記号

- 出てくる箇所: 「全角括弧は半角になるので、正規表現のグループ記号として
  解釈される。」
- `git grep -cF` の件数: 0 件（前例なし）
- 見立て: 正規表現の丸括弧 `()` を指す一般語としては「グループ」
  「キャプチャグループ」のほうが通用しそう。「グループ記号」という
  言い方自体が一般的かどうかは判断できない

### 手当て

- 出てくる箇所: 「実データを移行済みなら、手当てが要るかを着手時に
  確かめる」
- `git grep -cF` の件数: 0 件（前例なし）
- 見立て: 一般的な日本語（対応・処置の意味）で、このリポジトリ限定の
  言い換えには見えない。問題なさそう

## 前例ありだが件数が少なく、念のため挙げる語

### コードレビュー

- 出てくる箇所: 「TODO-021 までの変更（`src/` 全体）を `/code-review` に
  かけて出た 3 件」の見出しと本文
- `git grep -cF` の件数: 1 件
- 見立て: 一般的な IT 用語（`CLAUDE.md` にも「コードレビュー」の指示が
  ある）。造語ではない

## 前例が十分にあり、問題なさそうな語（参考）

以下は `git grep -cF` で複数件ヒットし、既に定着している語だったので
候補から外した: ゴールデンマスターテスト(21)、空振り(8)、重複(20)、
食い違う(15)・食い違い(20)、正規化(25)、リテラル(5)、実測(60)、
旧形式(53)、CRLF(4)、テキストモード(3)、着手時(16)、行末(9)、
全角括弧(54)、半角(29)、`split_lines`(21)、`load_line`(11)、
`orig_date`(7)、`cmd_del`(3)、`cmd_add`(25)、`search_str`(115)、
`normalize()`(10)。

## 読んだファイル

- `TODO.md`（`git diff` の差分部分のみ、TODO-029 の節）

## まとめ

前例の無い語: **2 語**（グループ記号、手当て）。

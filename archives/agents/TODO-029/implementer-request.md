# TODO-029 implementer への依頼

項目そのものは `TODO.md` の「TODO-029. コードレビューで見つかった 3 件を
直す」。**背景と「決めたこと」がそちらに詳しく書いてあるので、先に読む
こと。** ここには要点だけ書く。

なお、実データ（`~/ytsched/data`）は存在しないことを main で確認済み。
「すでに移行して `\r` が入ってしまったデータの手当て」は要らない。

## 1. 移行のときに行末の `\r` を落とす

`ytsched migrate` が CRLF の旧 `.cgi` を変換すると、最後のフィールド
（`detail`）の末尾に `\r` が残ったまま `.jsonl` に入る。旧形式では
テキストモードの `readlines()` と `text2htmlstr()` の
`replace("\r", "")` で消えていたので、移行で新しく入る不具合。

- 直すのは**移行の側**（`src/ytsched/migrate.py`）。
  `SchedDataFile.split_lines()` は JSON Lines の読み込みでも使うので、
  そちらの意味を変えないこと
- 旧コードは `\r` を**全部**消していた（行末だけではない）。同じにする
- CRLF のテストデータで、`detail` の末尾に `\r` が残らないことを
  確かめるテストを足す

## 2. 編集画面の `orig_date` を、その行が入っているファイルの日付にする

`load_line()` は、行の `date` がファイル名から決まる日付と違っても、
警告を出して行側の日付を残す。すると `edit.html` が `orig_date` に
その日付を出すので、`fix` したときに `cmd_del()` が別の日のファイルを
見に行って空振りし、`cmd_add()` が表示していた日のファイルへ書く。
結果は重複。

- **読み込みの方針（行の `date` を信じる）は変えない。**
  編集画面が渡す `orig_date` だけを「その行が実際に入っているファイルの
  日付」にする
- いま `edit.html` は `{% set orig_date = sde.date %}` としている。
  `sde.date` ではなく、その `sde` を取り出した `SchedDataFile` の日付を
  渡す。描画しているのは `EditHandler.get()` と
  `MainHandler.exec_cmd()`（`cmd=update`）の 2 か所。
  `sde.html` は既に `sched_date` を使っていて正しい
- 表示上の日付との食い違いはそのまま残してよい（TODO に書いたとおり）
- `date` が食い違う行を含むデータで、`fix` しても重複しないことを
  確かめるテストを足す

## 3. 検索・フィルタ文字列を `normalize()` に通す

照合される側の `SchedDataEnt.search_str()` は `normalize()`（全角括弧を
半角にして小文字化）を通るのに、入力側は `.lower()` しかしていない。
`会議（重要）` を `（重要）` で検索しても当たらない。

- **入力側も `normalize()` に通す。** `search_str` と `filter_str` の
  両方。TODO-028 で `filter_str` の保存が `.lower()` になっているので、
  そこも `normalize()` へ差し替える（保存する値も `normalize()` 後）
- 全角括弧は半角になるので、正規表現のキャプチャグループとして解釈され、
  `（重要）` は `(重要)` として当たる。全角括弧をリテラルとして書きたい
  ときは `\(` と打つ必要がある。**この挙動でよいと決めてある**
- `docs/data-format.md` の当該の段落（「これは旧形式でも同じだった」と
  書いてあるところ）も、今の挙動に合わせて書き直す

## テスト

- 2・3 は挙動が変わるので、TODO-021 で足したゴールデンマスターテストが
  落ちるかもしれない。**落ちたら書き直してよい**
- 3 件それぞれについてテストを足す

## 決まりごと

- `mise run fmt` / `typecheck` / `lint` / `test` は叩いてよい。
  **`mise run upgradeproject` は走らせない**
- アプリを起動して確かめるときは `--datadir` に一時ディレクトリを指定する
- 終わったら報告を `archives/agents/TODO-029/implementer-report.md` に
  書く。返事は「終わったか・報告のパス・判断が要る点」の 5 行以内

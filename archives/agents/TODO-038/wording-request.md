# TODO-038 wording への依頼

TODO-038（HTML・CSS のリファクタリング）のコミットに入る `.md` から、
**このリポジトリに前例の無い語**を挙げてほしい。

## 対象

このコミットに入る `.md` 全部。

- `TODO.md`
- `archives/todo/TODO-038. HTML・CSS のリファクタリング.md`
- `archives/agents/TODO-038/` の `.md` 全部（依頼書・報告・README・
  main の覚書）。**担当の報告ファイルも対象**

`git status --short` と `git diff` で、実際に入るものを確かめてから始めること。

## 前例があるかの調べ方

`git log` の履歴と、リポジトリにある既存の `.md`（`README.md`
`CLAUDE.md` `docs/` `src/README.md` `tests/README.md` `archives/todo/` の
既存分）を突き合わせる。TODO-038 で新しく書いたファイル自身は
「前例」に数えない。

## 出してほしいもの

- **候補を十数語に絞る。** 全部挙げると読めない
- 各語に、どのファイルの何行目で使っているかと、**見立て**
  （造語だと思う／一般に通用する専門用語だがこのリポジトリでは初出、など）
- 言い換えの案があれば添える

**決めるのは main。** 直さないこと。

今回とくに気になっているのは、CSS・ブラウザまわりの語
（画素単位の比較、揺らぎ、入れ子、打ち消す、など）と、検証の手順に
ついての語。一般語のつもりで書いたものが、このリポジトリでは初出に
なっているかもしれない。

## 決まりごと

- **文書は直さない。** 挙げるだけ
- **`TODO.md` は編集しない。git commit もしない**
- 報告は
  `/home/ytani/work/ytsched/archives/agents/TODO-038/wording-report.md`
  に**絶対パスで**書き、返事は 5 行以内で

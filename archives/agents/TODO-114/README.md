# TODO-114 の分担

| 担当 | 何を任せたか |
| --- | --- |
| main | 実装（`main.html` / `main-page.js` / `nav.js` / `main_binder.py` / `sched_load.py`）を読んで仕様を確かめ、`docs/User.md` を書く |
| [verifier](verifier-report.md) | 書いた文書と実装の照合 |

## この分担にした理由

- 文書 1 本の新設で、コードは変えない。実装の担当は分けず main が書いた
- 一方、**書いた本人は「読んだとおりに書いたはず」で済ませてしまう**。
  実際、下書きには「ホームボタンのダブルタップで検索が解除される」という
  誤りがあった（GET なので検索文字列は残る）。確認の担当は分けた
- `wording` は立てていない。利用者が明示して依頼した場合だけ使う決まり
  （`CLAUDE.md`）

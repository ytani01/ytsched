# TODO-023 の分担

見込み: main = Opus 5 / effort high、担当 = verifier + wording

## なぜこの分担にしたか

変えたのは `mise.toml` 1 ファイルだけで、アプリのコードには触れていない。
それでも確認を分けたのは、**書式が揃っているかではなく「タスクが実際に
走るか」を試せる**から（TODO-017 の基準）。実装した本人は、自分が叩いた
順番でしか試さない。

reviewer は入れていない。アプリの分岐も条件式も変わらないため。

実装は main が直接やった。1 ファイルで、決めることは着手前に利用者と
決め終わっていたため。

- `verifier-report.md` — mise のタスクを独立に走らせた結果
- `wording-report.md` — このコミットに入る `.md` の、前例の無い語

# TODO-073 wording 報告

対象: `TODO.md` の差分（TODO-073 節）のみ。

## 前例の無い語

### クレジット表示
- 出てくる箇所: 見出し「TODO-073. クレジット表示を「(c) 2026 ytani01」に統一する」
- `git grep -cF` の件数: 0（前例なし）
- 見立て: 「クレジット」は一般的な IT 用語として通用するが、この
  リポジトリでは初出。「著作権表示」（後述、こちらは前例 3 件あり）
  との使い分けが気になる。同じ節の本文では「著作権表示」の語は
  使わず、見出しと本文の両方で「クレジット表示」を使っている。
  一般的には通じるが、リポジトリ内の言い換えの揺れではないか判断が
  つかない

### ハンドル名
- 出てくる箇所: 「年を 2026 に、名前をハンドル名の `ytani01` に変える」
- `git grep -cF` の件数: 0（前例なし）
- 見立て: 一般に通用する語（オンラインでの呼び名）。造語ではなく、
  単にこのリポジトリで使うのが初めてというだけに見える

### 機械的な置換
- 出てくる箇所: 「機械的な置換なので implementer は分けず main で実施する」
- `git grep -cF` の件数: 0（前例なし）
- 見立て: 一般的な言い回し。問題なさそうだが、判断は main に委ねる

### 著者名
- 出てくる箇所: 「`pyproject.toml` の著者名を `ytani01` にする」
- `git grep -cF` の件数: 0（前例なし）
- 見立て: ごく普通の日本語。問題ないと思われる

## 参考（前例あり、造語の疑いは薄い）

- 著作権表示: 3 件（前例あり）
- フッター: 20 件
- 期待値: 10 件
- 追随: 36 件
- 固定: 96 件
- 揃える: 86 件

## 読んだファイル

- `TODO.md`（差分部分のみ、`git diff TODO.md` で確認）

## 前例の無い語数

4 語（クレジット表示・ハンドル名・機械的な置換・著者名）

## 決着時の確認

対象: 決着のコミットに入る 4 つの `.md`。

- `archives/todo/TODO-073. クレジット表示を「(c) 2026 ytani01」に統一する.md`（新規）
- `archives/agents/TODO-073/README.md`（新規）
- `archives/agents/TODO-073/verifier-report.md`（新規）
- `TODO.md`（差分。目次への 1 行追加と件数更新のみ）

`git grep` は `HEAD`（コミット `90310bd`。この時点で本報告の「立てる」時の
節はすでに含まれている）に対して行った。

### 前例の無い語

#### 一括置換
- 箇所: `archives/todo/TODO-073...md`「`Yoichi Tanibayashi` を含む 28
  ファイルを一括で置換した」「最初に走らせた正規表現は」の節の見出し
  「一括置換が説明文まで書き換えた」
- `git grep -cF` の件数: 0（前例なし）
- 見立て: 一般的な言い回し。造語というより単語の組み合わせで、
  問題は薄いと思う

#### 危なさ
- 箇所: `README.md`「一括置換に特有の危なさがあった」
- `git grep -cF` の件数: 0（前例なし）
- 見立て: 普通の日本語（「危うさ」の口語的な言い方）。一般には通じるが、
  もう少し硬い言い方（「危うさ」「リスク」）に寄せるかは判断できない

#### 誤爆
- 箇所: `verifier-report.md`「著作権表示ではない。誤爆なし」
- `git grep -cF` の件数: 0（前例なし）
- 見立て: IT の現場でよく使うスラング的表現。一般には通じるが、
  この文書のほかの箇所は「意図しない置換」のような言い方をしており、
  同じ意味を指す語が 2 通りある。言い換えの揺れに見える

#### 混入
- 箇所: `verifier-report.md`「Bootstrap の著作権表示や文書の説明文への
  混入なし」「他者の著作権表示への混入は無し」
- `git grep -cF` の件数: 0（前例なし）
- 見立て: 一般的な語で問題は薄いと思うが、判断は main に委ねる

#### 総括
- 箇所: `verifier-report.md` の見出し「## 総括」
- `git grep -cF` の件数: 0（前例なし）
- 見立て: 一般的な語。他の verifier 報告がこの位置をどう見出しにしているか
  次第では言い換えの揺れになりうるが、単体では問題なさそうに見える

### 参考（前例あり、造語の疑いは薄い）

- クレジット表示: 5 件（前回の「立てる」節を含む）
- ハンドル名: 4 件（同上）
- 巻き添え: 5 件（TODO-069 の wording 報告などに前例あり）
- 巻き込んで: 2 件
- 生残り: 28 件（verifier 報告で定番の言い回し）
- 分担の理由: 34 件
- 妥当・揃える・食い違う・先頭コメント: いずれも前例多数

## 読んだファイル（決着時）

- `archives/todo/TODO-073. クレジット表示を「(c) 2026 ytani01」に統一する.md`
- `archives/agents/TODO-073/README.md`
- `archives/agents/TODO-073/verifier-report.md`
- `TODO.md`（`git diff HEAD -- TODO.md` で差分のみ）

## 決着時の前例の無い語数

5 語（一括置換・危なさ・誤爆・混入・総括）

# TODO-033 wording 報告

対象: `git diff TODO.md`（TODO-032・TODO-033 の 2 節を追加した差分）。

基準は `HEAD`（この差分がまだ入っていない状態）。

## 前例の無い語（件数の少ない順）

### 改良案

- 出てくる箇所: 「TODO-032. 改良案」（見出し）、「今、考えられる改良案を
  まとめる」「改良案\n- Conf.cgi を JSON 形式にする。」
- `git grep -cF` 件数: 前例なし
- 見立て: 一般語ではあるが、この語自体を単独の見出し・節タイトルとして
  使うのはこのリポジトリで初出。「改善案」「変更案」など類義語もこれまで
  出ていない。造語というより一般語だが、初出という点は事実として挙げる。

### 全滅

- 出てくる箇所: 「`pytest` がコレクション段階で全滅する（`test_handler.py`
  / `test_main_handler.py` / `test_web.py` / `test_ytsched.py` の 4 つ）」
- `git grep -cF` 件数: 前例なし
- 見立て: 「全滅する」はテスト用語として口語的だが業界でも比較的よく
  使われる比喩。判断できない（一般に通用しそうだが、このリポジトリでは
  初めての言い回し）。

### コレクション段階

- 出てくる箇所: 「`pytest` がコレクション段階で全滅する」
- `git grep -cF` 件数: 前例なし
- 見立て: pytest 用語「collection」のカタカナ表記＋「段階」の組み合わせ。
  pytest の公式用語（test collection）を踏まえた言い方に見えるが、
  この組み合わせ自体はこのリポジトリで初出。一般に通用するかは
  判断できない。

## 前例はあるが参考として記載する語（初出ではないが近い言い回し）

- **改名**: 「`URL_PREFIX` の改名に追随できていない箇所を直す」
  （TODO-033 の見出し）。`git grep` では 2 件ヒット（`archives/agents/
  TODO-014/verifier-report.md` に 1 件、`archives/todo/TODO-014. …名.md`
  に 2 件）が、いずれも「報告ファイル名」の文脈での「改名」で、
  今回のような「識別子（`URL_PREFIX`）の改名」という使い方とは対象が違う。
  参考までに挙げるが、前例ありのため「前例の無い語」には含めない。
- **追随**: 「`URL_PREFIX` の改名に追随できていない箇所」。`git grep` で
  2 件ヒット（`archives/agents/TODO-003/report-reviewer.md`、
  `archives/agents/TODO-007/README.md`）。前例ありのため含めない。

## 前例があり、問題なさそうな語（参考）

- 気をつけること: TODO.md 自身に既に 4 件、archives にも複数件あり定着した見出し。
- 直に参照: 前例 1 件（`archives/todo/TODO-019. …md`）あり。

## 読んだファイル

- `/home/ytani/work/ytsched/TODO.md`（`git diff TODO.md` の追加部分、
  TODO-032・TODO-033 の 2 節）

## 前例の無い語数

**3 語**（改良案・全滅・コレクション段階）。いずれも一般語または
業界慣用の比喩・専門用語の組み合わせで、このリポジトリでは初出という
だけであり、造語と断定はできない。判断は main に委ねる。

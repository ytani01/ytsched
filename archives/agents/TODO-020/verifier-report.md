# verifier の報告（TODO-020）

## 1. lint / test

- `mise run lint`: ○ ruff / basedpyright / mypy とも 0 件
- `uv run pytest tests`: ○ **273 passed**（implementer の報告と一致）

```
uv run pytest tests
```

## 2. 移行ツールを実データ相当で動かす

`tests/data/old_format/` を一時ディレクトリへコピーし
`uv run ytsched migrate --datadir <一時ディレクトリ> --error-file <...>`
を実行。8 ファイル・27 行を変換、飛ばした行 0。依頼書の確認項目を
自分で jsonl を `cat` して 1 件ずつ確認した。

- **どちらでも読めない 1 バイトを含む行**（`2005/07/13.cgi` 1 行目）:
  残った。警告ログも出た。`detail` の当該箇所が
  `"資料は前日までに配布\n【欠席】�以上"`（U+FFFD）になっている。○
- **`&amp;#160;` が空白になる**（`2005/07/14.cgi`）: ○
  例: `"条件は 3 < x > 1 の範囲 \n備考なし"`
  （`&amp;#160;` の位置が半角空白に）。`&nbsp;&nbsp;` も
  `"  倉庫の鍵は総務へ"` と 2 個の空白になった
- **`28:00` が `04:00` になる**（`2012/01/09.cgi` 5 行目）: ○
  `"time_start": "04:00"`
- **全角括弧がそのまま残る**（`2021/08/01.cgi` の `（重要）健康診断の申込`）: ○
  jsonl でも `（重要）健康診断の申込` のまま
- **U+2028 を含む 1 件が 2 行に割れない**（`2021/08/02.cgi` 3 行目）: ○
  jsonl は 3 行のまま（元の生データを `split(b'\n')` で数えても 4 要素
  = 4 行で一致）。該当行の `detail` を Python で読むと
  `'前半のまとめ 後半のまとめ\n以上'` と 1 件のまま入っている
- **7 列未満・8 列以上の行**（`2021/08/04.cgi`）: ○
  6 列の行は `place` が足りない分空文字で埋まり（`detail` 側に
  値が来ていた点は元データの列構成どおり）、8 列の行は
  `"detail": "本文にタブが入っている\t8 個目から先は detail の続き"`
  とタブでつなぎ直され、捨てられていない
- **対象外の 3 ファイル**（`2021/08/01-backup.cgi`、`2021/08/01.cgi.bak`、
  `iappli_log.cgi`）: ○ `.jsonl` が作られていないことを確認
- **元の `.cgi` が消えていない**: ○ 変換後も全 `.cgi` が残っていた

## 3. `--dry-run`

○ 何も書かない。`--dry-run` 実行前後で `find` の一覧が完全に同一
（`.jsonl` が 1 つも増えていない）ことを確認した。

## 4. 変換したディレクトリでアプリを起動して動作確認

`uv run ytsched webapp --datadir <一時ディレクトリ> --port 10185` で起動。

- 一覧: `GET /ytsched/?date=2021-08-01` → **HTTP 200**。
  `{{ }}` `{% %}` が生で残っていないことを確認（grep 0 件）
- サーバのログに例外・トレースバックは出ていない
- **追加**: `cmd=add` で `sde_id=`（空文字）を渡す必要があった
  （`main_handler.py` の `exec_update()` が `get_argument("sde_id")` を
  必須として読んでいる。省略すると **400 `Missing argument sde_id`**
  になる。これは今回の実装のバグではなく元々の仕様だが、
  **フォームからでない直接 POST では sde_id を明示的に空で送る必要が
  ある**ことをここに記録しておく）。`title` に
  全角括弧・タブ・改行を含む値を送って追加 → 200、jsonl に
  `"title": "（重要）打合せ", "detail": "line1\nline2\ttabhere"` と
  タブ・改行がそのまま JSON エスケープで保存された
- **修正**（`cmd=fix`）、**削除**（`cmd=del`）とも 200 で成功し、
  jsonl の中身が期待どおり書き換わった。削除時は `.jsonl.bak` が
  作られた（`.bak` の仕組みは維持）
- 保存されたファイルはすべて JSON Lines（1 行 1 JSON）になっている

## 5. `（重要）打合せ` の判定

- 追加直後のレスポンス HTML で `font-weight: bold` になって表示 → ○
- サーバを一度完全に停止し（`pgrep` で PID を確認してから kill）、
  新しいプロセスで**キャッシュを介さず**再読み込みしても同じく
  bold で表示 → ○
- 保存された文字列は `（重要）打合せ` のまま（全角括弧）で、
  半角に書き換わっていない → ○

## 6. 単独で決めた判断の確認

- **判断 2（既に `.jsonl` があるファイルは飛ばす）**: ○
  1 回目の変換後、`2021/08/01.jsonl` を見張り用の別内容に差し替えて
  2 回目を実行 → 8 ファイルすべて `already exists .. skipped` の警告、
  変換 0 件。差し替えた内容がそのまま残り、上書きされなかった
- **判断 8（末尾の改行 1 つでは空行の警告を出さない）**: ○
  `SchedDataFile.split_lines()` を直接呼んで確認
  （`b"a\n"` → `[b"a"]`、`b"a\n\n"` → `[b"a", b""]`）。
  実際に `load()` でも、1 行 + 末尾改行 1 つのファイルは警告なしで
  読め、中間に空行を仕込んだファイルは
  `2021/01/02.jsonl:2: empty line .. ignored` の警告が出た

## 見つけたこと（判断が要る点）

- 上記「追加」の項目で触れた **`sde_id` を空文字で明示しないと 400 に
  なる**挙動は、今回の変更で入ったものではなく、以前からの
  `main_handler.py` の仕様（`get_argument("sde_id")` に既定値が無い）。
  今回の依頼の範囲外と考えて実害としては報告しない（既存の Web フォーム
  は隠しフィールドで空文字を送っているはずで、実際にブラウザから
  使う分には問題ない）。念のため記録だけ残す
- それ以外、不具合は見つからなかった

## 後片付け

- `pgrep -fa "ytsched webapp"` で PID を確認してから `kill` で停止。
  最終確認で `ytsched webapp` プロセスが残っていないことを確認済み
- 実データディレクトリ `~/ytsched/data` には最初から存在せず、
  今回の作業でも触れていない
- 一時ディレクトリ（scratchpad 配下）は自分が作ったものをすべて削除した

## 直したあとの再確認（2026-08-21）

reviewer の指摘を受けて implementer が直した 5 点を再確認した。
コードは直していない。

### 1. lint / test

- `mise run lint`: ○ ruff / basedpyright / mypy とも 0 件
- `uv run pytest tests`: ○ **289 passed**（想定どおり。直す前は 273）

### 2. 壊れた行の書き戻しが実際に効くこと

空行・utf-8 でデコードできない行（euc_jp の 1 バイト `\xa4\xa2`）・
JSON として読めない行（`{ broken`）・オブジェクトでない行
（`[1,2,3]`）・`date` の無い行、の 5 種類を仕込んだ `.jsonl` を用意し、
アプリ（`--datadir` に一時ディレクトリ、`--port 10185`）から `cmd=add`
で保存を **3 回**繰り返した。

- 1 回目保存後: `.bak` が元ファイルとバイト完全一致
  （Python で `bak == orig` を比較し `True`）。本体にも 5 行とも
  元のバイトのまま残っていた
- 2 回目保存後: `.bak`（2 回目）が「1 回目保存後の本体」とバイト
  完全一致。5 種類の壊れた行はそれぞれ本体・`.bak` に **1 回ずつ**
  しか現れず、増殖していない
- 3 回目保存後: 本体の非空行数は 9
  （予定 5 件 + 壊れた行のうち非空の 4 行）で、2 回目から増えて
  いない。壊れた行の出現回数もそれぞれ 1 のまま
- サーバのログに、5 種類それぞれについて
  `load_line()> .../05.jsonl:N: ...` の警告が**保存後の再読み込みの
  たびに**出続けることを確認した（書き戻した行がまた飛ばされる）

### 3. 通常データが壊れていないこと

タブ・改行・U+2028・全角括弧（`（重要）打合せ`）を含む `detail` /
`title` を `cmd=add` で追加。保存後の `.jsonl` を utf-8 で読むと
`detail` は `'line1\tsomeTAB\nline2 line3'` として 1 件のまま
正しく入っており、ファイルの行数（`\n` の数）は 2（＝件数と同じ、
末尾に 1 つだけ）で余計な行は増えていない。
`.bak` の仕組み: 中身のあるファイルを保存すると `.bak` が元の内容と
一致して作られ、**空ファイル**（0 バイト）に対して保存すると
`.bak` は作られないことを確認した（変わっていない）。

### 4. 書き戻した行が次の読み込みでまた飛ばされ、無限に増えないこと

上の「2. 」で 3 回保存して確認済み（行数が増えない、出現回数が
1 のまま）。

### 5. 壊れた行が無いファイルで余計なものが書かれない

「3. 」の追加前の元ファイル（正常な予定 1 件、末尾改行 1 つ）を
保存した結果、`\n` の数は件数どおりで、空行や重複行は増えていない。

### 6. 移行ツール: 対象 0 件の警告 / `migrate-errors.txt`

- 空の一時ディレクトリに対して `ytsched migrate` を実行すると
  `no target file .. check --datadir` の警告が出た（変換 0 件）
- `uv run pytest tests` の実行後、リポジトリ直下に
  `migrate-errors.txt` が無いことを確認した
  （`git status --short` にも現れない）

### 7. 変換したディレクトリでアプリの一覧・追加・修正・削除

`tests/data/old_format/` を一時ディレクトリへコピーして
`ytsched migrate` で変換（8 ファイル・27 行、飛ばした行 0、内容は
前回確認と同じ）。`--port 10185` でアプリを起動し、
一覧（200）・追加（200）・修正（200）・削除（200）がいずれも成功し、
`.jsonl` の中身も期待どおり書き換わった。サーバのログに例外・
トレースバックは出ていない。

### 見つけたこと

- 不具合は見つからなかった。前回報告した「`sde_id` を空文字で
  明示しないと 400 になる」挙動（既存仕様、今回の変更と無関係）を
  除き、判断が要る新たな点も無い

### 後片付け

- `pgrep -fa "ytsched webapp"` で PID を確認してから個別に `kill`。
  途中、`pgrep` が複数行を返したのを 1 つの変数に入れて `kill` した
  結果 `illegal pid` になり、実際にはプロセスが残っていたことが
  2 度あった（`kill $PID` に複数行がそのまま渡っていた）。
  **`pgrep` の結果が複数行になっていないか都度確認し、1 行ずつ
  `kill` する必要がある**（本作業では見つけて都度個別の PID で
  `kill` し直し、最終的に `ytsched webapp` プロセスが残っていない
  ことを確認した）
- 一時ディレクトリ・生成した一時ファイルはすべて削除した。
  実データディレクトリ `~/ytsched/data` には触れていない

## 空行の対応の確認（2026-08-21）

reviewer の指摘 B（空行は書き戻さない）と、全行が壊れているファイルの
テスト追加を再確認した。コードは直していない。

### 1. lint / test

- `mise run lint`: ○ ruff / basedpyright / mypy とも 0 件
- `uv run pytest tests`: ○ **290 passed**（想定どおり）

### 2. 空行が消えること・再読み込みで警告が出なくなること

「正常な予定 1 件 → 空行 → 空白だけの行(`'   '`) → euc_jp の 1 バイト
→ `{ broken` → `[1,2,3]` → `date` の無い行 → 正常な予定 1 件」の
7 行を仕込んだ `.jsonl` に対し、アプリ（`--port 10185`）を起動して
`cmd=add` で保存した。

- 1 回目の読み込み（起動直後の一覧表示）では、空行・空白だけの行を
  含む 6 種類すべてで `.. ignored` の警告が出た
- 保存後の本体をバイト列で確認すると、空行と空白だけの行
  （`b''`、`b'   '`）はどちらも**消えていた**
  （`split(b'\n')` した結果に含まれないことを確認）
- **キャッシュ越しの確認では不十分**と判断し、サーバをいったん
  完全に停止して（`pgrep` → 個別 `kill`）新しいプロセスで
  ディスクから読み直させたところ、`empty line` の警告は**出ず**、
  残り 4 種類の警告だけが出た

### 3. 残る 4 種類は元のバイトのまま書き戻されること

上と同じファイルの保存後、本体末尾に
`\xa4\xa2`（euc_jp の 1 バイト）・`{ broken`・`[1,2,3]`・
`{"sde_id": "nodete", "title": "no date"}` の 4 行が**元のバイトのまま**
残っていることを確認した（`title` に日本語を含む追加分の JSON との
並びも含め、目視とバイト列比較の両方で確認）。

### 4. 末尾の改行 1 つが空行扱いにならないこと

正常な予定 1 件・末尾改行 1 つのファイルに対し、`cmd=add` を**3 回**
繰り返した。

- 3 回とも末尾は `}\n`（改行 1 つ）のままで、`\n\n` にはならなかった
- 非空行数は 4（元の 1 件 + 追加 3 件）で、保存のたびに 1 ずつ
  正しく増えている（縮んだり、余分な空行が増えたりしない）
- ログに `empty line` の警告は一度も出なかった

### 5. 全行が壊れているファイル

`[1, 2]\n{ x\n` の 2 行だけ（正常な行が 0 件）のファイルに対し、
アプリから `cmd=add` で保存を**2 回**繰り返した。

- 1 回目保存後: `.bak` が元ファイルとバイト完全一致。本体は
  「追加 1 件 + `[1, 2]` + `{ x`」の 3 行
- 2 回目保存後: `.bak`（2 回目）が「1 回目保存後の本体」と
  バイト完全一致。本体は非空行 4 行（追加 2 件 + 壊れた 2 行）で、
  `[1, 2]` と `{ x` はそれぞれ**1 回ずつ**しか現れず、二重に
  書かれていなかった

### 6. アプリの一覧・追加・修正・削除

`tests/data/old_format/` を一時ディレクトリへコピーして
`ytsched migrate` で変換（8 ファイル・27 行、飛ばした行 0、内容は
これまでの確認と同じ）。`--port 10185` でアプリを起動し、
一覧（200）・追加（200）・修正（200）・削除（200）がいずれも成功。
サーバのログに例外・トレースバックは出ていない。

### 見つけたこと

- 不具合は見つからなかった

### 後片付け

- 前回の指摘（`pgrep` が複数行になることがある）を踏まえ、
  今回は `kill` の前に必ず `pgrep -fa "ytsched webapp"` の行数を
  目で見て、実プロセス（`python3 .../ytsched webapp ...` の行）の
  PID だけを個別に `kill` した。`kill` のたびに `pgrep` で
  再確認し、最終的に「確認: プロセス残っていない」ことを確かめた
- 一時ディレクトリ・生成した一時ファイルは自分が作った分をすべて
  削除した。実データディレクトリ `~/ytsched/data` には触れていない

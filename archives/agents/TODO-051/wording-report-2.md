# TODO-051 wording 報告（2 回目・実施分）

## 対象

このコミットに入る `.md` 4 つ。

- `TODO.md`（`git diff TODO.md`）
- `docs/Developer.md`（`git diff docs/Developer.md`）
- `archives/agents/TODO-051/README.md`（新規）
- `archives/agents/TODO-051/verifier-report.md`（新規）

`archives/agents/TODO-051/wording-report.md` は前回（立てたとき）の報告で、
今回のコミットには入らないので対象外（参考のみ）。

## 挙げる語（前例の件数が少ない順）

### `保留にした理由`
- 箇所: `TODO.md` の小見出し「### 保留にした理由」
- `git grep` の件数: 前例なし（「保留」単独は 1 件あり、TODO-004 の
  implementer-report で使われている）
- 見立て: 「保留」＋「理由」の組み合わせで、既存語の延長に見える。
  一般的な言い回しで問題なさそう

### `ついでに決めたこと（済み）`
- 箇所: `TODO.md` の小見出し「### ついでに決めたこと（済み）」
- `git grep` の件数: 前例なし（「済み」は 202 件、「ついでに決めること」は
  前回すでに前例なしと報告済み）
- 見立て: 前回指摘した「ついでに決めること」の活用形（決めた→済み扱い）。
  既存の言い回しの自然な変化に見える

### `保留`
- 箇所: `TODO.md` の見出し「…直すのを直す（保留）」、本文中の
  「症状が再現しなかったので保留にした」など
- `git grep` の件数: 1 件（TODO-004 の implementer-report、文脈が違う
  「機能追加を保留」）
- 見立て: 一般的な日本語（「保留にする」）。このリポジトリでの初出に近いが
  造語ではなさそう

### `X の転送先`
- 箇所: `TODO.md`「X の転送先で描画が返ってこない状態だったのだと思われる」
- `git grep` の件数: 前例なし（「転送」単独は前回前例なしと報告済み、
  「転送先」の組み合わせは未確認だが同様に一般的な語の延長）
- 見立て: 「X11 転送」＋「転送先」で普通の言い回し。造語には見えない

### `位置引数`
- 箇所: `docs/Developer.md`「位置引数で変えられる」
- `git grep` の件数: 11 件
- 見立て: プログラミングの一般的な用語（positional argument の訳語）。
  このリポジトリでも既に定着している

### `健全性`
- 箇所: `verifier-report.md` の小見出し「## 4. PNG の健全性」
- `git grep` の件数: 前例なし
- 見立て: 「壊れていないことの確認」を指す一般的な言い回し（データの
  健全性）。判断できないが、一般に通用する語には見える

### `実害`
- 箇所: `verifier-report.md`「単体指定したときだけ出る差分で、実害では
  ないと考えられる」
- `git grep` の件数: 46 件
- 見立て: 既に多用されている語で問題なし

## 前例が十分にあり、報告に載せなかった語（参考）

`分担にした理由`(21)、`済み`(202)、`確認済み`(49)、`範囲外`(100)、
`見立て`(371)、`再現した`(16)、`再現しなかった`(2)、`前置き`(2、
TODO-026 の reviewer-report に「前置きが付いた」あり）、`控えて`(2、
TODO-023・027 で既出) は前例が複数（または近い文脈での使用例）があり、
この文書で作った語ではないと判断した。

## 読んだファイル

- `TODO.md`（差分部分）
- `docs/Developer.md`（差分部分）
- `archives/agents/TODO-051/README.md`（全文）
- `archives/agents/TODO-051/verifier-report.md`（全文）

## 前例の無い語の数

4 語（`保留にした理由` / `ついでに決めたこと（済み）` / `保留` /
`X の転送先`）。いずれも既存語（「保留」「ついでに決めること」「転送」）の
自然な延長・活用形で、この文書だけの新しい言い換えには見えなかった。
`健全性`（前例なし）を含めると 5 語だが、一般に通用する語に見えるため
見立てを分けて書いた。

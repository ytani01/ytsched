# TODO-051 wording 報告

## 対象

`TODO.md` の差分（`git diff TODO.md`）のみ。
- 先頭の「残っている項目」「番号は」の 1 語ずつの変更
- 新設した「## TODO-051. `DISPLAY` があると画面のキャプチャが撮れないのを直す」節

このコミットに入るのは `TODO.md` だけなので、対象ファイルはこれ 1 つ。

## 挙げる語（前例の件数が少ない順）

### `swiftshader`
- 箇所: 「`--use-gl=swiftshader`」（起動オプションの一覧）
- `git grep` の件数: 前例なし
- 見立て: Chromium の software renderer の固有名詞。一般に通用する技術用語で、
  この文書で作った語ではない

### `DISPLAY`
- 箇所: 見出し「`DISPLAY` があると画面のキャプチャが撮れないのを直す」ほか
  節全体で多数
- `git grep` の件数: 前例なし
- 見立て: X Window System の環境変数名。一般に通用する用語で、造語ではない

### `X11`
- 箇所: 「環境は `DISPLAY=localhost:11.0`（ssh の X11 転送）」
- `git grep` の件数: 前例なし
- 見立て: 一般名称（X11 転送）。造語ではない

### `転送`
- 箇所: 「ssh の X11 転送」
- `git grep` の件数: 前例なし
- 見立て: 普通の日本語。「X11 転送」という定型句の一部で、単独でも問題なさそう

### `起動オプション`
- 箇所: 「chromium の起動オプションでは避けられない」
- `git grep` の件数: 前例なし
- 見立て: 普通の言い回し（「起動」＋「オプション」）。一般に通用しそう

### `割り当ててある`
- 箇所: 「一覧は `MainHandler` が `/` にも割り当ててあるので今までは
  困らなかったが」
- `git grep` の件数: 前例なし
- 見立て: 普通の動詞句。造語ではなさそう

### `ついでに決めること`
- 箇所: 小見出し「### ついでに決めること」
- `git grep` の件数: 前例なし（「ついでに」単独は 9 件、「決めること」単独は
  6 件で、両方に前例あり）
- 見立て: 既存語の組み合わせであり、新しい呼び名を作ったわけではなさそう。
  ただし念のため挙げる

### `調べたこと`
- 箇所: 小見出し「### 調べたこと」
- `git grep` の件数: 2 件
- 見立て: 普通の日本語の見出し。定着した言い回しに見える

### `決めること`
- 箇所: 小見出し「### 決めること」
- `git grep` の件数: 6 件
- 見立て: 普通の日本語の見出し。既に使われている

## 前例が十分にあり、報告に載せなかった語（参考）

`既定`(208)、`見込み`(113)、`担当`(378)、`verifier`(690)、`キャプチャ`(25)、
`タイムアウト`(4)、`headless`(31)、`chromium`(71)、`playwright`(48)、
`スクリーンショット`(40)、`症状`(13)、`確かめ方`(17)、`DEF_URL`(35)、
`urlprefix`(26)、`確かめること`(39) は前例が複数あり、この文書で作った語
ではないと判断した。

## 読んだファイル

- `TODO.md`（差分部分のみ、TODO-051 の節と先頭 2 行）

## 前例の無い語の数

7 語（`swiftshader` / `DISPLAY` / `X11` / `転送` / `起動オプション` /
`割り当ててある` / `ついでに決めること`）。ただしいずれも一般的な技術用語・
環境変数名・普通の日本語の組み合わせで、この文書だけの言い換えに見える語は
無かった。

# TODO-042 wording 報告

## 対象

`git diff TODO.md` の差分（冒頭の「残っている項目」行と、「## TODO-042.
左端のゲージの針(▶)の位置が内側にずれている。」の節）。

## 前例の無い語

### 基準線

- 箇所: 「基準線の `#gage_r_base`（`fa-grip-lines fa-xs`、幅 0.875em）も
  同じ理由で約 2.5px ずれている。」
- `git grep -cF 基準線 HEAD -- '*.md'`: 前例なし
- 見立て: 「目盛りの基準となる線」の意味で一般にも通用しそうな語。
  `#gage_r_base` という要素名の説明として自然で、このリポジトリ独自の
  言い換えには見えない。ただし初出であることは確か

### 横位置

- 箇所: 「針（▶）は `main.html` の `#gage_r`（...）で、横位置は `my.css`
  の `.my-gage-r { left: 1px; }` で決めている（縦位置だけ `my.js` が
  書き換える）。」
- `git grep -cF 横位置 HEAD -- '*.md'`: 前例なし
- 見立て: 一般的な語（CSS の `left` に対応する位置の呼び方として自然）。
  造語には見えない

### 縦位置

- 箇所: 上記と同じ文中「縦位置だけ `my.js` が書き換える」
- `git grep -cF 縦位置 HEAD -- '*.md'`: 前例なし
- 見立て: 「横位置」と対になる一般的な語。造語には見えない

## 前例はあるが参考までに触れた語

「ゲージ」「針」「既定値」「箱」「余白」「決め打ち」「実害」「幅」
「アイコン」「メニューバー」は `HEAD` の `.md` に前例があり、対象外とした。

## 読んだファイル

- `TODO.md`（`git diff` で示された差分のみ）

## まとめ

前例の無い語: **3 語**（基準線・横位置・縦位置）。いずれも一般に
通用しそうな普通の日本語に見えるが、判断は main に委ねる。

---

## 2 回目（済ませるコミット）

### 対象

- `archives/todo/TODO-042. 左端のゲージの針の位置がずれているのを直す.md`（新規、
  基準は `HEAD`＝`7a2c8f1`）
- `TODO.md`（節を archives へ移した差分。プローズの新規追加は無し。
  リンクの追加のみで、造語の観点で見るべき語は無かった）
- `archives/agents/TODO-042/verifier-report.md`
- `archives/agents/TODO-042/README.md`

verifier-report.md と README.md は、実装のコミット `7a2c8f1` で既に
`HEAD` に入っている。この 2 つを見るときの基準は、**その 2 つが
入る前**の `7a2c8f1^`（＝ `23a9500`）にした。
`archives/todo/TODO-042. ….md` を見るときの基準は `HEAD`（＝ `7a2c8f1`、
verifier-report.md・README.md を含む）にした。

### 前例の無い語

前例が少ない（0 件）順というより、全部 0 件だったので、出てくる
ファイルの順にまとめる。

#### left side bearing

- 箇所: README.md「余白（left side bearing）を持っている」、verifier-report.md
  「字面が `lsb = 65/512em ≈ 0.127em`」
- `git grep -cF "left side bearing" 7a2c8f1^ -- '*.md'`: 前例なし
- 見立て: フォント・タイポグラフィの一般的な専門用語（字体の左側の
  余白のこと）。造語ではない

#### グリフ

- 箇所: README.md「FA 5 と FA 7 ではグリフの実寸（字面の高さ）が違う」、
  verifier-report.md「グリフが `text-align: center` により中央寄せ」
- `git grep -cF グリフ 7a2c8f1^ -- '*.md'`: 前例なし
- 見立て: フォント関連の一般的な専門用語（文字の字形）。造語ではない

#### 字面（字面の幅・字面の高さ・字面の左端）

- 箇所: README.md「字面の幅が 0.5em しかない」ほか複数箇所、
  verifier-report.md「針の字面の左端が x = 0 に来ているか」ほか
- `git grep -cF 字面 7a2c8f1^ -- '*.md'`: 5 件（`archives/agents/TODO-027/reviewer-report.md`・
  `archives/agents/TODO-039/wording-report.md` など）
- 見立て: 前例はあるが、**意味が違う。** 既存の 5 件はいずれも
  「文面上・見かけ上は」という一般的な意味（「字面では範囲外」）。
  今回の使い方は「フォントの字形そのものの幅・輪郭」という、CSS・
  タイポグラフィの専門用語としての意味で、既存の用例とは別物。
  この意味では実質的に初出

#### 静的化

- 箇所: verifier-report.md「Python スクリプトで静的化」「静的化した
  HTML」など複数箇所
- `git grep -cF 静的化 7a2c8f1^ -- '*.md'`: 前例なし
- 見立て: 「動的なページを静的な HTML に落とす」の意味で、IT の
  文脈では通じそうな語（「静的サイト生成」などと同系）。この
  リポジトリでは初出だが、独自の言い換えには見えない

#### 撮り方

- 箇所: verifier-report.md「撮り方について（verifier の申し送り）」
  （見出し）、「手順・撮り方（`onloadHdr` 後半だけを止める静的化）」
- `git grep -cF 撮り方 7a2c8f1^ -- '*.md'`: 前例なし
- 見立て: 普通の日本語（スクリーンショットの撮影手順、の意味）。
  造語には見えない

#### 飛び出し量

- 箇所: verifier-report.md「右端の飛び出し量が縮んだ」「飛び出し量が
  小さい」（表の見出しにも使用）
- `git grep -cF 飛び出し量 7a2c8f1^ -- '*.md'`: 前例なし
- 見立て: 「はみ出す量」を短くした言い方で、意味は文脈から明快。
  一般語の組み合わせであり、独自すぎる造語には見えないが、
  「はみ出す量」のほうが平易ではある

#### 輝度

- 箇所: verifier-report.md「色を輝度で振り分けて求めた簡易な方法」
- `git grep -cF 輝度 7a2c8f1^ -- '*.md'`: 前例なし
- 見立て: 画像処理の一般的な専門用語（明るさの度合い）。造語ではない

#### 縦幅

- 箇所: verifier-report.md「三角の縦幅は old3 が 21 行…new3 が 23 行」
- `git grep -cF 縦幅 7a2c8f1^ -- '*.md'`: 前例なし
- 見立て: 「横幅」と対になる一般的な語（この文書では既に「横位置」
  「縦位置」を使っている）。造語には見えない

#### 構図

- 箇所: verifier-report.md「という構図そのものは old3・new3 でほぼ同じ」
  「利用者が示した『正しい』画像の構図」など
- `git grep -cF 構図 7a2c8f1^ -- '*.md'`: 前例なし
- 見立て: 一般的な語（絵・写真の配置の意味）。ここでは「▶ と ＝ の
  重なり方・位置関係」を指しており、比喩としてやや踏み込んだ使い方
  だが、通じないほどではない。判断できない

#### 帳尻（を合わせる）

- 箇所: `archives/todo/TODO-042. ….md`「`left` の値をずらして帳尻を
  合わせる案は採らなかった」
- `git grep -cF 帳尻 HEAD -- '*.md'`: 前例なし
- 見立て: 慣用句として一般的（「帳尻を合わせる」＝つじつまを合わせる）。
  造語ではない

#### 山型

- 箇所: verifier-report.md「y = 1904〜1926（山型に増減、ピークで 1 行
  28 画素）」
- `git grep -cF 山型 7a2c8f1^ -- '*.md'`: 前例なし
- 見立て: 一般的な語（グラフや分布の形の呼び方）。造語には見えない

#### 非白画素

- 箇所: verifier-report.md「各行の非白画素の左端・右端を求めた」
- `git grep -cF 非白画素 7a2c8f1^ -- '*.md'`: 前例なし
- 見立て: 「白でない画素」の意味で文脈からは明快だが、一般に通用する
  かは判断できない。造語寄りに見える（「背景以外の画素」などでも
  言い換えられそう）

#### 重なり方

- 箇所: verifier-report.md「視覚的にも重なり方は自然に見える」
  「『正しい重なり方』かの最終判断は」
- `git grep -cF 重なり方 7a2c8f1^ -- '*.md'`: 前例なし
- 見立て: 普通の日本語（「重なる」＋「〜方」）。造語には見えない

#### 上寄り

- 箇所: verifier-report.md「`＝` が三角の上寄りに乗る、という配置」
- `git grep -cF 上寄り 7a2c8f1^ -- '*.md'`: 前例なし
- 見立て: 普通の日本語。造語には見えない

#### 字面境界

- 箇所: verifier-report.md「厳密な字面境界の分離ではないので」
- `git grep -cF 字面境界 7a2c8f1^ -- '*.md'`: 前例なし
- 見立て: 「字面」と「境界」の合成語で、この報告限りの言い方に見える。
  やや専門的で、一般に通用するかは判断できない

#### 簡易な方法

- 箇所: verifier-report.md「輝度で振り分けて求めた簡易な方法」
- `git grep -cF "簡易な方法" 7a2c8f1^ -- '*.md'`: 前例なし
- 見立て: 普通の日本語の組み合わせ。造語には見えない

### 前例はあるが触れておく語

`揺らぎ`（16 件）・`申し送り`（13 件）・`実測`（83 件）・`内側`（9 件）・
`左端`（4 件）・`箱`（4 件）・`余白`（7 件）・`決め打ち`（4 件）・
`実害`（42 件）・`幅`（40 件）・`目盛り`（1 件）は、`7a2c8f1^` の
`.md` に前例があり対象外とした（`目盛り` は 1 件のみだが、既に一般語
として使われているものへの言及なので除外）。

### 読んだファイル

- `archives/todo/TODO-042. 左端のゲージの針の位置がずれているのを直す.md`
- `TODO.md`（この差分の追加部分）
- `archives/agents/TODO-042/verifier-report.md`
- `archives/agents/TODO-042/README.md`

### まとめ

前例の無い語: **16 語**（left side bearing・グリフ・字面（専門用語として
の意味）・静的化・撮り方・飛び出し量・輝度・縦幅・構図・帳尻・山型・
非白画素・重なり方・上寄り・字面境界・簡易な方法）。大半は一般的な
日本語・専門用語の組み合わせに見えるが、「字面境界」「非白画素」は
このリポジトリ限りの言い方に近い。判断は main に委ねる。

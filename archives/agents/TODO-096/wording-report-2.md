# TODO-096 wording 報告（2 回目・決着させるコミット分）

対象は次の 5 つ。基準は `HEAD`（このコミットが入る前）。

- `TODO.md`（差分。TODO-096 の節を archives へ移し、目次に足した分）
- `archives/todo/TODO-096. Android の Firefox でアイコンが黒く塗りつぶされる.md`（新規）
- `archives/agents/TODO-096/README.md`（新規）
- `archives/agents/TODO-096/verifier-request.md`（新規）
- `archives/agents/TODO-096/verifier-report.md`（新規）

`git grep` は `.md` に加えて `.svg` / `.css` も対象にした（1 回目の報告で、
`icons.svg` のコメントにある「消去キー」「漏斗」を前例なしとして
誤って挙げてしまった件を踏まえて）。

## 前置き: TODO.md の節はすでに HEAD にある

`TODO-096` を立てるコミット（`100e594`）で、`TODO.md` にはすでに
「きっかけ」相当の文章（「Android の Firefox で、フッターの ◀ ▶ が黒い
三角、虫めがねが黒い丸、消去キーが黒い矢印になる…」）が入っている。
そのため「消去キー」「漏斗」「shadow tree」「輪郭パス」「塗りつぶされる」
（1 回目の報告で前例なしとしていた 5 語）は、この 2 回目の時点では
`TODO.md`（`HEAD`）そのものに前例があり、造語かどうかを問う対象では
なくなっている。今回新しく増えた文章
（`archives/todo/` の「きっかけ」以降、「原因」「やったこと」「テスト」、
`README.md`、`verifier-request.md`、`verifier-report.md`）だけを見た。

## 前例の無い語（8 語）

前例の件数が少ない順（すべて 0 件）。

### 軒並み

- 出てくる箇所: `archives/todo/TODO-096. …md`
  「アイコンが軒並み黒く塗りつぶされて表示された」
- `git grep` の件数: 前例なし
- 見立て: 一般的な日本語の語彙で、造語ではない。単にこのリポジトリで
  初めて使っただけと見える

### 整形式

- 出てくる箇所: `archives/todo/TODO-096. …md`「整形式の XML のままで」、
  `verifier-request.md` 3「整形式の XML であること」、
  `verifier-report.md` 3「○ 整形式の XML」
- `git grep` の件数: 前例なし
- 見立て: XML の well-formed の訳語として一般的。造語ではない

### ベースパス

- 出てくる箇所: `verifier-report.md`「静的ファイルの URL は
  `/static/...` ではなく `/ytsched/static/...` がベースパスだった」
- `git grep` の件数: 前例なし
- 見立て: base path の訳語として一般的な IT 用語。造語ではない

### 潰されない

- 出てくる箇所: `archives/todo/TODO-096. …md`「継承より優先されるので
  この指定に潰されない」
- `git grep` の件数: 前例なし（「潰され」の活用形としては 3 件あるが、
  「潰されない」の形は初出）
- 見立て: 普通の日本語の活用違い。造語ではない

### 実体のタグ

- 出てくる箇所: `verifier-report.md`「コメント中に『ここに `<style>` を』
  という日本語文言が 1 件あるだけ（実体のタグではない）」
- `git grep` の件数: 前例なし
- 見立て: 「（コメントの中の文字列であって）実際にパースされるタグでは
  ない」という意味で使っている。意味は通るが、この文書だけの言い回しに
  見える。他の書き方（「タグとしては存在しない」等）でも足りそう

### 漏れなし

- 出てくる箇所: `verifier-report.md`「いずれも直前の `<svg
  class="my-icon …">` に `my-icon` クラスが付いている（漏れなし）」
- `git grep` の件数: 前例なし
- 見立て: 一般的な日本語。造語ではない

### 壊れうる

- 出てくる箇所: `README.md`「どちらも『直したところ』ではなく『直した
  せいで壊れうるところ』で、依頼書に書き出して見てもらった」
- `git grep` の件数: 前例なし
- 見立て: 「壊れる」＋「うる（得る）」の普通の活用。造語ではない

### 本当に黒くなる

- 出てくる箇所: `verifier-request.md`「持たないものがあると、そこだけ
  今度は本当に黒くなる」、`README.md` にも同じ言い回し
- `git grep` の件数: 前例なし
- 見立て: 語というより言い回し（フレーズ）。個々の語（「本当に」
  「黒くなる」）はどちらも一般的で、造語ではない

## 判断が要る語（前例はあるが、コミット間で見立てが割れうる語）

以下の 4 語は `git grep` の件数だけを見れば前例があるが、着眼した
ので併せて書く。

- **取りこぼし**（`README.md` 2 箇所）— 前例 17 ファイル。TODO-027 以来
  何度も使われている、このリポジトリでの定番語。一般的な日本語でもあり、
  問題ないと見立てる
- **切り分け**（`verifier-report.md` は使っていないが `README.md` の
  分担の文脈に近い語として検討）— 前例 28 ファイル。一般的な IT・
  デバッグ用語で定着している
- **目視**（`README.md`「描画の目視確認」）— 前例 22 ファイル。
  一般的な語で定着している
- **軒並み** — 前例なし（上の「前例の無い語」に計上済み）。単独では
  前例が無いが、日常語であり、他の 3 語と並べて見ても造語には
  見えない

いずれも「前例なし＝造語ではない」（`.claude/agents/wording.md` 参照）
の原則どおり、前例の有無にかかわらず一般に通用する語と見立てる。
決めるのは main。

## 読んだファイル

- `TODO.md`
- `archives/todo/TODO-096. Android の Firefox でアイコンが黒く塗りつぶされる.md`
- `archives/agents/TODO-096/README.md`
- `archives/agents/TODO-096/verifier-request.md`
- `archives/agents/TODO-096/verifier-report.md`

## 前例の無い語数

8 語（軒並み、整形式、ベースパス、潰されない、実体のタグ、漏れなし、
壊れうる、本当に黒くなる）。

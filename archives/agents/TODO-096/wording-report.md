# TODO-096 wording 報告

対象: `TODO.md` の未コミット差分（`git diff TODO.md`）に含まれる
「## TODO-096. Android の Firefox でアイコンが黒く塗りつぶされる」の節と、
冒頭の残っている項目の行。

## 前例なしの語（件数の少ない順）

### 消去キー
- 出てくる箇所: 「消去キーが黒い矢印になる」
- `git grep` の件数: 0 件（「消去」単独でも 0 件）
- 見立て: フッターの操作キーを指す呼び名。一般語としては通用するが、
  このリポジトリでこのキーをこれまで何と呼んでいたか（`clear` /
  `reset` などコード上の名前と揃っているか）は本文からは分からない。
  判断できない

### 漏斗
- 出てくる箇所: 「漏斗と `angle-down` も黒い三角になる」
- `git grep` の件数: 0 件
- 見立て: フィルタ（絞り込み）アイコンを指していると思われる。一般語
  ではあるが、このリポジトリでフィルタアイコンを「漏斗」と呼ぶのが
  初出。他の箇所で「フィルタアイコン」のような呼び方をしていないか
  確認の余地がある

### 塗りつぶされる
- 出てくる箇所: 節の見出し「アイコンが黒く塗りつぶされる」、
  本文「黒く塗られた形」（「塗られた」は 0 件、「塗りつぶされる」も 0 件、
  近い語形「塗り」12 件・「塗る」4 件は既出）
- `git grep` の件数: 0 件
- 見立て: ごく普通の日本語の言い回しで、造語には見えない。活用形の
  違いによる不一致の可能性が高い

### 輪郭パス
- 出てくる箇所: 「いずれも『線画の輪郭パスが黒く塗られた形』で」
- `git grep` の件数: 0 件（「線画」自体は 8 件で既出）
- 見立て: SVG のパス（アウトライン）を指す一般的な言い方で、専門用語
  としては通用しそう。ただしこのリポジトリで SVG のパスをこれまで
  何と呼んでいたかは不明。判断できない

### shadow tree
- 出てくる箇所: 「中身は shadow tree になり」「shadow tree の中まで届く」
- `git grep` の件数: 0 件
- 見立て: Shadow DOM の一般的な技術用語（`<use>` 要素が参照先の内容を
  展開する仕組み）。一般に通用する専門用語で、無理に和訳しない方針
  （CLAUDE.md）にも沿う。このリポジトリでは初出だが、造語ではないと見立てる

## 前例があった語（参考、件数は前と同じかそれ以上）

線画 8 件、継承 20 件、虫めがね 1 件、`angle-down` 7 件、
`circle-up-fill` 11 件、`dot-circle` 4 件、`my-icon-9x` 6 件、
`my-icon` 36 件、`icons.svg` 21 件、既定 269 件、フッター 50 件。
いずれも既存のコードコメントやアイコン名・過去の TODO 文書で使われて
おり、今回新しく作った語ではないと見立てる。

## 読んだファイル

- `TODO.md`（未コミットの差分のみ）

## 前例なしの語数

5 語（消去キー、漏斗、塗りつぶされる、輪郭パス、shadow tree）

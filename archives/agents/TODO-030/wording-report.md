# TODO-030 wording 報告

前例の有無は `git grep -cF <語> HEAD -- '*.md'`（未コミットの新規ファイルは
すべて 0 件になるのは当然なので、既存ファイル群への前例だけを見た）。

前例の件数が少ない順（0 件をまとめて先に、その中は出現順）。

## 前例なし（0 件）

- **走らせ方** — `tests/README.md:3`「走らせ方（`mise run test` や…）」、
  `docs/Developer.md:87`「## テストの走らせ方」、`TODO.md:149,165`、
  `writer-request.md`・`writer-report.md`・`verifier-request.md`・
  `verifier-report.md` にも頻出。前例なし。見立て: 「(コマンドの)
  実行のしかた」を短くした言い換えに見えるが、`docs/Developer.md` の
  見出しにもなっていて、この TODO-030 のコミット全体で統一して使われて
  いる（食い違いは無い）。一般には「実行方法」「使い方」でも通じるが、
  これ自体は自然な日本語で、造語というより新しい見出し語という印象

- **コードを触る前に読むこと / 触る前に** — `CLAUDE.md:18`（見出し）、
  `CLAUDE.md:21`、`writer-request.md:67`、`TODO.md:160`。前例なし。
  見立て: 普通の日本語で、造語には見えない

- **移し漏れ** — `verifier-request.md:3,54`、
  `verifier-report.md:127,147,167`（見出し「## 4. 移し漏れ」にもなって
  いる）。前例なし。見立て: 「移し忘れ」「漏れ」でも通じるが、
  「移し漏れ」という複合語自体は自然。ただし後述の「移り先」と語幹が
  違う言い方が同じ文書内で併用されている点は気になる

- **移り先** — `verifier-request.md:57,61`、
  `archives/agents/TODO-030/README.md:13`「消した記述の移り先」。
  前例なし。見立て: 「移し漏れ」と対になる語で、同じ verifier 系の
  文書内では一貫しているが、他の担当（writer）の報告には出てこない。
  一般的な日本語ではあるので通用しそう

- **落として構わない** — `verifier-request.md:62`「『Claude 向けだった
  ので落として構わない』と自分で判断せず」。前例なし。見立て:
  「（記述を）省略してよい」の意味で「落とす」を使っており、
  一般に通じる用法だが、このリポジトリでは初出

- **案内** — `writer-report.md:26`「`docs/data-format.md` への案内にした」。
  前例なし。見立て: 「リンクにした」を言い換えているだけで、一般的な
  語。他の文書（`CLAUDE.md` 本文など）では同じ内容を「案内」ではなく
  単に「〜への短い節」「リンク」と表現しており、報告書だけで使われた
  言い換えに見える

- **読み物** — `writer-request.md:23`「`CLAUDE.md` は Claude 向けの
  箇条書きで、読み物になっていない」。前例なし。見立て: 一般的な語。
  造語ではなさそう

- **掛ける（漢字）** — `docs/Developer.md:20`「型チェック（2 つとも
  掛ける）」。**「かける」自体（ひらがな）は `TODO.md:88`・
  `docs/data-format.md:249,252` に前例がある**（「`migrate` にかけると」
  「`html.unescape()` を 2 回かける」＝処理を適用する、の意味で使用
  済み）。漢字表記「掛ける」としては前例なし。見立て: 意味・用法は
  既存の使い方と同じで、表記が漢字かひらがなかだけの違い。造語ではない

## 前例あり（参考。念のため確認したもの）

- **生残り** — `verifier-report.md:75`「`{{` `{%` の生残りは 0 件」。
  `archives/agents/TODO-003・005・006・007・012・016・021` の
  verifier-report で使われており前例多数（8 件）。この担当（verifier）
  で定着した言い回しで、造語ではない

- **ゴールデンマスター**・**見立て**・**勘所** — いずれも前例多数
  （TODO-020〜029 で使用済み）。既に定着している語

## 読んだファイル

- 新規: `src/README.md`、`docs/Developer.md`、`tests/README.md`
- 変更: `CLAUDE.md`、`README.md`、`docs/data-format.md`、`TODO.md`
  （`git diff HEAD` で差分のみ確認）
- `archives/agents/TODO-030/` の `README.md`・`writer-request.md`・
  `writer-report.md`・`verifier-request.md`・`verifier-report.md`

## まとめ

前例の無い語: **7 語**（走らせ方・コードを触る前に読むこと・移し漏れ・
移り先・落として構わない・案内・読み物）＋ 表記だけ前例が無いもの 1 語
（掛ける＝漢字表記のみ、ひらがな「かける」には前例あり）。

いずれも一般的な日本語で、TODO-021 の「足場」のような専門用語の言い換え
（意味が変わってしまう造語）には見えない。強いて挙げるなら、**「移し漏れ」
と「移り先」が同じ verifier 系の文書内でしか使われておらず、writer 側の
文書には出てこない**点と、**「案内」が `writer-report.md` だけの
言い回し**である点は、決めるならリポジトリ全体で語を揃えるかどうかの
判断材料になる。

## 追記: TODO-031 の節

対象は `TODO.md` の「## TODO-031. 文書に Mermaid の図を入れる」の節と、
冒頭の「残っている項目: …」の行（`TODO.md:3`）。固有名詞
（Mermaid・classDiagram・sequenceDiagram・graph・mmdc）は対象外。

前例なし（0 件）:

- **積み上がり** — `TODO.md:204`「`SchedDataEnt` / `SchedDataFile` /
  `SchedData` の積み上がり（`classDiagram`）」。前例なし。見立て:
  一般的な日本語（「積み重なり」の意）で、`src/README.md` の「下から
  上へ積み上がっている」（今回の対象外だが同じ語幹）とも合っている。
  造語ではなさそう
- **決め打ち** — `TODO.md:217`「色を決め打ちにしない」。前例なし。
  見立て: IT でよく使う口語（「固定値にする」の意）で一般に通用する。
  このリポジトリでは初出なだけ
- **座標** — `TODO.md:196,217` 付近「座標を自分で決めることになり」
  「画面の座標そのものを説明する図」。前例なし。見立て: 普通名詞。
  問題なし

前例あり（参考）:

- **絞る** — `TODO.md:213`「全部入れると多すぎるので、**絞る**」。
  `.claude/agents/reviewer.md` ほか複数に前例あり（6 件）。定着した語
- **採らない** — `TODO.md:222`「`mermaid-cli`（`mmdc`）で SVG に書き出す
  案は**採らない**」。`archives/agents/TODO-026/implementer-request2.md`
  に前例 1 件。造語ではない
- **書き出す** — `TODO.md:196,222`「SVG に書き出す」。前例多数
  （`.claude/agents/wording.md` ほか）。定着した語

まとめ: 前例の無い語は **3 語**（積み上がり・決め打ち・座標）。いずれも
一般的な日本語で、専門用語の独自言い換えには見えない。

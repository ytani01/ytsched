# TODO-047 wording 報告

対象: `TODO.md`（今回追加された「## TODO-047. Bootstrap をやめて、素の CSS
にする」の節と、冒頭の 2 行のみ。他は既存記述）。

`git diff --name-only -z HEAD -- '*.md' | tr '\0' '\n'` で `TODO.md` の
1 ファイルのみが対象と確認。

## 前例の無い語（件数の少ない順）

すべて `git grep -cF <語> HEAD -- '*.md'` の合計件数。

- **素の CSS**（見出し「Bootstrap をやめて、素の CSS にする」）
  0 件 / 見立て: 一般的な言い回し（「フレームワークを使わない生の CSS」の
  意味）。造語ではなく、単にこのリポジトリで CSS の話をするのが初めて
  なだけと見る
- **flexbox**（「`row` / `col-N` は Bootstrap では flexbox だが、CSS Grid
  に置き換える」）0 件 / 見立て: CSS のレイアウト方式を指す一般的な専門
  用語。造語ではない
- **CSS Grid**（同上）0 件 / 見立て: CSS の標準機能の名称そのもの。
  造語ではない
- **align-self**（「Grid の `align-self: center` とは別物」）0 件 /
  見立て: CSS プロパティ名そのもの。造語ではない
- **詳細度**（「Bootstrap の詳細度に勝つためのもの」）0 件 / 見立て:
  CSS の specificity の訳語として一般に使われる。判断できない部分は、
  「詳細度」という訳語自体がこのリポジトリの言い換えなのか、CSS 分野で
  広く定着した訳語なのかを main で確認してほしい点
- **ドロップダウン**（「ドロップダウン・モーダル・折りたたみは使って
  いない」）0 件 / 見立て: UI 部品名として一般的。造語ではない
- **モーダル**（同上）0 件 / 見立て: 同上、一般的な UI 用語
- **折りたたみ**（同上）0 件 / 見立て: 一般的な日本語（アコーディオン
  UI の和訳としても定着）。造語ではない
- **grid / Grid**（大文字小文字問わず単独語としては 0 件。ただし
  「CSS Grid」としては上記で計上済み。二重計上を避けるため単独語としては
  参考情報のみ）

## 前例ありだが念のため確認した語

- Bootstrap 78 件、implementer 590 件、verifier 682 件、reviewer 377 件、
  グリッド 2 件、余白 18 件、配置 6 件、キャプチャ 16 件、
  Font Awesome 52 件、CDN 19 件、開閉 10 件、フレームワーク 1 件、
  min-width 5 件、vertical-align 2 件、突き合わせる 3 件、
  見た目を変えない 5 件、既定のフォント 1 件、縮まない 1 件、図形 26 件、
  important / !important 38・11 件 — いずれも前例ありなので対象外

## 読んだ範囲

`TODO.md` 全体のうち、冒頭の「残っている項目」「番号は」の 2 行と、
「## TODO-047.」節（1〜70 行目付近）。それ以外の既存記述は依頼により
対象外。

## まとめ

前例の無い語: **8 語**（素の CSS、flexbox、CSS Grid、align-self、
詳細度、ドロップダウン、モーダル、折りたたみ）。いずれも CSS・UI 分野の
一般的な用語・言い回しに見え、このリポジトリ独自の造語には見えない。
ただし「詳細度」がこのリポジトリだけの訳語かどうかは判断が付かないため、
main の確認を勧める。

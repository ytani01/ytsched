# TODO-076 wording 報告（2 回目）

対象: 完了コミットに入る 5 つの `.md`（前回は `TODO.md` の差分だけを見ていたため、
残り 4 つを含めて読み直した）。

- `archives/todo/TODO-076. ゲージの綴りを gage から gauge に直す.md`
- `archives/agents/TODO-076/README.md`
- `archives/agents/TODO-076/verifier-report.md`
- `archives/agents/TODO-076/wording-report.md`
- `TODO.md`（差分のみ）

前回「問題なし」と判断済みの `gauge` 系・「綴り」・「移行の手当てはしない」は
対象から外した。

## 前例なしの語

- **`過剰置換`**
  - 箇所: `README.md`「機械置換は『置換漏れ』と『片側だけ直って壊れる』…が
    起きやすく」／`verifier-report.md` 見出し「2. 置換漏れ・過剰置換」
  - `git grep -cF` の件数: 0（`置換` 単体は 49 件、`過剰` 単体は 0 件）
  - 見立て: 「置換しすぎて別の箇所まで書き換わる」ことを指す造語的な
    組み合わせ。意味は通るが、リポジトリでの言い回しとしては初出。
    一般に通用するかは判断できない

- **`置換漏れ`**
  - 箇所: `verifier-report.md` 見出し「2. 置換漏れ・過剰置換」
  - 件数: 0（`置換` 49 件、`漏れ` 56 件はいずれも既存）
  - 見立て: 「対応漏れ」「修正漏れ」と同系統の一般的な言い回しに見える。
    造語というより普通の複合語だと考える

- **`対象通り`**
  - 箇所: `verifier-report.md`「…の 7 ファイルが対象通り」
  - 件数: 0
  - 見立て: 「対象の通り」「想定通り」に近い意味だが、この形での言い回しは
    初出。読んで意味は通るので、書いた本人には気づきにくい類い。
    一般に通用するかは判断できない

## 前例ありとして除外した語（参考）

- `突き合わせ`（77 件）、`生残り`（30 件）、`識別子`（25 件）、
  `既知`（35 件）、`折り返し`（18 件）、`残存なし`（2 件）、
  `読み替え`（3 件）、`読み替える`（1 件）、`壊れている箇所`（1 件、
  既に他所で使われている）— いずれも前例があり、造語の対象にならない

## 読んだファイル

- `archives/todo/TODO-076. ゲージの綴りを gage から gauge に直す.md`
- `archives/agents/TODO-076/README.md`
- `archives/agents/TODO-076/verifier-report.md`
- `archives/agents/TODO-076/wording-report.md`
- `TODO.md`（`git diff HEAD~1 -- TODO.md` の追記・変更箇所）

## まとめ

前例の無い語: **2 語**（`過剰置換`、`対象通り`）＋ 判断が割れる `置換漏れ`。
数としては TODO-025 の目安（造語を作っていない文書は 1〜3 語）の範囲内で、
「足場」のような造語が紛れ込んでいる兆候（10 語超）は見られない。
決めるのは main。

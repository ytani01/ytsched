# TODO-076 wording 報告

対象: `git diff TODO.md` の追記部分（`TODO.md` のみ）。

## 前例なし（0 件）の語

- **`gauge` / `Gauge` / `GAUGE`**
  - 箇所: 「`gage` から `gauge` に直す」「`gauge` / `Gauge` / `GAUGE` に直す」
  - `git grep -cF` の件数: 0
  - 見立て: これはこの項目の主題そのもの（誤字を正しい英語綴りに直す）。
    造語ではなく、リポジトリ内でまだ一度も使われていない正しい綴りが
    初めて出てきただけ。問題ないと考える
- **`綴り`**
  - 箇所: タイトル「ゲージの綴りを `gage` から `gauge` に直す」、
    本文「誤った綴りを使っている」「当時のコードの綴りをそのまま残す」
  - 件数: 0
  - 見立て: 一般的な日本語の名詞（spelling の意味）。造語ではないと考える
- **`移行の手当てはしない`**
  - 箇所: 「移行の手当てはしない」
  - 件数（フレーズ全体）: 0。ただし「手当て」単独では 2 件ヒットあり
    （既存の用法がある）
  - 見立て: 普通の言い回し。組み合わせとして前例が無いだけで、
    造語ではないと考える

## 前例が少ないが 0 ではない語（参考）

- `sessionStorage`（38 件）、`ytsched_gage_monday`（1 件、TODO-058 由来）、
  `DAYS_GAGE_K`（15 件）、`DAYS_GAGE_MAX`（16 件）、`my-gage`（123 件）
  はいずれも既存コード中の識別子で、前例あり。造語の対象にならない

## 読んだファイル

- `TODO.md`（`git diff TODO.md` で追記された箇所のみ）

## まとめ

前例の無い語（0 件）: **3 語**（`gauge` 系、`綴り`、「移行の手当てはしない」）。
いずれも見立てとしては問題ない語（正しい英語綴り、一般的な日本語）と考えるが、
判断は main に委ねる。

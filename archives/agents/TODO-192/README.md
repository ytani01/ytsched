# TODO-192 の分担

## 誰に何を担当させたか

| 担当 | 何を |
|------|------|
| main | `.claude/hooks/guard-bash-ytsched.sh` の実装、`.claude/settings.json` への登録 |
| verifier（Sonnet 5） | 判定の確認。止めるべきものが止まるか、**正しい書き方が素通りするか** |

- [verifier の報告](verifier-report.md)（1 回目・再確認・再々確認・最終確認の 4 節）

## その分担にした理由

シェルスクリプトで、挙動がある。文書だけの項目ではないので、確認は main から
分けた。実装は 1 ファイルなので implementer は立てず、main が書いた。

reviewer は立てていない。新規のフックで、既存の分岐や条件式を変える項目では
ないため。ただし結果としては、verifier が挙げた指摘の多くが「動くか」ではなく
「この判定でよいか」という質のものだった（`git` を丸ごと許可してよいか、など）。

## やり取りの回数

verifier は 1 回目のあと、`SendMessage` で 3 回続けた（新しく立て直すと
前回のケースを渡し直すことになるため）。指摘は 6 件 → 2 件 → 1 件 → 1 件と
減って収束した。
